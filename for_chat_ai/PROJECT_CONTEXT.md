# 🧠 Слепок контекста проекта "Эффект Манделы"

**Дата создания:** 2025-01-18  
**Последнее обновление:** 2025-01-27  
**Версия:** 2.1

---

## 1. Описание и Цели

**"Эффект Манделы"** — это интерактивный каталог феноменов коллективной памяти с AI-генерацией контента и системой голосования.

**Основная концепция:**
- Пользователи исследуют эффекты Манделы (коллективные ложные воспоминания)
- Голосуют за "ложную память" (Вариант А) или "реальность" (Вариант Б)
- Оставляют комментарии с доказательствами (Архив Аномалий)
- Получают персональный архетип личности на основе своих голосов
- Админы управляют контентом через AI-агентов (генерация, поиск, стилизация)

**Целевая аудитория:** Люди, интересующиеся феноменами коллективной памяти, теориями заговора, научной фантастикой.

---

## 2. Технологический стек

### Frontend
- **Next.js 15+** (App Router) - React фреймворк
- **React 18.3** - UI библиотека
- **TypeScript** - типизация
- **Tailwind CSS 3.4** - стилизация
- **Framer Motion 12.23** - анимации
- **Recharts 3.3** - графики и статистика
- **Lucide React** - иконки
- **react-hot-toast** - уведомления

### Backend
- **Next.js Server Actions** - серверная логика
- **Prisma ORM** - работа с БД
- **PostgreSQL** (Supabase) - база данных
- **Supabase Storage** - хранение файлов

### AI & External Services
- **OpenRouter** - агрегатор AI моделей
- **Google Gemini** - основная AI модель
- **DeepSeek V3** - дешевая модель для обработки данных (через OpenRouter)
- **Claude (Anthropic)** - резервная модель
- **Llama** - fallback модель
- **Perplexity (Sonar)** - поиск эффектов в интернете
- **Exa.ai** - поиск остатков (Residue) для RAG (Retrieval-Augmented Generation)
- **Pollinations.ai (Flux)** - генерация изображений
- **Gemini API** - извлечение изображений из чата
- **Wayback Machine CDX API** - поиск архивных изображений (1996-2012)

### Инфраструктура
- **Vercel** - хостинг и деплой
- **GitHub** - версионирование
- **Supabase** - БД и Storage

---

## 3. База Данных (Актуальная схема)

```prisma
// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model Effect {
  id          String  @id @default(cuid())
  title       String
  description String
  content     String
  category    String
  imageUrl    String?
  videoUrl    String?
  
  // Источник изображения
  imageSourceType  ImageSourceType?
  imageSourceValue String? // URL к оригиналу, имя файла, или null для загрузки
  
  // Статистика
  votesFor     Int @default(0)
  votesAgainst Int @default(0)
  views        Int @default(0)
  
  // Новые фичи
  residue          String? // Культурные следы
  residueSource    String? // Ссылка для остатков
  history          String? // История открытия
  historySource    String? // Ссылка для истории
  yearDiscovered   Int?
  similarEffectIds String[] @default([]) // Массив ID похожих
  currentState     String? // Текущее состояние (факты)
  
  // Интерпретации (научные объяснения и версии сообщества)
  interpretations Json?

  // Видимость эффекта
  isVisible Boolean @default(true) // Скрыт ли эффект от публичного каталога
  
  submissions Submission[] // Связь, если эффект создан из заявки
  comments    Comment[] // Комментарии к эффекту

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

model Submission {
  id String @id @default(cuid())
  
  // Основные поля формы
  category String
  title    String
  question String
  variantA String
  variantB String
  
  // Дополнительные поля
  currentState   String? // "Описание текущего состояния"
  sourceLink     String? // "Ссылка на источник"
  submitterEmail String? // "Ваш email"
  
  // Поле для блока "Интерпретации" (храним как JSON объект, так как там может быть много подпунктов)
  interpretations Json? 

  // Технические поля
  status    String   @default("PENDING") // PENDING, APPROVED, REJECTED
  createdAt DateTime @default(now())
  
  // Связь с созданным эффектом (на будущее)
  createdEffectId String?
  createdEffect   Effect? @relation(fields: [createdEffectId], references: [id])
}

model Category {
  id        String   @id @default(cuid())
  slug      String   @unique // например "films", "brands"
  name      String // например "Фильмы и сериалы"
  emoji     String // например "🎬"
  color     String? // цвет для бейджа, опционально (например "red", "blue")
  sortOrder Int      @default(0) // порядок сортировки
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}

// Голоса пользователей (привязаны к анонимному visitorId)
model Vote {
  id        String   @id @default(cuid())
  visitorId String // Анонимный ID посетителя (из localStorage/cookie)
  effectId  String // ID эффекта
  variant   String // 'A' или 'B'
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@unique([visitorId, effectId]) // Один голос на эффект от одного посетителя
  @@index([visitorId])
  @@index([effectId])
}

// Профили пользователей (для будущей авторизации)
model UserProfile {
  id         String   @id @default(cuid())
  visitorId  String   @unique // Связь с анонимным ID
  email      String?  @unique // Email для Magic Link (опционально)
  provider   String? // 'email', 'google', 'github' (опционально)
  providerId String? // ID от провайдера OAuth (опционально)
  createdAt  DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@index([email])
}

// Результаты генерации цифрового ID Хроно-Путешественника
model IdentityResult {
  id          String   @id @default(cuid())
  syncRate    Int // Процент синхронизации с текущей реальностью (0-100)
  archetype   String // Заголовок (например: "Сквозной Дрифтер")
  description String // Уникальное описание от AI
  stats       Json? // Хранение статистики по категориям { "films": 80, "brands": 20 }
  createdAt   DateTime @default(now())

  // Опциональная связь, если пользователь авторизован
  userId String?
}

// Комментарии к эффектам (Архив Аномалий)
model Comment {
  id        String @id @default(cuid())
  effectId  String // ID эффекта
  visitorId String // Анонимный ID пользователя

  // Тип комментария
  type CommentType // WITNESS, ARCHAEOLOGIST, THEORIST

  // Контент
  text     String
  imageUrl String? // Внешняя ссылка на изображение (для Археологов)
  videoUrl String? // Внешняя ссылка на видео (для Археологов)
  audioUrl String? // Внешняя ссылка на аудио (для Археологов)

  // Для Теоретиков - выбор базовой теории
  theoryType String? // "quantum", "timeline", "memory", "simulation", "other"

  // Модерация
  status      CommentStatus @default(PENDING) // PENDING, APPROVED, REJECTED
  moderatedAt DateTime?

  // Взаимодействие
  likes   Int @default(0)
  dislikes Int @default(0)
  reports Int @default(0)

  // Связь с эффектом
  effect Effect @relation(fields: [effectId], references: [id], onDelete: Cascade)
  
  // Лайки пользователей
  commentLikes CommentLike[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  
  @@index([effectId])
  @@index([visitorId])
  @@index([status])
  @@index([createdAt])
}

// Лайки/дизлайки комментариев
model CommentLike {
  id        String   @id @default(cuid())
  commentId String
  visitorId String
  isLike    Boolean  // true = лайк, false = дизлайк
  
  comment Comment @relation(fields: [commentId], references: [id], onDelete: Cascade)
  
  createdAt DateTime @default(now())
  
  @@unique([commentId, visitorId]) // Один лайк/дизлайк на комментарий от одного пользователя
  @@index([commentId])
  @@index([visitorId])
}

enum CommentType {
  WITNESS // Свидетели - простые комментарии
  ARCHAEOLOGIST // Археологи - с фото/видео/аудио
  THEORIST // Теоретики - с выбором теории
}

enum CommentStatus {
  PENDING
  APPROVED
  REJECTED
}

enum ImageSourceType {
  YANDEX
  GOOGLE
  URL
  UPLOAD
  AI_GENERATED
  GEMINI
}
```

---

## 4. Ключевой функционал (Подробно)

### А. Пользовательский интерфейс

#### Главная страница (`app/page.tsx`, `app/HomeClient.tsx`)

**Структура:**
1. **Hero секция:**
   - Заголовок с глитч-эффектом ("ЭФФЕКТ МАНДЕЛЫ")
   - Описание проекта
   - HUD статистика (Эффектов, Участников, Голосов, Твой вклад)

2. **Эффект дня (Effect of the Day):**
   - **Логика выбора:** Детерминированный выбор на основе текущей даты
     - Функция `getDeterministicIndex()` в `app/actions/effects.ts`
     - Использует хеш текущей даты (YYYY-MM-DD) для выбора индекса
     - Сортирует эффекты по `createdAt` для стабильности
     - **НЕ кэшируется** - пересчитывается при каждом запросе
     - Обновляется в полночь (00:00:00)
   - **Отображение:**
     - Карточка с изображением и глитч-эффектом
     - Статистика голосования (Мандела % / Реальность %)
     - Прогресс-бар с градиентами
     - Счетчик до следующего обновления
     - "SHIFT LOG #XXX" (номер дня года)
     - Настроение сдвига (на основе процента Манделы)
   - **Динамическое обновление:** При голосовании пользователя данные обновляются через `getEffectById()`

3. **Тренды:**
   - Топ-3 эффекта по сумме голосов (исключая эффект дня)
   - Сортировка: `(votesFor + votesAgainst) DESC`
   - Бейджи с номерами (#1, #2, #3)

4. **Новые обнаружения:**
   - Последние 6 эффектов по дате создания
   - Сортировка: `createdAt DESC`
   - Бейдж "Новое"

5. **Quick Actions:**
   - Случайный эффект (редирект)
   - Тест памяти (квиз)
   - Полный архив (каталог)

**Компоненты:**
- `EffectCard` - карточка эффекта с бейджами новых комментариев
- `ImageWithSkeleton` - умная загрузка изображений
- `GlitchTitle` - заголовок с эффектом глитча

#### Каталог (`app/catalog/`)

**Функционал:**
- Фильтрация по категориям
- Сортировка (популярность, новые, спорные)
- Поиск по названию/описанию
- Пагинация
- Бейджи новых комментариев (на основе localStorage)

#### Страница эффекта (`app/effect/[id]/`)

**Структура:**
1. **Hero:**
   - Изображение эффекта
   - Название и описание
   - Категория

2. **Голосование:**
   - **Оптимистичный UI:**
     - Мгновенное обновление UI при клике (без ожидания сервера)
     - Анимация выбора
     - Показ результата сразу
   - **Server Action:**
     - Асинхронное сохранение в БД
     - Пересчет статистики
     - Обновление UI после завершения
   - **Защита:**
     - Один голос на эффект от `visitorId`
     - Сохранение в localStorage для офлайн-режима
     - Автоматическая синхронизация при возврате

3. **Аккордеон с секциями:**
   - **Текущее состояние** (зеленый) - факты
   - **Научное объяснение** (синий) - почему путают
   - **История** (желтый) - как появился миф
   - **Культурные остатки** (розовый) - примеры в медиа
   - **Архив Аномалий** (фиолетовый) - комментарии пользователей

4. **Архив Аномалий (Комментарии):**
   - **Реализация:** Компонент `ArchiveAnomalies` (`components/comments/ArchiveAnomalies.tsx`)
   - **Типы комментариев:**
     - **WITNESS** (Свидетели) - простые текстовые комментарии
     - **ARCHAEOLOGIST** (Археологи) - с прикрепленными медиа (фото/видео/аудио)
     - **THEORIST** (Теоретики) - с выбором теории (квантовая, временные линии, память, симуляция)
   - **Вложенность:** Плоская структура (нет вложенных ответов)
   - **Модерация:**
     - Все комментарии создаются со статусом `PENDING`
     - Отображаются только `APPROVED` комментарии
     - Админ одобряет/отклоняет в админ-панели
   - **Взаимодействие:**
     - Лайки/дизлайки (через `CommentLike`)
     - Один лайк/дизлайк на комментарий от пользователя
   - **Отслеживание прочитанных:**
     - `lib/comments-tracker.ts` - localStorage для отслеживания
     - Бейджи "новых комментариев" на карточках эффектов
     - Автоматическое обновление при открытии аккордеона

5. **Форма комментария:**
   - Текстовое поле (до 5000 символов)
   - Поле для ссылки на медиа (опционально)
   - Автоматическое определение типа медиа (YouTube, Rutube, Яндекс.Музыка и т.д.)
   - Автоматическое определение типа комментария (ARCHAEOLOGIST если есть медиа, иначе WITNESS)
   - Валидация URL через `lib/security/media-whitelist.ts`
   - Rate limiting (максимум 10 комментариев в час)

#### Личный кабинет (My Memory) (`app/my-memory/`)

**Функционал:**
- **Архетип личности:**
  - Генерация на основе статистики голосов
  - Процедурная генерация через `lib/identity-engine.ts`
  - Использует биометрию устройства (платформа, разрешение, часовой пояс)
  - Reality ID (например: "Dimension WIN-1920-Alpha")
  - Архетип (прилагательное + существительное)
  - Описание от AI
  - Цитата
- **Статистика:**
  - Процент синхронизации с реальностью (SyncRate)
  - Распределение по категориям
  - Графики (Recharts)

### Б. Админ-панель и Модерация

#### Управление эффектами (`app/admin/AdminClient.tsx`)

**Функционал:**
- CRUD операции (создание, редактирование, удаление)
- Поиск и фильтрация
- Управление видимостью (`isVisible`)
- Загрузка изображений (Supabase Storage)
- Поддержка ссылок на изображения (Yandex, Google, URL, Upload, Gemini)

#### AI-Агенты

**1. Генератор контента** (`app/actions/generate-content.ts`):
- **Каскад моделей:**
  1. DeepSeek V3 через OpenRouter (приоритет 1) - дешевая обработка
  2. Google Gemini (приоритет 2) - резервная модель
- **RAG (Retrieval-Augmented Generation):**
  - Интеграция с Exa.ai для поиска остатков (Residue)
  - Автоматический поиск доказательств до 2023 года
  - Поддержка русского и английского языков
  - Автоматическое заполнение `residueSource` из найденных ссылок
  - Фильтрация низкокачественных источников (wikipedia.org, habr.com, meduza.io, vc.ru)
  - Fallback на Google Dorks если Exa.ai не находит результаты
- **OSINT инструменты:**
  - **Google Dorks** (`lib/dorks.ts`):
    - Генерация специализированных поисковых запросов
    - Поддержка русского и английского языков
    - Платформы: Google, Reddit, YouTube, eBay, Archives, Blogs
    - Исключение новостных сайтов для более точного поиска
  - **Wayback Machine** (`app/actions/wayback-search.ts`):
    - Поиск архивных изображений (1996-2012)
    - Поддержка кириллицы и латиницы
    - Стратегия поиска: сначала домен (*query.com/*), затем подстрока
    - Фильтрация мусора (кнопки, пиксели, навигация)
    - Детальное логирование для отладки
- **UI компонент:** `components/admin/modals/GenerationDialog.tsx` (v2.4 PRO):
  - Многошаговый процесс: Input → Processing → Review
  - Вкладки OSINT: Dorks и Wayback Machine
  - Lightbox для просмотра архивных изображений
  - Редактирование промптов и ссылок перед применением
- **Функционал:**
  - Генерация структурированного JSON (currentState, scientific, community, history, residue)
  - Валидация входных данных (проверка на адекватность запроса)
  - Автоматическое переключение при перегрузке
  - Генерация промпта для изображения (на английском, Visual Profiler)
  - Генерация изображения через Flux (Pollinations.ai)
  - Стилизация изображений (img2img) - не реализовано (Phase 4)
  - Ресайз изображений под формат - не реализовано (Phase 4)

**2. Агент-Охотник** (`app/actions/find-new-effects.ts`):
- **Процесс:**
  1. Perplexity (Sonar) ищет в интернете реальные эффекты Манделы
     - Reddit r/MandelaEffect
     - Wikipedia статьи
     - Новостные статьи и блоги
     - YouTube видео
     - Онлайн форумы
  2. Google Gemini форматирует результаты в JSON
     - Исключает дубликаты (сравнение с существующими эффектами)
     - Структурирует данные (title, question, variantA, variantB, category)
     - Валидирует качество найденных эффектов
- **Результат:** Массив новых эффектов, готовых к добавлению в базу

**3. Нейро-Синтезатор v2.4 PRO** (`components/admin/modals/GenerationDialog.tsx`):
- **UI для генерации контента:**
  - Многошаговый процесс: Input → Processing → Review
  - Выбор стиля визуализации (Кино, VHS 90-х, Полароид, Газета, 3D)
  - Редактирование промптов перед генерацией
  - Просмотр и редактирование сгенерированных данных
  - Перегенерация только изображения
  - Структурированный просмотр ссылок (Residue, История, Наука, Сообщество)
  - Логи процесса генерации в реальном времени
- **OSINT инструменты (блок "Разведка"):**
  - **Вкладка Dorks:**
    - Автоматическая генерация Google Dorks через `lib/dorks.ts`
    - Ссылки на форумы, Reddit, YouTube, eBay
    - Поддержка русского и английского языков
  - **Вкладка Wayback Machine:**
    - Поиск архивных изображений (1995-2010)
    - Сетка изображений (2-3 колонки)
    - Lightbox для полноэкранного просмотра
    - Детальные логи поиска (SYSTEM LOGS)
    - Фильтрация мусора и дубликатов
- **Интеграция:** Используется в `EffectEditorModal.tsx` через кнопку "AI"

**4. Извлечение изображений из Gemini чата** (`app/actions/gemini-extract.ts`):
- Парсинг ссылки на Gemini чат
- Использование Gemini API для поиска изображений в чате
- Возврат URL изображения (без автоматического сохранения)

#### Модерация UGC

**Комментарии:**
- **Отправка:** Все комментарии создаются со статусом `PENDING`
- **Валидация:**
  - Минимум 3 символа, максимум 5000
  - Проверка доменов медиа через whitelist
  - Проверка типа файла
  - Rate limiting (10 комментариев в час)
- **Модерация в админке:**
  - Вкладка "Комментарии" в админ-панели
  - Список всех `PENDING` комментариев
  - Показ типа комментария, текста, медиа-ссылок
  - Кнопки "Одобрить" / "Отклонить"
  - После модерации комментарий появляется/исчезает на странице эффекта
- **Заявки (Submissions):**
  - Пользователи отправляют эффекты через форму `/submit`
  - Статус: `PENDING`, `APPROVED`, `REJECTED`
  - Админ может одобрить заявку и создать эффект (с AI-генерацией или вручную)

### В. AI Логика (`app/actions/`)

#### `generate-content.ts`
**Суть:** Генерация полного контента для эффекта Манделы через каскад AI моделей с RAG.

**Процесс:**
1. Валидация входных данных (проверка на адекватность)
2. **RAG: Поиск остатков через Exa.ai** (`lib/exa.ts`):
   - Определение языка запроса (кириллица/латиница)
   - Поиск доказательств до 2023 года
   - Русский: `"${query}" эффект манделы фото доказательство старая упаковка`
   - Английский: `"${query}" mandela effect residue evidence photo vintage`
   - Фильтрация низкокачественных источников (wikipedia.org, habr.com, meduza.io, vc.ru)
3. **Fallback на Google Dorks** (`lib/dorks.ts`):
   - Если Exa.ai не находит результаты или находит только низкокачественные
   - Используется первая ссылка из сгенерированных Dorks
   - Приоритет: форумы и обсуждения, исключая новостные сайты
4. Попытка генерации через DeepSeek V3 (через OpenRouter)
5. При ошибке - переключение на Google Gemini
6. Парсинг JSON ответа
7. Генерация промпта для изображения (Visual Profiler, на английском)
8. Генерация изображения через Flux (опционально)

**Функции:**
- `generateEffectData()` - основная генерация контента с RAG
- `generateEffectImage()` - генерация изображения
- `restyleImage()` - стилизация существующего изображения (не реализовано, Phase 4)
- `fitImageToFormat()` - ресайз под формат (не реализовано, Phase 4)

#### `lib/exa.ts`
**Суть:** Поиск остатков (Residue) через Exa.ai для RAG.

**Процесс:**
1. Определение языка запроса (кириллица/латиница)
2. Формирование поискового запроса в зависимости от языка
3. Поиск через Exa.ai API (neural search)
4. Фильтрация по дате (до 2023 года)
5. Возврат структурированных результатов (title, url, text, publishedDate)

**Функции:**
- `searchResidue(query: string)` - поиск остатков для эффекта

#### `find-new-effects.ts`
**Суть:** Автоматический поиск новых эффектов Манделы в интернете.

**Процесс:**
1. Perplexity ищет эффекты в интернете
2. Google Gemini форматирует в JSON
3. Исключение дубликатов
4. Валидация качества

**Функции:**
- `findNewEffects()` - основной поиск

#### `generate-identity.ts`
**Суть:** Генерация архетипа личности на основе голосов пользователя.

**Процесс:**
1. Подсчет статистики (SyncRate = процент совпадения с реальностью)
2. Определение архетипа по SyncRate
3. Генерация описания и цитаты через AI
4. Сохранение в БД

**Архетипы:**
- 90-100%: "Якорь Реальности"
- 65-89%: "Резонирующий Наблюдатель"
- 35-64%: "Сквозной Дрифтер"
- 10-34%: "Агент Парадокса"
- 0-9%: "Реликт Утерянной Эпохи"

**Функции:**
- `generateIdentity()` - генерация архетипа
- `getIdentityResult()` - получение результата

#### `identity-engine.ts` (lib)
**Суть:** Процедурная генерация архетипа на основе биометрии устройства.

**Процесс:**
1. Генерация Reality ID из биометрии (платформа, разрешение, часовой пояс)
2. Seed-генерация на основе данных
3. Выбор прилагательного по SyncRate
4. Выбор существительного по категории
5. Генерация описания из шаблонов

**Функции:**
- `generateRealityID()` - генерация ID реальности
- `generateArchetype()` - генерация архетипа

---

## 5. Дизайн-система

### Стиль
**Направление:** Cyberpunk / Glitch / Glassmorphism

**Характеристики:**
- Темная тема (Dark Mode) как основа
- Неоновые акценты (Cyan, Purple, Orange)
- Эффекты глитча и искажений
- Полупрозрачные элементы с размытием (backdrop-blur)
- Градиенты для акцентов

### Цветовая палитра
- **Cyan** (`#06b6d4`, `cyan-400`) - основной акцент
- **Purple** (`#9333ea`, `purple-600`) - вторичный акцент
- **Orange** (`#f97316`, `orange-500`) - третичный акцент
- **Black** (`#000000`) - фон
- **White** (`#ffffff`) - текст и границы

### Основные компоненты

#### `GlitchTitle` (`components/ui/GlitchTitle.tsx`)
- Заголовок с глитч-эффектом
- Первое слово белое, остальное - градиент
- Анимация появления (Framer Motion)
- CSS класс `glitch-text` для эффекта

#### `EffectCard` (`components/EffectCard.tsx`)
- Карточка эффекта для каталога
- Изображение с скелетоном загрузки
- Статистика голосования
- Бейджи (новое, тренд, новые комментарии)
- Hover эффекты

#### `ImageWithSkeleton` (`components/ui/ImageWithSkeleton.tsx`)
- Умная загрузка изображений
- Блюр-фон при загрузке
- Проксирование Google URL через `/api/image-proxy` для обхода CORS
- Автоматический ретрай при ошибках
- Плавная анимация появления
- Поддержка Next.js Image и нативного `<img>`

#### `ArchiveAnomalies` (`components/comments/ArchiveAnomalies.tsx`)
- Аккордеон для комментариев
- Загрузка комментариев через Server Action
- Отображение только одобренных комментариев
- Отслеживание прочитанных комментариев
- Автоматическое обновление бейджей

#### `CommentForm` (`components/comments/CommentForm.tsx`)
- Форма отправки комментария
- Автоматическое определение типа медиа
- Валидация URL
- Rate limiting предупреждения

#### `CommentCard` (`components/comments/CommentCard.tsx`)
- Карточка комментария
- Отображение типа (WITNESS, ARCHAEOLOGIST, THEORIST)
- Медиа (изображения, видео, аудио)
- Лайки/дизлайки
- Теории для THEORIST

### Анимации
**Библиотека:** Framer Motion

**Типы:**
- Появление элементов: `fadeIn`, `scale`
- Переходы между страницами: `PageTransition`
- Счетчики: `AnimatedCounter` с плавным увеличением
- Hover эффекты: плавные переходы цветов и теней
- Глитч-эффекты: CSS анимации с `@keyframes`

### Адаптивность
**Подход:** Mobile-first

**Breakpoints:**
- `sm:` - 640px (планшеты)
- `md:` - 768px (небольшие десктопы)
- `lg:` - 1024px (десктопы)
- `xl:` - 1280px (большие экраны)

---

## 6. Переменные окружения

### Обязательные

```env
# База данных
DATABASE_URL="postgresql://..."
DIRECT_URL="postgresql://..." # Прямое подключение (без пулера)

# Supabase
NEXT_PUBLIC_SUPABASE_URL="https://xxx.supabase.co"
SUPABASE_SERVICE_ROLE_KEY="xxx" # Или SUPABASE_SECRET_KEY
SUPABASE_SECRET_KEY="xxx" # Альтернатива SERVICE_ROLE_KEY

# AI провайдеры (минимум один)
GOOGLE_API_KEY="xxx" # Приоритет 2 (резервная модель)
OPENROUTER_API_KEY="xxx" # Для DeepSeek V3 (приоритет 1), Claude/Llama
EXA_API_KEY="xxx" # Для поиска остатков (RAG) - обязательно для генерации контента
SILICONFLOW_API_KEY="xxx" # Опционально
CEREBRAS_API_KEY="xxx" # Опционально
GROQ_API_KEY="xxx" # Опционально
HYPERBOLIC_API_KEY="xxx" # Опционально

# Сайт
NEXT_PUBLIC_SITE_URL="https://your-domain.com" # Для OpenRouter
```

### Опциональные

```env
# Админка
ADMIN_PASSWORD="xxx" # Пароль для входа в админку

# Дополнительные сервисы
PERPLEXITY_API_KEY="xxx" # Для поиска эффектов (через OpenRouter)
```

---

## 7. Важные детали реализации

### Производительность и кэширование
- **ISR (Incremental Static Regeneration):** `revalidate = 60` секунд для всех страниц
  - `app/page.tsx` - главная страница
  - `app/catalog/page.tsx` - каталог
  - `app/effect/[id]/page.tsx` - страница эффекта
- **Оптимизация:** Увеличен интервал кэширования с 1 секунды до 60 секунд для улучшения производительности

### Эффект дня
- **Детерминированный выбор:** Один и тот же эффект для всех пользователей в один день
- **Алгоритм:** Хеш текущей даты (YYYY-MM-DD) → индекс в отсортированном массиве
- **Обновление:** В полночь (00:00:00) автоматически меняется
- **Кэширование:** НЕТ - пересчитывается при каждом запросе (для актуальности)

### Комментарии
- **Модерация:** Все комментарии требуют одобрения админа
- **Whitelist доменов:** Только разрешенные сервисы (imgur, Яндекс, Google, YouTube и т.д.)
- **Rate limiting:** Максимум 10 комментариев в час от одного `visitorId`
- **Отслеживание:** localStorage для отслеживания прочитанных комментариев

### Голосование
- **Оптимистичный UI:** Мгновенное обновление без ожидания сервера
- **Офлайн-режим:** Сохранение в localStorage с последующей синхронизацией
- **Уникальность:** Один голос на эффект от `visitorId` (через `@@unique` в Prisma)

### AI Генерация
- **Каскад моделей:** Автоматическое переключение при ошибках
- **Валидация:** Проверка входных данных перед генерацией
- **Retry логика:** Повторные попытки при временных ошибках
- **Fallback:** Всегда есть запасной вариант (Llama через OpenRouter)

### Безопасность
- **Whitelist медиа:** Только разрешенные домены для изображений/видео
- **Валидация URL:** Проверка на подозрительные ссылки
- **Rate limiting:** Защита от спама
- **Server Actions:** Вся логика на сервере

---

## 8. Файловая структура (Ключевые файлы)

```
app/
├── actions/
│   ├── comments.ts          # Комментарии (создание, получение, модерация)
│   ├── effects.ts           # Эффекты (получение, эффект дня)
│   ├── generate-content.ts  # AI генерация контента с RAG и fallback на Dorks
│   ├── find-new-effects.ts  # Агент-Охотник
│   ├── generate-identity.ts # Генерация архетипа
│   ├── votes.ts             # Голосование
│   ├── wayback-search.ts    # Поиск архивных изображений в Wayback Machine
│   └── admin.ts             # Админ-панель
├── admin/
│   ├── AdminClient.tsx      # Клиент админки
│   └── page.tsx             # Серверная страница
├── effect/[id]/
│   ├── EffectClient.tsx     # Клиент страницы эффекта
│   └── page.tsx             # Серверная страница
└── page.tsx                 # Главная страница

components/
├── admin/
│   └── modals/
│       └── GenerationDialog.tsx  # Нейро-Синтезатор v2.4 PRO (AI + OSINT)
├── comments/
│   ├── ArchiveAnomalies.tsx # Аккордеон комментариев
│   ├── CommentForm.tsx      # Форма комментария
│   └── CommentCard.tsx      # Карточка комментария
└── ui/
    ├── GlitchTitle.tsx      # Заголовок с глитчем
    └── ImageWithSkeleton.tsx # Умная картинка

lib/
├── identity-engine.ts       # Генерация архетипа
├── identity-data.ts         # База текстов для архетипов
├── comments-tracker.ts      # Отслеживание прочитанных
├── exa.ts                   # Поиск остатков через Exa.ai (RAG)
├── dorks.ts                 # Генератор Google Dorks для OSINT поиска
├── security/
│   └── media-whitelist.ts   # Whitelist доменов
└── visitor.ts               # Генерация visitorId
```

---

## 9. Полезные ссылки

- **Документация структуры:** `PROJECT_STRUCTURE.md`
- **Правила разработки:** `.cursor/rules/`
- **Уроки и исправления:** `.cursor/rules/lessons-learned.mdc`

---

**Последнее обновление:** 2025-01-27

### Последние изменения (2025-01-27)
- ✅ Интеграция Exa.ai для поиска остатков (RAG)
- ✅ Нейро-Синтезатор: улучшенный UI для генерации контента
- ✅ Оптимизация производительности: увеличен интервал кэширования до 60 секунд
- ✅ Улучшен поиск Exa.ai для русского языка
- ✅ Исправлены типы TypeScript (Category.color, StatsClientWrapper)

