# Prompt Engineer MCP - Краткая инструкция

## 🎯 Что это

MCP сервер для оптимизации промптов через Claude 3 Sonnet. Переписывает ваши промпты для лучших результатов в Cursor AI.

## ✅ Установка (уже сделано)

Сервер установлен локально в `C:\Temp\prompt-engineer-mcp-server` и добавлен в `mcp.json`.

**Что было сделано:**

1. ✅ Клонирован репозиторий из GitHub
2. ✅ Установлены зависимости (`npm install`)
3. ✅ Настроена конфигурация в `mcp.json`

## 🔑 Настройка API ключа

### Шаг 1: Получить Anthropic API ключ

1. Зарегистрируйтесь на [console.anthropic.com](https://console.anthropic.com)
2. Перейдите в Settings → API Keys
3. Создайте новый ключ
4. Скопируйте ключ (начинается с `sk-ant-...`)

### Шаг 2: Добавить ключ в переменные окружения

**Windows (PowerShell):**

```powershell
# Временно (для текущей сессии)
$env:ANTHROPIC_API_KEY="sk-ant-your-key-here"

# Постоянно (для пользователя)
[System.Environment]::SetEnvironmentVariable('ANTHROPIC_API_KEY', 'sk-ant-your-key-here', 'User')
```

**Или через GUI:**

1. Win + R → `sysdm.cpl` → Advanced → Environment Variables
2. User variables → New
3. Variable: `ANTHROPIC_API_KEY`
4. Value: ваш ключ

### Шаг 3: Перезапустить Cursor

После добавления переменной окружения перезапустите Cursor полностью.

## 🚀 Использование

### В Cursor чате:

```
Используй prompt-engineer чтобы переписать этот промпт:
"Создай функцию для конвертации температуры"
Язык: typescript
```

Или напрямую:

```
@prompt-engineer rewrite_coding_prompt
Промпт: "Создай компонент формы с валидацией"
Язык: typescript
```

### Параметры инструмента:

- **prompt** (обязательно): Ваш исходный промпт
- **language** (обязательно): Язык программирования (typescript, javascript, python, и т.д.)

## 📊 Что делает сервер

1. Анализирует ваш промпт через Claude 3 Sonnet
2. Добавляет структуру и контекст
3. Учитывает специфику языка программирования
4. Оптимизирует для понимания AI IDE
5. Возвращает улучшенную версию промпта

## ⚙️ Конфигурация

Текущая конфигурация в `mcp.json`:

```json
"prompt-engineer": {
  "command": "npx",
  "args": ["-y", "tsx", "C:\\Temp\\prompt-engineer-mcp-server\\index.ts"],
  "env": {
    "ANTHROPIC_API_KEY": "${ANTHROPIC_API_KEY}"
  }
}
```

**Примечание:** Используется `tsx` для запуска TypeScript файла напрямую без компиляции.

## ✅ Проверка работы

1. Перезапустите Cursor
2. Settings → MCP → Installed MCP Servers
3. Проверьте что `prompt-engineer` подключен (зеленый статус)
4. Проверьте доступные инструменты в Settings → MCP → Available Tools
5. Должен быть инструмент `rewrite_coding_prompt`

## 💡 Примеры использования

### Пример 1: Простой промпт

```
Исходный: "Создай функцию сортировки"
Язык: typescript

Оптимизированный: [Claude перепишет с деталями, типами, edge cases]
```

### Пример 2: Компонент React

```
Исходный: "Сделай форму входа"
Язык: typescript

Оптимизированный: [Добавит требования к валидации, состояниям, accessibility]
```

## ⚠️ Важно

- **Требуется Anthropic API ключ** - без него сервер не работает
- **Использует Claude 3 Sonnet** - списываются токены с вашего Anthropic аккаунта
- **Температура 0.2** - для консистентных результатов
- **Лимит 40 инструментов** - убедитесь что не превышен лимит Cursor

## 🔧 Troubleshooting

### Ошибка: "Missing API key"

- Проверьте что переменная `ANTHROPIC_API_KEY` установлена
- Перезапустите Cursor после установки переменной
- Проверьте что ключ правильный (начинается с `sk-ant-`)

### Сервер не подключается

- Проверьте интернет соединение
- Убедитесь что Anthropic API доступен
- Проверьте логи в Output панели (MCP фильтр)

### Инструмент не виден

- Проверьте лимит инструментов (максимум 40)
- Отключите неиспользуемые MCP серверы
- Перезапустите Cursor

## 📝 Быстрая справка

**Команда для проверки:**

```powershell
# Проверить переменную окружения
echo $env:ANTHROPIC_API_KEY

# Установить временно
$env:ANTHROPIC_API_KEY="sk-ant-your-key"
```

**Использование в Cursor:**

- Просто упомяните "используй prompt-engineer" в запросе
- Или явно: "@prompt-engineer rewrite_coding_prompt"

**Ожидаемый результат:**
Оптимизированный промпт с четкой структурой, требованиями и спецификой языка.
