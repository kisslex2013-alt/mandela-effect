---
description: Базовые принципы стека проекта - загружается для всех задач
alwaysApply: false
globs: ["**/*"]
# Примечание: Используем globs вместо alwaysApply для лучшей производительности
# Правила применяются ко всем файлам через globs: ["**/*"]
---

# Core Rules: React 18.3 + Zustand 5.0 + Tailwind 3.4

## Tech Stack

- **Frontend**: React 18.3 с TypeScript (строгий режим через tsconfig.json)
- **State Management**: Zustand 5.0
- **Styling**: Tailwind CSS 3.4
- **Testing**: Vitest 4.0 + React Testing Library
- **Build Tool**: Vite 7.1
- **Charts**: Recharts 3.3
- **Animations**: Framer Motion 12.23

## Критические принципы

### React 18.3+: Rules of Hooks

**КРИТИЧНО:** Хуки должны вызываться только на верхнем уровне функций, **НИКОГДА** внутри условий, циклов или после early returns.

```javascript
// ❌ НЕПРАВИЛЬНО
function Component({ condition }) {
  if (condition) {
    return null
  }
  const [state, setState] = useState(0) // ОШИБКА!
}

// ✅ ПРАВИЛЬНО
function Component({ condition }) {
  const [state, setState] = useState(0) // Всегда на верхнем уровне
  
  if (condition) {
    return null
  }
}
```

**Правило:** "Hooks at the top level, before any conditional logic, never inside if statements or loops."

### Dependency Arrays обязательны

Массивы зависимостей в `useEffect`, `useCallback` и `useMemo` **обязательны в React 18**. Их отсутствие вызывает бесконечные циклы.

```javascript
// ❌ НЕПРАВИЛЬНО
useEffect(() => {
  fetchData()
}) // Нет массива зависимостей!

// ✅ ПРАВИЛЬНО
useEffect(() => {
  fetchData()
}, []) // Пустой массив для однократного выполнения
```

**Правило:** "Always specify dependency arrays. Use ESLint react-hooks/exhaustive-deps rule."

### Очистка в useEffect

Критична очистка в `useEffect` для событийных слушателей и подписок:

```javascript
useEffect(() => {
  const handleResize = () => {
    // обработка
  }
  
  window.addEventListener('resize', handleResize)
  
  // ✅ ОБЯЗАТЕЛЬНО: очистка
  return () => {
    window.removeEventListener('resize', handleResize)
  }
}, [])
```

### Key props в списках

**НИКОГДА** не используй индекс массива как key. Используй только уникальные стабильные ID.

```javascript
// ❌ НЕПРАВИЛЬНО
{items.map((item, index) => (
  <Item key={index} item={item} />
))}

// ✅ ПРАВИЛЬНО
{items.map((item) => (
  <Item key={item.id} item={item} />
))}
```

**Правило:** "NEVER use array index as key. Use unique stable IDs only."

### Zustand 5.0+: Атомарные селекторы

**КРИТИЧНО:** Никогда не экспортируй стор напрямую — только кастомные хуки с атомарными селекторами.

```javascript
// ❌ НЕПРАВИЛЬНО - вызывает ре-рендер при изменении любого поля
const { bears, fish } = useBearStore()

// ✅ ПРАВИЛЬНО - атомарные селекторы
export const useBears = () => useBearStore(state => state.bears)
export const useFish = () => useBearStore(state => state.fish)

// Использование
const bears = useBears()
const fish = useFish()
```

**Правило:** "Export ONLY custom hooks (atomic selectors), NOT the store."

### Разделение actions от state

Организация стора должна отделять actions от state через namespace:

```javascript
// ✅ ПРАВИЛЬНО
const useStore = create((set) => ({
  count: 0,
  theme: 'dark',
  actions: {
    increment: () => set((state) => ({ count: state.count + 1 })),
    setTheme: (theme) => set({ theme })
  }
}))
```

### Tailwind CSS 3.4+: Mobile-first

**КРИТИЧНО:** Слабые модели не помнят mobile-first подход. Всегда начинай с базовых классов для mobile, добавляй breakpoints прогрессивно.

```javascript
// ❌ НЕПРАВИЛЬНО (desktop-first)
<div className="w-1/2 md:w-full">

// ✅ ПРАВИЛЬНО (mobile-first)
<div className="w-full md:w-1/2 lg:w-1/3">
```

**Правило:** "Start with base classes for mobile, add breakpoints progressively: sm:, md:, lg:, xl:"

### Группировка классов Tailwind

Группируй утилиты по категориям для читаемости:

```javascript
// ✅ ПРАВИЛЬНО
<div className="
  flex items-center justify-between
  w-full h-12
  px-4 py-2
  text-lg font-semibold
  bg-blue-600 text-white
  rounded-lg shadow-md
  transition-colors duration-200
">
```

**Порядок:** layout → sizing → typography → colors & effects → transitions

### Запрет cursor-pointer на кнопках

**НИКОГДА** не используй `cursor-pointer` на интерактивных элементах (button, a, input). Браузер предоставляет курсор по умолчанию.

```javascript
// ❌ НЕПРАВИЛЬНО
<button className="cursor-pointer">Click</button>

// ✅ ПРАВИЛЬНО
<button>Click</button>
```

**Правило:** "NEVER use cursor-pointer on interactive elements (button, a, input). Browser provides default cursor."

## Naming Conventions

- Компоненты: PascalCase (`UserCard.tsx`)
- Файлы компонентов: PascalCase (`UserCard.tsx`)
- Hooks: camelCase с префиксом `use` (`useAuth.js`)
- Stores: camelCase с суффиксом `Store` (`useAuthStore.js`)
- Utils: camelCase (`formatDate.js`)
- Constants: UPPER_SNAKE_CASE (`API_BASE_URL`)

## Code Style

- Используй функциональные компоненты (не классовые)
- Используй `function` вместо `const` для компонентов (лучше для стектрейсов)
- Комментарии на русском для сложной логики
- Максимум 100 строк на файл (разбивай на подкомпоненты)

## 📋 Планирование и самообучение

**КРИТИЧЕСКИ ВАЖНО:** Всегда создавай план перед началом работы над задачей.

**ПРИНУДИТЕЛЬНАЯ АКТИВАЦИЯ:**
Если пользователь использует триггерные фразы в запросе, ОБЯЗАТЕЛЬНО применяй полный workflow независимо от сложности задачи:

**Триггерные фразы:**
- "по правилам"
- "структурированно"
- "с планированием"
- "workflow"

**Что активируется при триггере:**
1. ✅ Создание детального плана (обязательно)
2. ✅ Создание задач в Beads (обязательно, даже для багов)
3. ✅ Запуск проверки качества (`npm run quality:check`)
4. ✅ Следование всем шагам из `.cursor/rules/workflows/planning.mdc`
5. ✅ Запись уроков в `lessons-learned.mdc` после завершения

**ПРАВИЛО:** "Always create a detailed plan before starting work. Update the plan if needed. Record lessons learned in lessons-learned.mdc."

**Workflow планирования:** `.cursor/rules/workflows/planning.mdc`
**Уроки и исправления:** `.cursor/rules/lessons-learned.mdc`
**Основные правила планирования:** `.cursorrules` (в корне проекта)

## 🚀 Деплой проекта

**КРИТИЧЕСКИ ВАЖНО:** Главный скрипт деплоя - `scripts/update-build-version.js`

**ПРАВИЛО:** "NEVER modify update-build-version.js without explicit user permission. Always ask first."

**Команда деплоя:** `npm run deploy`

**Процесс:** `npm run deploy` → git commit/push → Vercel автоматически деплоит с GitHub

Подробные правила деплоя: `.cursor/rules/workflows/deployment.mdc`
