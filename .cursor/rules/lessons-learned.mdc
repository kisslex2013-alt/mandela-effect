---
description: Накопленные уроки и исправления - используется для самообучения AI
alwaysApply: false
---

# 📚 Уроки и исправления

> Этот файл содержит накопленные знания о проекте, исправленные ошибки и лучшие практики.
> AI автоматически обновляет этот файл при получении feedback или исправлении ошибок.

## Как использовать

- **Перед началом работы:** Проверь этот файл на релевантные уроки
- **После исправления ошибки:** Добавь запись в этот файл
- **При получении feedback:** Запиши урок для будущего использования

---

## 2025-01-18 - Кнопка не вызывает обработчик из-за неправильного условия

**Проблема:** Кнопка "Старт" в FloatingPomodoroPanel не запускала стандартный таймер. В логах отсутствовали DEBUG сообщения, что означало - функция `start` вообще не вызывалась.

**Причина:** В коде была неправильная логика:
```typescript
// ❌ НЕПРАВИЛЬНО
<button onClick={timeLeft > 0 ? resume : start}>
```

При первом запуске Pomodoro `timeLeft` равен полному времени (25 минут = 1500 секунд), а не 0. Поэтому вызывался `resume` вместо `start`. Функция `resume` только возобновляет Pomodoro счетчик, но НЕ запускает стандартный таймер.

**Решение:** Всегда вызывать `start` при нажатии кнопки "Старт":
```typescript
// ✅ ПРАВИЛЬНО
<button onClick={start}>
```

Функция `start` сама определяет, нужно ли запускать стандартный таймер или только Pomodoro счетчик.

**Правило:** 
- НИКОГДА не используй тернарный оператор для выбора между `start`/`resume` на основе `timeLeft`
- Логика запуска/возобновления должна быть внутри функции `start`, а не в JSX
- Если кнопка не вызывает обработчик - первым делом проверь DEBUG логи. Если логов нет - проблема в привязке обработчика, а не в его логике

**Контекст:** Применяется в `src/components/pomodoro/FloatingPomodoroPanel.tsx` при работе с Pomodoro таймером как "wrapper" над стандартным таймером.

**Файлы:** 
- `src/components/pomodoro/FloatingPomodoroPanel.tsx` (исправлена кнопка)
- `src/hooks/usePomodoro.js` (добавлено DEBUG логирование)

**Дополнительно:** Для отладки добавлено подробное логирование в `usePomodoro.js`, которое помогает отследить все этапы работы Pomodoro таймера.

---

## 2025-01-17 - Рендеринг компонентов иконок в BaseModal

**Проблема:** Ошибка "Objects are not valid as a React child" при рендеринге компонентов иконок (History, Settings) в BaseModal.

**Решение:** Использовать `createElement` для рендеринга компонентов иконок, которые не являются валидными React элементами.

**Правило:** 
```typescript
// ✅ ПРАВИЛЬНО
{isValidElement(titleIcon) ? (
  titleIcon
) : typeof titleIcon === 'function' ? (
  createElement(titleIcon, { className: 'w-6 h-6' })
) : null}
```

**Контекст:** Применяется в `src/components/ui/BaseModal.tsx` при рендеринге `titleIcon` prop.

**Файлы:** `src/components/ui/BaseModal.tsx`

---

## 2025-01-17 - Иконки из Lucide React

**Проблема:** Использование несуществующего `SearchIcon` вместо `Search` в IconSelect.

**Решение:** Всегда использовать правильные названия иконок из lucide-react. Проверять импорты перед использованием.

**Правило:** 
- Иконки из lucide-react имеют названия в PascalCase
- Всегда проверяй импорты в `src/utils/icons.js`
- Используй официальный сайт https://lucide.dev/icons/ для поиска иконок

**Контекст:** Применяется при работе с иконками в любых компонентах.

**Файлы:** `src/components/ui/IconSelect.tsx`, `src/utils/icons.js`

---

## 2025-01-18 - Вертикальный скролл в Flexbox модальных окнах

**Проблема:** При большом объеме контента в модальном окне (например, SoundNotificationsSettingsModal, AI-подсказки таб) отсутствует скролл, и информация не помещается на экране.

**Причина:** 
1. Родительский контейнер с `overflow-hidden` блокирует скролл дочерних элементов
2. Дочерний flex-элемент с `flex-1` не сжимается без `min-h-0`
3. Лишние `mb-6` увеличивают расчетную высоту контента

**Решение:**
1. Заменить `overflow-hidden` на `min-h-0` в родительском flex-контейнере
2. Убрать `flex-1` из внутренних div внутри контейнера со скроллом
3. Убрать лишние margin-bottom классы (`mb-6`)

**Правило:** 
```tsx
// ❌ НЕПРАВИЛЬНО - блокирует скролл
<div className="flex flex-1 overflow-hidden mb-6">
  <div className="flex-1 overflow-y-auto">
    <div className="flex-1">контент</div>
  </div>
</div>

// ✅ ПРАВИЛЬНО - скролл работает
<div className="flex flex-1 min-h-0">
  <div className="flex-1 min-h-0 overflow-y-auto custom-scrollbar">
    <div>контент</div>
  </div>
</div>
```

**Best Practice (из веб-источников):**
- В flexbox контейнерах для вертикального скролла ОБЯЗАТЕЛЬНО использовать `min-h-0` на flex-элементах
- Контейнер со скроллом должен иметь: `flex-1 min-h-0 overflow-y-auto`
- Родительский контейнер НЕ должен иметь `overflow-hidden`
- Убрать ненужные `flex-1` у внутренних элементов контента

**Контекст:** Применяется в модальных окнах с большим объемом контента и flexbox layout.

**Файлы:** 
- `src/components/modals/SoundNotificationsSettingsModal.tsx`
- `src/components/ui/AnimatedModalContent.tsx`

---

## 2025-01-18 - Бесконечный цикл рендеринга из-за неправильного useCallback/useMemo

**Проблема:** Ошибка "Maximum update depth exceeded" в React при использовании Pomodoro компонента. Компонент вызывал бесконечный цикл ре-рендеров.

**Причина:**
1. В `usePomodoro` хуке использовался `useCallback` для функции `progress`, которая затем вызывалась при каждом рендере: `progress: progress()`
2. Каждый вызов функции создавал новый результат, что триггерило новый рендер
3. Зависимости в `useCallback` были неполными (`mode` использовался, но не указан в deps)

**Решение:**
1. Заменить `useCallback(() => {...}, [deps])` на `useMemo(() => {...}, [deps])`
2. Возвращать вычисленное значение напрямую, а не функцию
3. Добавить все используемые переменные в массив зависимостей

**Правило:**
```javascript
// ❌ НЕПРАВИЛЬНО - создает бесконечный цикл
const progress = useCallback(() => {
  const value = calculateProgress()
  return value
}, [dep1]) // mode отсутствует!

return {
  progress: progress() // ⚠️ Вызов функции при каждом рендере
}

// ✅ ПРАВИЛЬНО - вычисляется один раз при изменении deps
const progress = useMemo(() => {
  const value = calculateProgress()
  return value
}, [dep1, mode]) // все зависимости указаны

return {
  progress // Используем значение напрямую
}
```

**Best Practice:**
- `useCallback` - для мемоизации **функций** (например, event handlers)
- `useMemo` - для мемоизации **значений** (например, вычисления)
- Всегда указывай ВСЕ используемые внешние переменные в deps
- Никогда не вызывай `useCallback`-функцию при каждом рендере

**Контекст:** Применяется при работе с React хуками для оптимизации вычислений.

**Файлы:** 
- `src/hooks/usePomodoro.js`

---

## 2025-01-18 - Бесконечный цикл из-за селектора, возвращающего новый объект

**Проблема:** Ошибка "Maximum update depth exceeded" и "The result of getSnapshot should be cached" в React при использовании селектора, возвращающего объект.

**Причина:**
```typescript
// ❌ Создает НОВЫЙ объект при каждом рендере
export const usePomodoroDurations = () =>
  usePomodoroStore(state => ({
    work: state.workDuration,
    shortBreak: state.shortBreakDuration,
    // ...
  }))
```

Даже если значения полей не изменились, объект `{work: 25, shortBreak: 5}` технически является **новым объектом** с новой референцией в памяти. React видит это как изменение и вызывает ре-рендер → бесконечный цикл.

**Попытка решения №1 (НЕ сработала):** Использовать `shallow` comparison:
```typescript
import { shallow } from 'zustand/shallow'

// ⚠️ НЕ ПОМОГЛО - все равно вызывает "getSnapshot should be cached"
export const usePomodoroDurations = () =>
  usePomodoroStore(
    state => ({
      work: state.workDuration,
      shortBreak: state.shortBreakDuration,
      longBreak: state.longBreakDuration,
      pomodorosUntilLongBreak: state.pomodorosUntilLongBreak,
    }),
    shallow
  )
```

**Решение (финальное):** Создать **отдельные атомарные селекторы** для каждого поля:

```typescript
// ✅ ПРАВИЛЬНО - каждый селектор возвращает примитив
export const usePomodoroWorkDuration = () => usePomodoroStore(state => state.workDuration)
export const usePomodoroShortBreakDuration = () => usePomodoroStore(state => state.shortBreakDuration)
export const usePomodoroLongBreakDuration = () => usePomodoroStore(state => state.longBreakDuration)
export const usePomodoroPomodorosUntilLongBreak = () => usePomodoroStore(state => state.pomodorosUntilLongBreak)

// В компоненте используй только нужный селектор:
const pomodorosUntilLongBreak = usePomodoroPomodorosUntilLongBreak()
```

**Правило:**
- **НИКОГДА** не возвращай объекты из селекторов, если можешь избежать
- Всегда создавай **атомарные селекторы** для каждого поля
- Если действительно нужен объект - используй `shallow`, но помни, что это может не всегда работать
- Подписывайся только на те данные, которые действительно нужны компоненту

**Best Practice:**
```typescript
// ❌ ПЛОХО - возвращает объект
export const useUser = () => useStore(state => ({ 
  name: state.name, 
  age: state.age 
}))

// ✅ ХОРОШО - атомарные селекторы
export const useUserName = () => useStore(state => state.name)
export const useUserAge = () => useStore(state => state.age)

// В компоненте:
const name = useUserName()  // Подписка только на name
const age = useUserAge()    // Подписка только на age
```

**Контекст:** Критично для всех Zustand stores. Атомарные селекторы - основа производительности.

**Файлы:** 
- `src/store/usePomodoroStore.ts`
- `src/components/pomodoro/PomodoroPanel.tsx`

---

## 2025-01-18 - Интеграция Pomodoro таймера с основным таймером

**Проблема:** Два таймера (основной и Pomodoro) работали полностью независимо друг от друга, что создавало конфликты:
- Одновременная работа без синхронизации
- Дублирование звуковых уведомлений
- Конфликт обновления заголовка вкладки (document.title)
- Pomodoro не создавал записи в журнале
- Непонятно пользователю, какой таймер использовать

**Решение:** Реализована интеграция Pomodoro как надстройки над основным таймером:

1. **При запуске Pomodoro** → автоматически запускается основной таймер с категорией "Pomodoro Work"
2. **При завершении Pomodoro** → основной таймер останавливается и создается запись в журнале
3. **Пауза/Возобновление синхронизировано** → оба таймера работают одновременно
4. **Проверка конфликтов** → если основной таймер уже запущен, показывается модальное окно подтверждения
5. **Приоритет Pomodoro** → при запущенном Pomodoro его данные показываются в заголовке вкладки

**Правило:**

```typescript
// ✅ ИНТЕГРАЦИЯ: Pomodoro запускает основной таймер

const start = useCallback(() => {
  // Проверка конфликта
  if (timer.isRunning()) {
    showConfirmModal(
      'Таймер уже запущен',
      'Хотите остановить текущий таймер и запустить Pomodoro?',
      () => {
        timer.stop()
        pomodoroStart()
        timer.start('Pomodoro Work') // Интеграция
      }
    )
    return
  }

  pomodoroStart()
  timer.start('Pomodoro Work') // Автоматический запуск
}, [timer, pomodoroStart, showConfirmModal])

// ✅ ПРИОРИТЕТ: Pomodoro управляет title когда активен

useEffect(() => {
  if (pomodoroIsRunning) {
    return // Пропускаем обновление, Pomodoro сам управляет
  }
  
  // Обновляем title только если Pomodoro не активен
  if (activeTimer && !isPaused) {
    document.title = `${formatElapsedTime(currentElapsed)} - Работаем`
  }
}, [activeTimer, isPaused, pomodoroIsRunning])
```

**Best Practice:**
- Основной таймер создает записи в журнале (с категорией "Pomodoro Work" / "Pomodoro Break")
- Pomodoro управляет циклами работы/перерыва
- Одна система звуковых уведомлений (приоритет у Pomodoro)
- UI индикатор показывает когда оба таймера активны

**UI индикатор:**
```tsx
{timer.isRunning && pomodoroIsRunning && (
  <div className="px-2 py-1 rounded-lg bg-yellow-100 dark:bg-yellow-900/30 animate-pulse">
    <span className="text-[10px] font-semibold">🍅+⏱️ Интеграция</span>
  </div>
)}
```

**Преимущества:**
- Все записи сохраняются в журнале
- Нет конфликтов звуков/уведомлений
- Единая система отслеживания времени
- Понятная логика для пользователя
- Сохраняется вся функциональность обоих таймеров

**Контекст:** Применяется при интеграции нескольких систем отслеживания времени.

**Файлы:** 
- `src/hooks/usePomodoro.js` - интеграция с основным таймером
- `src/hooks/useTimer.js` - приоритет Pomodoro для title
- `src/components/entries/EntriesListHeader.tsx` - UI индикатор

---

## 2025-01-18 - Ошибка "is not a function" при использовании свойств хука

**Проблема:** Ошибка `TypeError: timer.isRunning is not a function` при вызове `timer.isRunning()` в коде.

**Причина:** `useTimer` хук возвращает `isRunning` как **свойство** (boolean), а не как **функцию**:

```typescript
// useTimer.js - return
return {
  isRunning: isTimerRunning(), // ❌ Вызывается здесь!
  isPaused: isPausedTimer(),    // ❌ Вызывается здесь!
  // ...
}
```

В коде использовалось `timer.isRunning()` с вызовом, но это уже **значение**, а не функция.

**Решение:** Убрать скобки `()` при использовании свойств хука:

```typescript
// ❌ НЕПРАВИЛЬНО
if (timer.isRunning()) {  // TypeError!
  timer.pause()
}

// ✅ ПРАВИЛЬНО
if (timer.isRunning) {  // Свойство, не функция
  timer.pause()
}
```

**Правило:**
- Свойства хука (значения) - используются **без скобок**: `timer.isRunning`, `timer.isPaused`
- Методы хука (функции) - вызываются **со скобками**: `timer.start()`, `timer.stop()`

**Best Practice:**
При создании хука всегда четко разделяй:
- **State/Properties** - возвращай как значения (без вызова)
- **Actions/Methods** - возвращай как функции (для вызова)

```typescript
// ✅ ПРАВИЛЬНО: четкое разделение
return {
  // State - значения (без вызова)
  isRunning,
  isPaused,
  elapsedTime,
  
  // Actions - функции (для вызова)
  start,
  stop,
  pause,
  resume,
}
```

**Контекст:** Применяется при работе с любыми custom hooks в React.

**Файлы:** 
- `src/hooks/usePomodoro.js` - использование `timer.isRunning` / `timer.isPaused`

---

## 2025-01-18 - Race condition при завершении таймера и создании записи

**Проблема:** При завершении Pomodoro запись в журнале не создавалась, хотя основной таймер останавливался.

**Причина:** Race condition - когда Pomodoro завершается (`timeLeft === 0`), метод `tick()` в `usePomodoroStore` устанавливает `isRunning: false` **до** того, как `useEffect` в `usePomodoro.js` успевает обработать завершение. К моменту проверки `if (timer.isRunning)` основной таймер уже мог перестать быть "running".

```typescript
// ❌ ПРОБЛЕМА: timer.isRunning может быть уже false
useEffect(() => {
  if (timeLeft === 0 && !isRunning) {
    if (timer.isRunning) {  // Уже false!
      timer.stop()  // Не вызывается
    }
  }
}, [timeLeft, isRunning, timer.isRunning])
```

**Решение:** Использовать `useRef` для отслеживания, был ли таймер запущен, независимо от текущего состояния:

```typescript
// ✅ ПРАВИЛЬНО: Используем ref для надежного отслеживания
const timerWasRunningRef = useRef(false)

// Отслеживаем запуск таймера
useEffect(() => {
  if (isRunning && timer.isRunning) {
    timerWasRunningRef.current = true
  }
}, [isRunning, timer.isRunning])

// Обработка завершения
useEffect(() => {
  if (timeLeft === 0 && !isRunning) {
    // Проверяем ref, а не текущее состояние
    if (timerWasRunningRef.current) {
      const entryData = timer.stop()
      timerWasRunningRef.current = false // Сбрасываем флаг
      
      if (entryData) {
        showSuccess(`Запись создана (${entryData.duration}ч)`)
      }
    }
  }
}, [timeLeft, isRunning, timer])
```

**Правило:**
- Если нужно отследить **прошлое состояние** между ре-рендерами → используй `useRef`
- Если нужно реагировать на **текущее состояние** → используй `useState`/props
- `useRef` сохраняет значение между рендерами и не триггерит ре-рендер при изменении

**Best Practice:**
```typescript
// Используй ref для:
// 1. Отслеживания предыдущих значений
const previousValueRef = useRef(value)

// 2. Флагов состояния, которые не должны триггерить ре-рендер
const isMountedRef = useRef(true)

// 3. Хранения таймеров/интервалов
const intervalRef = useRef(null)

// 4. Отслеживания race conditions
const wasRunningRef = useRef(false)
```

**Контекст:** Применяется при работе с асинхронными операциями и race conditions в React.

**Файлы:** 
- `src/hooks/usePomodoro.js` - использование `timerWasRunningRef`

---

## 2025-01-18 - Отсутствие модального окна редактирования после Pomodoro

**Проблема:** При завершении Pomodoro работы запись создавалась, но не открывалось модальное окно редактирования записи (как при обычном таймере).

**Причина:** Обычный таймер при остановке вызывает:
```typescript
const entryData = stop()
if (entryData) {
  openModal('editEntry', { entry: entryData })
  showSuccess('Таймер остановлен. Проверьте и сохраните запись.')
}
```

Но Pomodoro останавливал таймер внутри `useEffect` и **не открывал модальное окно**, только показывал уведомление.

**Решение:** Добавить `openModal` в логику завершения Pomodoro:

```typescript
// ✅ ПРАВИЛЬНО: Открываем модальное окно как обычный таймер
if (timerWasRunningRef.current) {
  const entryData = timer.stop()
  timerWasRunningRef.current = false
  
  if (entryData && previousMode === 'work') {
    openModal('editEntry', { entry: entryData }) // Открываем модальное окно
    showSuccess(`🍅 Pomodoro завершен! Запись создана`)
  }
}
```

**Правило:**
- Если автоматически создается/обновляется запись → открывай модальное окно для редактирования
- Это позволяет пользователю проверить и скорректировать автоматически созданную запись
- Обеспечивает консистентность UX между ручным и автоматическим управлением

**Best Practice:**
```typescript
// Всегда после stop() открывай модальное окно редактирования
const entryData = timer.stop()

if (entryData) {
  // 1. Открой модальное окно
  openModal('editEntry', { entry: entryData })
  
  // 2. Покажи уведомление
  showSuccess('Запись создана. Проверьте данные.')
}
```

**Контекст:** Применяется при автоматическом создании/обновлении записей таймера.

**Файлы:** 
- `src/hooks/usePomodoro.js` - добавлен `openModal` при завершении

---

## 2025-01-18 - Переход на архитектуру "Wrapper над стандартным таймером"

**Проблема:** Сложная интеграция Pomodoro с стандартным таймером приводила к проблемам:
- Запись создавалась, но модальное окно не открывалось
- Дублирование логики создания/обновления записей
- Сложность отладки и поддержки
- Race conditions и проблемы синхронизации

**Решение:** Полностью изменить архитектуру - Pomodoro теперь **обертка** над стандартным таймером:

```typescript
// ✅ НОВЫЙ ПОДХОД: Pomodoro = wrapper над стандартным таймером

// Запуск
const start = () => {
  timer.start('Разработка')  // Запускаем стандартный таймер
  pomodoroStart()             // Запускаем Pomodoro счетчик
}

// Завершение (автоматически через 25 мин)
if (timeLeft === 0 && previousMode === 'work') {
  const entryData = timer.stop()  // Стандартный таймер сам создает запись
  
  if (entryData) {
    openModal('editEntry', { entry: entryData })  // Стандартное модальное окно
    showSuccess('🍅 Pomodoro завершен!')
  }
}
```

**Преимущества:**
1. **Нет дублирования** - вся логика записей в стандартном таймере
2. **Автоматическое модальное окно** - стандартный `timer.stop()` уже возвращает `entryData`
3. **Простота** - Pomodoro только управляет временем, не создает записи
4. **Надежность** - используем проверенный код стандартного таймера
5. **Консистентность** - идентичное поведение для пользователя

**Архитектура:**
```
Pomodoro Timer (usePomodoro)
    ↓ управляет
Standard Timer (useTimer)
    ↓ создает/обновляет
Entries (useEntriesStore)
```

**Правило:**
- Когда у тебя есть две похожие системы → сделай одну оберткой над другой
- Не дублируй логику - используй композицию
- Wrapper отвечает только за свою специфику (в данном случае - отсчет 25 минут)

**Best Practice:**
```typescript
// Wrapper pattern
class PomodoroTimer {
  constructor(standardTimer) {
    this.timer = standardTimer  // Композиция, не наследование
    this.duration = 25 * 60
  }
  
  start() {
    this.timer.start('Work')  // Используем существующую логику
    this.startCountdown()      // Добавляем свою специфику
  }
  
  onComplete() {
    return this.timer.stop()  // Возвращаем результат стандартного таймера
  }
}
```

**Контекст:** Применяется при интеграции двух систем с похожей функциональностью.

**Файлы:** 
- `src/hooks/usePomodoro.js` - полностью переработан как wrapper

---

## 2025-01-20 - Вынос NotificationsDisplaySettingsPanel в отдельное модальное окно

**Задача:** Вынести `NotificationsDisplaySettingsPanel` в отдельное модальное окно для разработчиков/тестирования, вызываемое по CTRL+ALT+N.

**Решение:**

1. **Создание модального окна:**
   - Создан `NotificationsDisplayModal` в `src/components/modals/NotificationsDisplayModal.tsx`
   - Использует `BaseModal` с размером `large`
   - Интегрирует `NotificationsDisplaySettingsPanel`

2. **Регистрация в useUIStore:**
   - Добавлен `notificationsDisplay: { isOpen: false }` в `modals`
   - Добавлен в `initialStates` для корректного закрытия

3. **Добавление хоткея:**
   - Добавлен хоткей `'ctrl+alt+n'` в `useHotkeys` в `App.tsx`
   - Хоткей открывает модальное окно `notificationsDisplay`
   - Установлен `ignoreInputs: false` для работы хоткея даже в input полях

4. **Интеграция в App.tsx:**
   - Добавлен lazy import для `NotificationsDisplayModal`
   - Добавлен `shouldRenderNotificationsDisplay` с `useDelayedUnmount`
   - Добавлен рендеринг модального окна в Suspense блоке

5. **Удаление из SoundNotificationsSettingsModal:**
   - Убран импорт `NotificationsDisplaySettingsPanel`
   - Убран раздел "Настройки отображения уведомлений" из таба "Уведомления"

**Правило:** 
- Инструменты для разработки и тестирования должны быть вынесены в отдельные модальные окна с хоткеями
- Не показывать инструменты разработки в основных настройках для конечных пользователей
- Использовать комбинации клавиш типа CTRL+ALT+[буква] для инструментов разработки

**Контекст:** Применяется для инструментов разработки и тестирования, которые не должны быть доступны обычным пользователям.

**Файлы:** 
- `src/components/modals/NotificationsDisplayModal.tsx` (новое модальное окно)
- `src/store/useUIStore.ts` (регистрация модального окна)
- `src/App.tsx` (хоткей и рендеринг)
- `src/components/modals/SoundNotificationsSettingsModal.tsx` (удаление компонента)

---

## 2025-01-20 - Создание инструмента настройки условий отображения уведомлений

**Задача:** Создать визуальный инструмент для настройки условий отображения всех уведомлений в приложении по категориям и типам.

**Решение:**

1. **Структура данных:**
   - Расширен `NotificationSettings` в `src/types/index.ts` для добавления `display` настроек
   - Созданы типы `NotificationDisplaySettings`, `NotificationCategorySettings`, `NotificationTypeSettings`
   - Добавлены дефолтные настройки в `src/store/useSettingsStore.ts`

2. **UI компонент:**
   - Создан `NotificationsDisplaySettingsPanel` в `src/components/settings/NotificationsDisplaySettingsPanel.tsx`
   - Компонент позволяет настраивать отображение уведомлений по 14 категориям и 4 типам
   - Интегрирован в `SoundNotificationsSettingsModal` в таб "Уведомления"

3. **Логика проверки:**
   - Создан хелпер `src/utils/notificationHelpers.ts` с функциями:
     - `getNotificationCategory()` - определяет категорию по сообщению
     - `shouldShowNotification()` - проверяет, нужно ли показывать уведомление
   - Обновлены функции `showSuccess`, `showError`, `showWarning`, `showInfo` в `src/store/useUIStore.ts` для проверки настроек

4. **Категории уведомлений:**
   - timer, entries, categories, exportImport, backups, settings, filters, actions, cleanup, colors, validation, overtime, breaks, autoSync

5. **Типы уведомлений:**
   - success, error, warning, info

**Правило:** 
- При создании новых уведомлений нужно убедиться, что они правильно определяются по категориям
- Критичные уведомления (ошибки валидации) всегда показываются, даже если категория отключена
- Настройки сохраняются в `notifications.display` в `useSettingsStore`

**Контекст:** Применяется во всех местах, где используются уведомления через `showSuccess`, `showError`, `showWarning`, `showInfo`.

**Файлы:** 
- `src/types/index.ts` (расширение типов)
- `src/store/useSettingsStore.ts` (дефолтные настройки)
- `src/components/settings/NotificationsDisplaySettingsPanel.tsx` (UI компонент)
- `src/utils/notificationHelpers.ts` (логика проверки)
- `src/store/useUIStore.ts` (обновление функций уведомлений)
- `src/components/modals/SoundNotificationsSettingsModal.tsx` (интеграция)

---

## 2025-01-20 - Исправление автоматического закрытия EditEntryModal при закрытии вложенного модального окна

**Проблема:** После открытия SoundNotificationsSettingsModal из EditEntryModal, изменения категории и закрытия SoundNotificationsSettingsModal, EditEntryModal автоматически закрывалось, хотя пользователь еще не сохранил данные.

**Причина:** 
- В предыдущем исправлении мы закрывали EditEntryModal перед открытием SoundNotificationsSettingsModal через `onClose()`
- После закрытия SoundNotificationsSettingsModal EditEntryModal не открывалось обратно
- Это приводило к потере несохраненных данных

**Решение:**
- НЕ закрывать EditEntryModal перед открытием SoundNotificationsSettingsModal
- Использовать nested модальные окна - EditEntryModal остается открытым, SoundNotificationsSettingsModal открывается поверх него
- SoundNotificationsSettingsModal уже имеет `nested={true}`, что обеспечивает правильный z-index

```typescript
// ✅ ПРАВИЛЬНО - не закрываем EditEntryModal
const handleOpenCategoriesModal = useCallback(() => {
  // Открываем SoundNotificationsSettingsModal поверх EditEntryModal (nested)
  // EditEntryModal остается открытым, чтобы пользователь мог продолжить редактирование после закрытия настроек
  openModal('soundSettings', { activeTab: 'categories' })
}, [openModal])
```

**Правило:** 
- При открытии модального окна из другого модального окна НЕ закрывай первое модальное окно
- Используй nested модальные окна - первое остается открытым, второе открывается поверх него с `nested={true}`
- Это позволяет пользователю вернуться к первому модальному окну после закрытия второго

**Проверка других модальных окон:**
- ConfirmModal - уже использует `nested={true}` ✅
- SoundNotificationsSettingsModal - уже использует `nested={true}` ✅
- Другие модальные окна (ImportModal, TutorialModal, AboutModal) не открывают модальные окна из других модальных окон ✅

**Контекст:** Применяется в `src/components/modals/EditEntryModal.tsx`, `src/components/modals/SoundNotificationsSettingsModal.tsx`.

**Файлы:** 
- `src/components/modals/EditEntryModal.tsx` (убран вызов onClose перед открытием SoundNotificationsSettingsModal)

---

## 2025-01-20 - Исправление перекрытия модальных окон и жесткое указание иконки Save

**Проблема 1:** После открытия SoundNotificationsSettingsModal из EditEntryModal кнопки не работали, как будто что-то перекрывает  
**Проблема 2:** Иконка на кнопке Сохранить все еще показывала папку вместо Save

**Причина 1:** 
- EditEntryModal имел z-index `z-[999999]`, а SoundNotificationsSettingsModal открывался поверх него
- Overlay EditEntryModal блокировал клики в новом модальном окне

**Причина 2:**
- Button компонент использовал замену иконки из localStorage через `iconId="edit-entry-save"`
- Даже при передаче `icon={Save}`, если есть `iconId` и замена в localStorage, используется замена

**Решение:**

1. **Закрытие EditEntryModal перед открытием SoundNotificationsSettingsModal:**
   ```typescript
   // ✅ ПРАВИЛЬНО
   const handleOpenCategoriesModal = useCallback(() => {
     onClose() // Закрываем текущее модальное окно
     setTimeout(() => {
       openModal('soundSettings', { activeTab: 'categories' })
     }, 100)
   }, [openModal, onClose])
   ```

2. **Добавление nested prop для SoundNotificationsSettingsModal:**
   ```typescript
   // ✅ ПРАВИЛЬНО
   <BaseModal
     nested={true} // Для вложенных модальных окон используем z-[9999999]
     ...
   />
   ```

3. **Убрать iconId для жесткого указания иконки:**
   ```typescript
   // ✅ ПРАВИЛЬНО - без iconId, иконка жестко указана через icon prop
   <Button
     icon={Save}
     // iconId убран - иконка не будет заменяться из localStorage
     ...
   >
     Сохранить
   </Button>
   ```

**Правило:** 
- При открытии нового модального окна из другого модального окна нужно либо закрыть первое, либо использовать `nested={true}`
- Для жесткого указания иконки без замены из localStorage не передавай `iconId`
- Если нужна замена иконки через localStorage, используй `iconId`, но тогда иконка может быть заменена

**Контекст:** Применяется в `src/components/modals/EditEntryModal.tsx`, `src/components/modals/SoundNotificationsSettingsModal.tsx`, `src/components/entries/EntryFormActions.tsx`.

**Файлы:** 
- `src/components/modals/EditEntryModal.tsx` (закрытие перед открытием нового модального окна)
- `src/components/modals/SoundNotificationsSettingsModal.tsx` (добавлен nested prop)
- `src/components/entries/EntryFormActions.tsx` (убран iconId для жесткого указания иконки)

---

## 2025-01-20 - Три улучшения UX в модальном окне редактирования записи

**Проблема 1:** Placeholder в поле заработка был слишком длинным ("Введите сумму заработка")  
**Проблема 2:** Клик на кнопку "Добавить новую категорию" открывал CategoriesModal вместо SoundNotificationsSettingsModal с табом "Категории"  
**Проблема 3:** Кнопка "Сохранить" не имела иконки

**Решение:**

1. **Изменен placeholder:**
   ```typescript
   // ✅ ПРАВИЛЬНО
   placeholder="Введите сумму"
   ```

2. **Исправлено открытие модального окна категорий:**
   ```typescript
   // ✅ ПРАВИЛЬНО
   const handleOpenCategoriesModal = useCallback(() => {
     openModal('soundSettings', { activeTab: 'categories' })
   }, [openModal])
   ```
   Теперь используется `SoundNotificationsSettingsModal` с активным табом "Категории" вместо отдельного `CategoriesModal`.

3. **Добавлена иконка на кнопку Сохранить:**
   ```typescript
   // ✅ ПРАВИЛЬНО
   import { Save } from '../../utils/icons'
   
   <Button
     icon={Save}
     iconId="edit-entry-save"
     ...
   >
     Сохранить
   </Button>
   ```

**Правило:** 
- Placeholder должен быть кратким и понятным
- Для открытия модальных окон настроек с конкретным табом используй `openModal('soundSettings', { activeTab: 'categories' })`
- Кнопки действий должны иметь соответствующие иконки для лучшего UX

**Контекст:** Применяется в `src/components/modals/EditEntryModal.tsx`, `src/components/entries/EntryFormFields.tsx`, `src/components/entries/EntryFormActions.tsx`.

**Файлы:** 
- `src/components/entries/EntryFormFields.tsx` (изменен placeholder)
- `src/components/modals/EditEntryModal.tsx` (исправлено открытие модального окна)
- `src/components/entries/EntryFormActions.tsx` (добавлена иконка Save)

---

## 2025-01-20 - Поле заработка должно быть пустым для записей из таймера

**Проблема:** При остановке таймера поле заработка автоматически заполнялось рассчитанным значением (например, 75 ₽), хотя пользователь не вводил его. Пользователь хочет, чтобы поле было пустым по умолчанию, так как ставка рассчитывается исходя из времени работы и заработанной суммы, а не наоборот.

**Причина:** 
1. В `useTimer.js` при остановке таймера рассчитывался `earned` через `calculateEarned(durationHours, rate)`
2. Это значение передавалось в запись и попадало в форму через `useEntryForm.js`
3. Поле автоматически заполнялось рассчитанным значением

**Решение:** 
1. Убрать расчет и передачу `earned` при остановке таймера в `useTimer.js`
2. В `useEntryForm.js` очищать поле `earned` для записей из таймера (`isManual: false`):

```javascript
// ✅ ПРАВИЛЬНО
earned: effectiveEntry.isManual === false 
  ? '' 
  : (effectiveEntry.earned != null ? String(effectiveEntry.earned) : ''),
```

**Правило:** 
- Для записей из таймера (`isManual: false`) поле заработка должно быть пустым по умолчанию
- Пользователь должен ввести заработок вручную
- Ставка рассчитывается исходя из времени работы и заработанной суммы, а не наоборот
- После сохранения записи она получает `isManual: true` и начинает учитываться в расчетах

**Контекст:** Применяется в `src/hooks/useTimer.js` и `src/hooks/useEntryForm.js` при работе с записями из таймера.

**Файлы:** 
- `src/hooks/useTimer.js` (убрана передача earned)
- `src/hooks/useEntryForm.js` (очистка earned для записей из таймера)

---

## 2025-01-20 - Выбор категории не работает в модальном окне

**Проблема:** Клик по кнопке выбора категории в модальном окне редактирования записи ничего не делал - dropdown не открывался.

**Причина:** 
1. Обработчик `handleClickOutside` срабатывал сразу после открытия dropdown, закрывая его
2. Z-index dropdown (`z-[999999]`) был равен z-index модального окна, что могло вызывать конфликты

**Решение:** 
1. Использовать `setTimeout` в `handleClickOutside` для предотвращения закрытия сразу после открытия
2. Увеличить z-index dropdown до `z-[9999999]` для гарантированного отображения поверх модального окна

```typescript
// ✅ ПРАВИЛЬНО
const handleClickOutside = event => {
  setTimeout(() => {
    if (
      selectRef.current &&
      !selectRef.current.contains(event.target) &&
      dropdownRef.current &&
      !dropdownRef.current.contains(event.target)
    ) {
      setIsOpen(false)
    }
  }, 0)
}
```

```typescript
// ✅ ПРАВИЛЬНО - z-index выше модального окна
className={`fixed z-[9999999] ...`}
```

**Правило:** 
- Dropdown элементы внутри модальных окон должны иметь z-index выше модального окна
- Обработчики клика вне элемента должны использовать `setTimeout` для предотвращения конфликтов с обработчиками открытия
- Для модальных окон с `z-[999999]` dropdown должен иметь `z-[9999999]` или выше

**Контекст:** Применяется в `src/components/ui/CategorySelect.tsx` при использовании внутри модальных окон.

**Файлы:** 
- `src/components/ui/CategorySelect.tsx` (исправлен handleClickOutside и z-index)

---

## 2025-01-20 - Неверный расчет заработка за день для записей из таймера

**Проблема:** При остановке таймера модальное окно редактирования показывало заработок за день (например, 30 ₽), хотя записей за этот день еще не было сохранено. Последняя запись была за предыдущий день (19.11), а таймер запускался 20.11.

**Причина:** 
1. При остановке таймера запись обновляется в store через `updateEntry()` с `earned = 30 ₽` и `isManual: false`
2. Модальное окно открывается с этой записью
3. Функция `getDailyEarnings()` видит запись в `entries` и добавляет ее заработок к общей сумме
4. Но запись еще не сохранена пользователем (имеет `isManual: false`), поэтому не должна учитываться

**Решение:** Исключить из расчета заработка за день записи с `isManual: false` (созданные таймером и еще не сохраненные):

```typescript
// ✅ ПРАВИЛЬНО
const dayEntries = entries.filter(e => e.date === dateToCheck && e.isManual !== false)
```

После сохранения записи пользователем она получает `isManual: true` и начинает учитываться в расчете.

**Правило:** 
- Записи с `isManual: false` (созданные таймером) не должны учитываться в заработке за день до сохранения пользователем
- После сохранения записи через `handleSave()` она получает `isManual: true` и начинает учитываться
- В расчете заработка за день учитываются только сохраненные записи (`isManual: true` или `isManual === undefined` для старых записей)

**Контекст:** Применяется в `src/components/modals/EditEntryModal.tsx` в функции `getDailyEarnings()` при расчете заработка за день.

**Файлы:** 
- `src/components/modals/EditEntryModal.tsx` (исправлена логика `getDailyEarnings()`)

**Дополнительно:** Это исправление связано с предыдущим исправлением расчета заработка за день (2025-01-19), но теперь учитывает специфику записей из таймера.

---

## 2025-01-19 - Массовое исправление нарушений правил проекта

**Проблема:** При аудите правил проекта обнаружено 79 нарушений:
- 56 использований `cursor-pointer` на интерактивных элементах
- 21 использование `key={index}` в списках React
- 2 прямых использования `getState()` в Zustand stores

**Решение:** Систематическое исправление всех нарушений:

1. **key={index} → уникальные ID:**
   - Заменены все `key={index}` на уникальные стабильные ключи из данных
   - Использованы: `feature.name`, `task.title`, `action.label`, `bar.x`, `coin.x`, `point.x`
   - Для случаев без уникального ID - комбинированные ключи: `key={feature-${name}-${index}}`

2. **cursor-pointer удален:**
   - Удален со всех интерактивных элементов (button, input, select, label, summary)
   - Браузер автоматически предоставляет правильный курсор для интерактивных элементов
   - Оставлен только для неинтерактивных элементов (div, span), которые должны быть кликабельными

3. **Zustand stores оптимизированы:**
   - Заменено `useUIStore.getState().openModal()` на `useOpenModal()` хук
   - Оптимизирован `useEffect` в `InsightsPanel.tsx` - использует `entries` из хука реактивно
   - Оставлено одно использование `getState()` в `useState` инициализаторе (допустимо для синхронной инициализации)

**Правило:**
```typescript
// ❌ НЕПРАВИЛЬНО - key={index}
{items.map((item, index) => (
  <Item key={index} item={item} />
))}

// ✅ ПРАВИЛЬНО - уникальный ID
{items.map((item) => (
  <Item key={item.id} item={item} />
))}

// ✅ ПРАВИЛЬНО - комбинированный ключ если нет ID
{items.map((item, index) => (
  <Item key={`${item.name}-${index}`} item={item} />
))}
```

```typescript
// ❌ НЕПРАВИЛЬНО - cursor-pointer на button
<button className="cursor-pointer">Click</button>

// ✅ ПРАВИЛЬНО - браузер предоставляет курсор
<button>Click</button>
```

```typescript
// ❌ НЕПРАВИЛЬНО - getState() в компоненте
const openModal = useUIStore.getState().openModal

// ✅ ПРАВИЛЬНО - атомарный селектор
const openModal = useOpenModal()
```

**Best Practice:**
- Всегда используй уникальные стабильные ID для `key` в списках React
- Никогда не используй `cursor-pointer` на интерактивных элементах (button, a, input, select, label)
- Всегда используй атомарные селекторы Zustand вместо прямого `getState()`
- Для синхронной инициализации в `useState` допустимо использовать `getState()`, но лучше избегать

**Контекст:** Применяется при:
- Работе со списками в React компонентах
- Стилизации интерактивных элементов
- Оптимизации Zustand stores для минимизации ре-рендеров

**Файлы:** 
- 30+ компонентов обновлено (см. `RULES_AUDIT_REPORT.md` для полного списка)

---

## 2025-11-22 - Детальные настройки уведомлений

**Проблема:** Пользователь запросил более детальную настройку уведомлений с возможностью настройки условий показа (пороги, минимальная длительность, только рабочие дни и т.д.) и частоты показа (максимум в день, минимальный интервал, показывать каждые X часов и т.д.).

**Решение:**

1. **Расширение типов (`src/types/index.ts`):**
   - Добавлены интерфейсы `NotificationConditions` и `NotificationFrequency` для детальных настроек.
   - Обновлен `NotificationCategorySettings` для включения опциональных `conditions` и `frequency`.

2. **Обновление структуры данных (`src/store/useSettingsStore.ts`):**
   - Добавлены дефолтные настройки `conditions` и `frequency` для категорий `overtime` и `breaks`.

3. **Обновление UI компонента (`src/components/settings/NotificationsDisplaySettingsPanel.tsx`):**
   - Создан новый компонент `CategoryAdvancedSettings` для отображения детальных настроек.
   - Добавлена кнопка "Детальные настройки" с раскрывающимся блоком для категорий, которые поддерживают детальные настройки (`overtime`, `breaks`, `timer`, `entries`).
   - Для каждой категории реализованы специфичные настройки:
     - **overtime**: Порог предупреждения, критический порог, только рабочие дни, показывать только один раз в день, минимальный интервал.
     - **breaks**: Минимальная длительность работы, только активная работа, показывать каждые X часов, минимальный интервал.
     - **timer**: Показывать при запуске/остановке/паузе, минимальная длительность для показа.
     - **entries**: Показывать при добавлении/обновлении/удалении.

4. **Обновление логики проверки (`src/utils/notificationHelpers.ts`):**
   - Добавлена функция `checkConditions` для проверки условий показа уведомления.
   - Добавлена функция `checkFrequency` для проверки частоты показа с отслеживанием в `Map`.
   - Обновлена функция `shouldShowNotification` для использования новых проверок.
   - Добавлено хранилище `notificationFrequencyTracker` для отслеживания частоты показа.

5. **Обновление UI store (`src/store/useUIStore.ts`):**
   - Функции `showSuccess`, `showError`, `showWarning`, `showInfo` теперь принимают опциональный параметр `context` для передачи дополнительной информации (длительность, значение, рабочий день, активная работа).

**Правило:** При реализации детальных настроек для категорий уведомлений:
- Используй опциональные поля в структуре данных для обратной совместимости.
- Создавай отдельные компоненты для сложных настроек, чтобы не перегружать основной UI.
- Используй `Map` для отслеживания частоты показа в памяти (не сохраняй в localStorage, так как это временные данные).
- Передавай контекст через параметры функций показа уведомлений для проверки условий.

**Контекст:** Применяется при работе с настройками уведомлений, требующими детальной конфигурации условий и частоты показа.

**Файлы:**
- `src/types/index.ts`
- `src/store/useSettingsStore.ts`
- `src/components/settings/NotificationsDisplaySettingsPanel.tsx`
- `src/utils/notificationHelpers.ts`
- `src/store/useUIStore.ts`

---

## 2025-11-22 - Проверка логики звуковых уведомлений

**Проблема:** Пользователь запросил проверку логики работы звуковых уведомлений - правильно ли срабатывают при выставлении интервала и проигрывается ли выбранная мелодия.

**Решение:**

1. **Проверка логики интервала (`src/hooks/useTimer.js`):**
   - Логика работает правильно: интервал корректно конвертируется из минут в секунды
   - Используется `lastNotificationTimeRef` для отслеживания последнего показа
   - Настройки обновляются в реальном времени через зависимость `notifications` в `useEffect`

2. **Проверка проигрывания мелодии:**
   - Все звуки из UI определены в `useSound.js`: `chime`, `alert`, `phone`, `doorbell`, `alarm`, `notification`, `bell`, `beep`, `ping`
   - `handleTestSound` корректно вызывает `playSound` с переданным типом
   - Выбранный звук сохраняется в `notificationSound` и используется в `useTimer.js`

3. **Исправления:**
   - Добавлен `ping` в типы `NotificationSettings` в `src/types/index.ts`
   - Обновлен комментарий в `src/store/useSettingsStore.ts`

4. **Ресурсы для звуков:**
   - Freesound.org - бесплатная библиотека с открытой лицензией
   - Pixabay - бесплатные звуки без лицензионных отчислений
   - Zapsplat - высокое качество звуков
   - SoundEffect.app - более 400 000 звуков
   - ZvukBox - более 29 000 звуков
   - Soundeff - более 10 000 аудиоэффектов

**Правило:** При проверке логики звуковых уведомлений:
- Проверяй конвертацию интервалов (минуты → секунды)
- Убеждайся, что все звуки из UI определены в коде
- Проверяй синхронизацию типов между UI, store и кодом
- Используй `useRef` для отслеживания времени последнего показа
- Обновляй настройки в реальном времени через зависимости в `useEffect`

**Контекст:** Применяется при работе со звуковыми уведомлениями, проверке логики интервалов и проигрывания звуков.

**Файлы:**
- `src/hooks/useTimer.js`
- `src/hooks/useSound.js`
- `src/components/modals/SoundNotificationsSettingsModal.tsx`
- `src/types/index.ts`
- `src/store/useSettingsStore.ts`

## 2025-11-22 - Добавление новых звуков для продуктивности

**Проблема:** Пользователь запросил замену текущих 9 звуков уведомлений на более подходящие для рабочей обстановки и продуктивности.

**Решение:**

1. **Добавление реализации звуков (`src/hooks/useSound.js`):**
   - Добавлены 9 новых звуков: `gentle`, `soft`, `zen`, `focus`, `breeze`, `crystal`, `harmony`, `whisper`, `bloom`
   - Каждый звук реализован с использованием Tone.js синтезатора
   - Звуки оптимизированы для частого прослушивания (не раздражающие)

2. **Обновление типов (`src/types/index.ts`):**
   - Добавлены новые звуки в тип `notificationSound` в `NotificationSettings`

3. **Обновление UI (`src/components/modals/SoundNotificationsSettingsModal.tsx`):**
   - Добавлены новые звуки в массив `soundTypes` с описаниями
   - Новые звуки отображаются в сетке вместе со старыми

4. **Обновление дефолтных значений (`src/store/useSettingsStore.ts`):**
   - Обновлен комментарий с полным списком доступных звуков

**Новые звуки:**
- **gentle** (Нежный) - мягкий, ненавязчивый звук (D5, F5)
- **soft** (Мягкий) - приглушенный, успокаивающий (A4, C5)
- **zen** (Дзен) - медитативный, спокойный (G4, B4, D5)
- **focus** (Фокус) - помогает концентрироваться (C5, E5)
- **breeze** (Легкий ветерок) - воздушный, легкий (F5, A5)
- **crystal** (Кристалл) - чистый, прозрачный (E6, G6)
- **harmony** (Гармония) - гармоничный, приятный (C5, E5, G5)
- **whisper** (Шепот) - тихий, ненавязчивый (B4, D5)
- **bloom** (Цветение) - мягкий, восходящий (C4, E4, G4, C5)

**Правило:** При добавлении новых звуков уведомлений:
- Добавляй реализацию в `useSound.js` с использованием `safePlay`
- Обновляй типы в `types/index.ts` для TypeScript валидации
- Добавляй в UI компонент с описанием для пользователя
- Обновляй комментарии в store для документации
- Используй ноты и интервалы, подходящие для частого прослушивания

**Контекст:** Применяется при расширении библиотеки звуков уведомлений для улучшения пользовательского опыта.

**Файлы:**
- `src/hooks/useSound.js`
- `src/types/index.ts`
- `src/components/modals/SoundNotificationsSettingsModal.tsx`
- `src/store/useSettingsStore.ts`

## 2025-11-22 - Удаление дублирования иконок в уведомлениях

**Проблема:** В уведомлениях о переработке, перерывах и Pomodoro использовались эмодзи-иконки (🚨, ⚠️, ⏸️, 🍅) в тексте сообщений, что создавало дублирование с иконками, которые автоматически добавляются компонентом уведомления на основе типа (error, warning, success, info).

**Решение:**

1. **Удаление эмодзи из текста сообщений (`src/hooks/useOvertimeAlerts.js`):**
   - Убраны эмодзи 🚨 и ⚠️ из сообщений о критической переработке и предупреждении о переработке
   - Убраны эмодзи из логов

2. **Удаление эмодзи из напоминаний о перерывах (`src/hooks/useBreakReminders.js`):**
   - Убран эмодзи ⏸️ из сообщения о перерыве
   - Убран эмодзи из логов

3. **Удаление эмодзи из уведомлений Pomodoro (`src/hooks/usePomodoro.js`):**
   - Убраны эмодзи 🍅 из всех сообщений о Pomodoro (завершен, запущен, остановлен)

4. **Исправление тестовых уведомлений (`src/components/modals/SoundNotificationsSettingsModal.tsx`):**
   - Убраны эмодзи из тестовых уведомлений о переработке, перерывах и Pomodoro

5. **Исправление других уведомлений (`src/App.tsx`):**
   - Убран эмодзи 🎨 из уведомления об обновлении цветов категорий

6. **Проверка иконок в компонентах (`src/components/ui/NotificationVariants.tsx`):**
   - Подтверждено, что используются стандартные иконки из проекта (CheckCircle2, AlertCircle, Info, AlertTriangle из lucide-react)
   - Иконки не цветные - они получают цвет через className
   - Добавлен импорт X для кнопки закрытия

**Правило:** В уведомлениях не должно быть эмодзи-иконок в тексте сообщений, так как компонент уведомления автоматически добавляет соответствующую иконку на основе типа (success, error, warning, info). Иконки должны быть стандартными из проекта (lucide-react), не цветными, и получать цвет через className.

**Контекст:** Применяется при создании и обновлении уведомлений в приложении.

**Файлы:**
- `src/hooks/useOvertimeAlerts.js`
- `src/hooks/useBreakReminders.js`
- `src/hooks/usePomodoro.js`
- `src/components/modals/SoundNotificationsSettingsModal.tsx`
- `src/components/ui/NotificationVariants.tsx`
- `src/App.tsx`

## 2025-11-22 - Добавление предпросмотра уведомлений в настройках

**Проблема:** Пользователь запросил добавление возможности предпросмотра уведомлений в модальном окне "Настройки отображения уведомлений" для тестирования различных типов уведомлений.

**Решение:**

1. **Добавление секции предпросмотра (`src/components/modals/NotificationsDisplayModal.tsx`):**
   - Добавлена новая секция "Предпросмотр уведомлений" с кнопками для каждого типа
   - Использован хук `useNotifications()` для доступа к методам показа уведомлений
   - Добавлены иконки для каждого типа уведомления (CheckCircle, XCircle, AlertTriangle, Info)
   - Кнопки стилизованы в соответствии с типом уведомления (цвета, границы, фон)

2. **Интеграция с системой уведомлений:**
   - Использованы методы `showSuccess`, `showError`, `showWarning`, `showInfo` из хука `useNotifications()`
   - Каждое уведомление показывается с соответствующим типом и длительностью
   - Уведомления проходят через систему фильтрации настроек отображения

3. **UI компонента:**
   - Секция предпросмотра размещена перед панелью настроек
   - Использован `glass-effect` для стилизации
   - Кнопки расположены в сетке (2 колонки на мобильных, 4 на десктопе)
   - Добавлены hover-эффекты и переходы

**Правило:** При создании инструментов для разработки и тестирования (особенно настроек уведомлений) добавляй секцию предпросмотра с кнопками для тестирования различных типов уведомлений. Это позволяет разработчику быстро проверить, как выглядят уведомления и работают ли настройки фильтрации.

**Контекст:** Инструменты разработчика, настройки уведомлений, предпросмотр UI компонентов.

**Файлы:**
- `src/components/modals/NotificationsDisplayModal.tsx`

## 2025-11-22 - Организация документации проекта

**Проблема:** В корне проекта накопилось множество .md файлов (отчеты, планы, анализы), что затрудняло навигацию и поддержку документации.

**Решение:**

1. **Создание структуры подпапок в docs:**
   - `docs/reports/` - для отчетов о багах, интеграциях, проверках правил
   - `docs/analysis/` - для аналитических документов
   - `docs/technical/` - для технических документов и тестовых файлов

2. **Перенос файлов из корня:**
   - `BUG_FIX_PLAN*.md` → `docs/reports/` (4 файла)
   - `TRIGGER_PHRASES_ADDED.md` → `docs/reports/`
   - `RULES_VERIFICATION_REPORT.md` → `docs/reports/`
   - `INTEGRATION_PHASE1_COMPLETE.md` → `docs/reports/`
   - `INTEGRATION_PHASE2_COMPLETE.md` → `docs/reports/`
   - `RULES_CONFLICTS_REPORT.md` → `docs/reports/`
   - `RULES_AUDIT_REPORT.md` → `docs/reports/` (как `RULES_AUDIT_REPORT_ROOT.md`, так как отличался от версии в docs)
   - `IMPROVEMENT_SUGGESTIONS.md` → `docs/reports/`
   - `TEST_RESULTS_SUMMARY.md` → `docs/reports/`
   - `KIRO_ANALYSIS.md` → `docs/analysis/`
   - `CLAUDE.md` → `docs/technical/`
   - `test.md` → `docs/technical/`

3. **Обновление ссылок:**
   - Обновлены ссылки в `Kiro/STRUCTURE.md` на `docs/analysis/KIRO_ANALYSIS.md`
   - Обновлены ссылки в `docs/reports/INTEGRATION_PHASE2_COMPLETE.md` на новый путь
   - Обновлены ссылки в `docs/reports/IMPROVEMENT_SUGGESTIONS.md` на `docs/reports/TEST_RESULTS_SUMMARY.md`

**Правило:** Все .md файлы документации (кроме README.md в корне) должны находиться в папке `docs` с соответствующей подпапкой:
- `docs/reports/` - отчеты, планы исправления багов, результаты проверок
- `docs/analysis/` - аналитические документы
- `docs/technical/` - технические документы, тестовые файлы
- `docs/guides/` - руководства
- `docs/plans/` - планы разработки
- `docs/problems/` - отчеты о проблемах

При переносе файлов обязательно обновляй все ссылки на них в других документах.

**Контекст:** Организация документации проекта, поддержка структуры файлов.

**Файлы:**
- Все перенесенные .md файлы
- `Kiro/STRUCTURE.md`
- `docs/reports/INTEGRATION_PHASE2_COMPLETE.md`
- `docs/reports/IMPROVEMENT_SUGGESTIONS.md`

---

## 2025-01-19 - Подготовка папки проекта для другого ИИ

**Проблема:** Нужно было подготовить папку `project_001` с минимальным набором файлов проекта для работы другого ИИ, исключив все файлы, связанные с Cursor IDE.

**Решение:** 
1. Удалено все содержимое папки `project_001` (кроме проблемных файлов `nul` - зарезервированное имя Windows)
2. Скопированы все необходимые файлы проекта:
   - Конфигурационные файлы (package.json, tsconfig.json, vite.config.js, tailwind.config.js, eslint.config.js, и др.)
   - Исходный код (src/)
   - Публичные файлы (public/)
   - Тесты (e2e/)
   - Необходимые скрипты из scripts/ (исключая Cursor-специфичные)
   - Документация (README.md, .gitignore)
3. Удалены из package.json скрипты, специфичные для Cursor IDE (quality:check, quality:sync-rules, quality:to-beads)

**Правило:** 
- При подготовке проекта для другого ИИ/разработчика:
  - ✅ Включать: конфигурации, исходный код, публичные файлы, тесты, необходимые скрипты, документацию
  - ❌ Исключать: папки `.cursor/`, `.enforcer/`, файлы `.agent-enforcer.json`, скрипты, специфичные для Cursor IDE
  - ❌ Исключать: временные файлы (nul, tree.txt, time-tracker-export-*.json)
  - Проверять package.json на наличие скриптов, ссылающихся на отсутствующие инструменты
- Файл `nul` в Windows - зарезервированное имя, его нельзя удалить стандартными методами (не критично)

**Контекст:** Применяется при подготовке проекта для передачи другому разработчику или ИИ, когда нужно исключить IDE-специфичные файлы и инструменты.

**Файлы:** 
- `project_001/` - подготовленная папка
- `project_001/package.json` - удалены Cursor-специфичные скрипты

**Результат:** Создана чистая копия проекта с 310 файлами, без файлов Cursor IDE, готовая для работы другого ИИ.

---

## 2025-11-20 - Создание Hero секции с анимированными счётчиками

**Проблема:** Нужно было создать Hero секцию с анимированными счётчиками чисел, градиентными заголовками и fade-in анимацией при загрузке.

**Решение:** 
1. Создан хук `useCountUp` для плавной анимации чисел от 0 до целевого значения за 2 секунды
2. Создана утилита `getProjectStats` для подсчёта статистики из JSON данных
3. Использован `useState` и `useEffect` для fade-in анимации (хуки на верхнем уровне)
4. Градиентная анимация заголовка через Tailwind keyframes с `bg-clip-text`
5. Плавный скролл добавлен в `globals.css`

**Правило:** 
```typescript
// ✅ ПРАВИЛЬНО - хуки на верхнем уровне
export default function Home() {
  const [isVisible, setIsVisible] = useState(false); // Хук на верхнем уровне
  const stats = getProjectStats();
  
  const countEffects = useCountUp(stats.totalEffects, 2000);
  
  useEffect(() => {
    setIsVisible(true);
  }, []); // Обязательный массив зависимостей
  
  // Остальной код...
}

// ✅ ПРАВИЛЬНО - анимация градиента текста
<span className="bg-gradient-to-r from-primary via-secondary to-primary bg-clip-text text-transparent bg-[length:200%_auto] animate-gradient-x">
  Текст
</span>
```

**Контекст:** Применяется при создании Hero секций с анимированными элементами, счётчиками и градиентными эффектами в Next.js проектах.

**Файлы:** 
- `app/page.tsx` - Hero секция
- `lib/hooks/useCountUp.ts` - хук для анимации чисел
- `lib/utils/stats.ts` - утилиты для подсчёта статистики
- `tailwind.config.ts` - добавлены keyframes для градиентных анимаций
- `app/globals.css` - добавлен smooth scroll

**Дополнительно:** 
- Для анимации градиента текста нужно использовать `bg-[length:200%_auto]` вместе с `animate-gradient-x`
- Keyframes должны анимировать `background-position`, а не `background-size`
- Все хуки должны быть на верхнем уровне компонента, до любых условий

---

## 2025-11-20 - Пустая страница из-за начального состояния isVisible = false

**Проблема:** Hero секция не отображалась на странице, хотя код был правильным. Страница была полностью пустой (только темный фон).

**Причина:** Начальное состояние `isVisible` было установлено в `false`, и контент был скрыт через `opacity-0 translate-y-8`. Если `useEffect` не успевал выполниться или была ошибка, контент оставался невидимым.

**Решение:** 
1. Изменить начальное состояние `isVisible` на `true` для немедленного отображения
2. Упростить анимацию - убрать условное скрытие контента
3. Добавить явный `bg-dark` на `main` элемент для гарантированного фона
4. Добавить обработку ошибок с fallback значениями

**Правило:** 
```typescript
// ❌ НЕПРАВИЛЬНО - контент может остаться скрытым
const [isVisible, setIsVisible] = useState(false);
// Если useEffect не выполнится - контент невидим

// ✅ ПРАВИЛЬНО - контент виден сразу, анимация опциональна
const [isVisible, setIsVisible] = useState(true);
// Контент всегда виден, анимация добавляется при необходимости

// ✅ ПРАВИЛЬНО - явный фон на main
<main className="min-h-screen bg-dark">
  {/* Контент */}
</main>
```

**Контекст:** Применяется при создании компонентов с fade-in анимациями. Всегда начинать с видимого состояния, а не скрытого.

**Файлы:** 
- `app/page.tsx` - исправлено начальное состояние и упрощена анимация

**Дополнительно:** 
- При использовании fade-in анимаций всегда проверять, что контент виден даже без анимации
- Добавлять fallback значения для данных, загружаемых асинхронно
- Явно указывать фоновые цвета на основных контейнерах

---

## 2025-11-20 - Восстановление Hero секции после упрощений для отладки

**Проблема:** В процессе отладки пустой страницы компонент был упрощён до inline стилей, что нарушило требования промтов 1-3.

**Причина:** При отладке проблемы с пустой страницей (которая оказалась из-за не запущенного dev сервера) компонент был упрощён для быстрой диагностики, но упрощения не были восстановлены.

**Решение:** 
1. Восстановлен `app/layout.tsx` - убраны inline стили, используются Tailwind классы `bg-dark` и `text-light`
2. Восстановлен `app/page.tsx` - полная Hero секция с:
   - Анимированным заголовком с градиентом (`bg-gradient-to-r from-primary via-secondary to-primary`)
   - Fade-in анимацией при загрузке (`opacity-0 translate-y-8` → `opacity-100 translate-y-0`)
   - Живой статистикой с `useCountUp` хуком
   - Градиентным фоном с анимацией (`animate-gradient-shift`)
   - Стрелкой вниз для скролла
   - Все стили через Tailwind, никаких inline стилей
3. Использован API route `/api/stats` для загрузки статистики вместо прямого импорта JSON

**Правило:** 
```typescript
// ❌ НЕПРАВИЛЬНО - упрощения для отладки остались в коде
export default function Home() {
  return (
    <div style={{ backgroundColor: '#1a1a1a' }}>
      {/* inline стили */}
    </div>
  );
}

// ✅ ПРАВИЛЬНО - все стили через Tailwind, соответствие промтам
export default function Home() {
  return (
    <main className="min-h-screen">
      <section className="relative h-[70vh] flex flex-col items-center justify-center">
        {/* Tailwind классы */}
      </section>
    </main>
  );
}
```

**Контекст:** При отладке проблем всегда нужно помнить о восстановлении упрощений после решения проблемы. Упрощения должны быть временными и удаляться после диагностики.

**Файлы:** 
- `app/page.tsx` - восстановлена полная Hero секция
- `app/layout.tsx` - убраны inline стили, используются Tailwind классы

**Дополнительно:** 
- Всегда проверяй соответствие кода требованиям промтов после отладки
- Используй API routes для загрузки данных в клиентских компонентах вместо прямого импорта JSON
- Fade-in анимация должна начинаться с `opacity-0` и переходить в `opacity-100` через `useEffect`

---

## Шаблон для новых записей

```markdown
## [Дата] - [Краткое описание проблемы]

**Проблема:** [Детальное описание проблемы]

**Решение:** [Как исправили]

**Правило:** 
```код
// Пример правильного подхода
```

**Контекст:** [Где это применимо, в каких файлах]

**Файлы:** [список затронутых файлов]
```
