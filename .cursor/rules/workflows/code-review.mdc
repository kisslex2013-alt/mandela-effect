---
description: Workflow для систематического code review с фокусом на качество и best practices
alwaysApply: false
---

# Code Review Workflow

## Когда использовать

Активируй этот workflow когда пользователь запрашивает:

- "Проверь мой код"
- "Code review для [файл/компонент]"
- "Что можно улучшить в этом коде?"
- "Есть ли баги или проблемы?"

**ПРИНУДИТЕЛЬНАЯ АКТИВАЦИЯ:**
Если пользователь использует триггерные фразы ("по правилам", "структурированно", "с планированием", "workflow"), ОБЯЗАТЕЛЬНО:
1. ✅ Создать план review
2. ✅ Запустить `npm run quality:check`
3. ✅ Создать задачи в Beads для найденных проблем
4. ✅ Следовать всем шагам workflow

## Структура review (выполняй последовательно)

### 1. Быстрая проверка (5 секунд)

**Автоматическая проверка качества:**

Перед началом review всегда запускай автоматическую проверку:

```bash
# Проверка качества кода через agent-enforcer
npm run quality:check

# Или для конкретного файла
npm run quality:check:verbose
```

**Ручная проверка:**

✓ Код компилируется без ошибок?
✓ TypeScript типы корректны?
✓ Нет console.log/debugger в production коде?
✓ Импорты организованы и не дублируются?

**Если найдены проблемы качества:**

1. **Критические ошибки (error):**
   - Создай задачу в Beads: `bd create "Исправить [правило] в [файл]" -t bug -p 0`
   - Блокируй merge до исправления

2. **Предупреждения (warning):**
   - Создай задачу в Beads: `bd create "Улучшить [правило] в [файл]" -t task -p 2`
   - Можно исправить позже, но лучше сразу

### 2. Архитектура и дизайн (30 секунд)

**Проверь:**

- Соответствие Single Responsibility Principle
- Правильная абстракция (не over-engineering)
- Разделение concerns (UI / logic / data)
- Naming conventions понятны и консистентны

**Вопросы для анализа:**

- Можно ли переиспользовать этот код?
- Легко ли тестировать?
- Понятен ли код без комментариев?
- Соответствует ли архитектуре проекта (@001-workspace.mdc)?

### 3. React-специфичные проблемы

```typescript
// ❌ ПРОБЛЕМА: Прямая мутация state
const [items, setItems] = useState([])
items.push(newItem) // Плохо!

// ✅ РЕШЕНИЕ: Иммутабельное обновление
setItems([...items, newItem])

// ❌ ПРОБЛЕМА: Отсутствие cleanup в useEffect
useEffect(() => {
  const interval = setInterval(fetchData, 1000)
  // Нет cleanup!
}, [])

// ✅ РЕШЕНИЕ: Всегда возвращай cleanup
useEffect(() => {
  const interval = setInterval(fetchData, 1000)
  return () => clearInterval(interval)
}, [])

// ❌ ПРОБЛЕМА: Неоптимальные селекторы Zustand
const store = useStore() // Подписка на весь store!

// ✅ РЕШЕНИЕ: Атомарные селекторы
const user = useStore((state) => state.user)

// ❌ ПРОБЛЕМА: Inline объекты в props
<Component style={{ color: 'red' }} /> // Новый объект каждый рендер

// ✅ РЕШЕНИЕ: Tailwind классы или useMemo
<Component className="text-red-600" />

// ❌ ПРОБЛЕМА: Пропущенные dependencies в useEffect
useEffect(() => {
  fetchData(userId) // userId в зависимостях?
}, []) // Пустой массив - баг!

// ✅ РЕШЕНИЕ: Все внешние переменные в deps
useEffect(() => {
  fetchData(userId)
}, [userId])
```

### 4. Performance проблемы

**Проверь на:**

- Излишние ре-рендеры (используй React DevTools)
- Тяжелые вычисления без useMemo/useCallback
- Большие списки без виртуализации (>100 элементов)
- Неоптимизированные изображения
- Bundle size (проверь через `npm run build`)

**Типичные проблемы:**

```typescript
// ❌ Дорогие вычисления каждый рендер
function Component({ items }) {
  const sorted = items.sort((a, b) => b.value - a.value) // Каждый рендер!
  return <List items={sorted} />
}

// ✅ Мемоизация дорогих операций
function Component({ items }) {
  const sorted = useMemo(
    () => items.sort((a, b) => b.value - a.value),
    [items]
  )
  return <List items={sorted} />
}

// ❌ Inline функции в списках
{items.map(item => (
  <Item key={item.id} onClick={() => handleClick(item)} />
))}

// ✅ Используй useCallback или стабильные функции
const handleClick = useCallback((item) => {
  // логика
}, [])

{items.map(item => (
  <Item key={item.id} onClick={handleClick} item={item} />
))}
```

### 5. Безопасность (для backend кода)

```typescript
// ❌ SQL инъекция
const query = `SELECT * FROM users WHERE id = ${userId}` // Опасно!

// ✅ Параметризованные запросы
const query = prisma.user.findUnique({ where: { id: userId } })

// ❌ Возврат чувствительных данных
return { user: { id, email, password, apiKey } } // Утечка!

// ✅ Фильтруй чувствительные поля
return { user: { id, email } }

// ❌ Отсутствие rate limiting
export async function POST(request) {
  // Нет защиты от спама
  return await createUser(data)
}

// ✅ Rate limiting
export async function POST(request) {
  await checkRateLimit(request.ip)
  return await createUser(data)
}

// ❌ Слабая валидация
if (email && password) { } // Только проверка на существование

// ✅ Строгая валидация с Zod
const schema = z.object({
  email: z.string().email(),
  password: z.string().min(8).regex(/[A-Z]/).regex(/[0-9]/)
})
```

### 6. Тестируемость

**Проверь наличие:**

- Явных типов для всех публичных функций
- Чистых функций без side effects
- Инъекции зависимостей (не хардкод)
- Разделения logic и presentation

```typescript
// ❌ Сложно тестировать
function UserProfile() {
  const [user, setUser] = useState(null)
  
  useEffect(() => {
    fetch('/api/user')
      .then(r => r.json())
      .then(setUser)
  }, [])
  
  if (!user) return <div>Loading...</div>
  
  return <div>{user.name}</div>
}

// ✅ Легко тестировать
function UserProfile({ userId }: { userId: string }) {
  const user = useUser(userId) // Можно замокать
  
  if (!user) return <div>Loading...</div>
  
  return <UserProfileView user={user} />
}

// Отдельный testable hook
function useUser(id: string) {
  const [user, setUser] = useState(null)
  
  useEffect(() => {
    fetchUser(id).then(setUser)
  }, [id])
  
  return user
}
```

### 7. Accessibility (a11y)

```typescript
// ❌ Проблемы доступности
<div onClick={handleClick}>Click me</div>
<img src="photo.jpg" />
<input type="text" />

// ✅ Правильная accessibility
<button onClick={handleClick}>Click me</button>
<img src="photo.jpg" alt="User profile photo" />
<label htmlFor="email">Email</label>
<input id="email" type="text" aria-label="Email address" />

// Проверь:
// - Все интерактивные элементы доступны с клавиатуры
// - Семантические HTML теги (button, nav, main, article)
// - ARIA атрибуты где необходимо
// - Контраст цветов (минимум 4.5:1)
// - Focus indicators видны
```

### 8. Error handling

```typescript
// ❌ Игнорирование ошибок
async function fetchData() {
  const response = await fetch('/api/data')
  return response.json() // Что если 404 или 500?
}

// ✅ Полная обработка ошибок
async function fetchData() {
  try {
    const response = await fetch('/api/data')
    
    if (!response.ok) {
      throw new Error(`HTTP ${response.status}: ${response.statusText}`)
    }
    
    return await response.json()
    
  } catch (error) {
    console.error('Failed to fetch data:', error)
    throw new Error('Data fetch failed')
  }
}

// ❌ Нет error boundaries
function App() {
  return <MainContent /> // Если упадет - белый экран
}

// ✅ Error boundary для защиты
function App() {
  return (
    <ErrorBoundary fallback={<ErrorPage />}>
      <MainContent />
    </ErrorBoundary>
  )
}
```

## Формат ответа для review

Структурируй feedback следующим образом:

```markdown
## 🔴 Критические проблемы (исправить немедленно)

1. **[Название проблемы]**
   - Где: `filename.tsx:42`
   - Проблема: [краткое описание]
   - Решение: [код или объяснение]
   - Почему важно: [последствия если не исправить]

## 🟡 Улучшения (желательно исправить)

2. **[Название улучшения]**
   - Где: `filename.tsx:15-20`
   - Текущий код: [что есть сейчас]
   - Предложение: [как лучше]
   - Выгода: [что получим]

## 🟢 Хорошие практики (уже применены)

- ✅ Использованы TypeScript типы
- ✅ Правильная структура компонента
- ✅ Есть error handling

## 📊 Метрики

- Сложность: [низкая/средняя/высокая]
- Тестируемость: [легко/средне/сложно]
- Производительность: [оптимально/приемлемо/проблемы]
- Безопасность: [✅ ОК / ⚠️ Есть риски]

## 🎯 Приоритет исправлений

1. [Самое важное]
2. [Важное]
3. [Можно отложить]
```

## Чек-лист перед завершением review

□ Все критические проблемы выявлены
□ Даны конкретные примеры решений
□ Объяснены причины каждого замечания
□ Отмечены хорошие моменты (не только критика)
□ Приоритезированы исправления
□ Предложены ссылки на документацию где нужно
□ Код можно улучшить по следующим критериям:
  □ Читаемость
  □ Производительность
  □ Безопасность
  □ Тестируемость
  □ Поддерживаемость

## Tone guidelines для review

- **Будь конструктивным**, не критикуй автора
- **Объясняй "почему"**, не только "что исправить"
- **Показывай примеры**, не ограничивайся теорией
- **Отмечай хорошее**, не только проблемы
- **Будь конкретным**, избегай общих фраз
- **Помни**: это zero-кодер, объясняй подробно

## Примеры хорошего feedback

❌ **Плохо**: "Этот код плохой, переделай"

✅ **Хорошо**: "В строке 15 происходит мутация state напрямую (`items.push()`), что нарушает принципы React. Используй иммутабельное обновление: `setItems([...items, newItem])`. Это гарантирует правильные ре-рендеры."

❌ **Плохо**: "Тут проблемы с производительностью"

✅ **Хорошо**: "Функция `sortItems()` вызывается при каждом рендере (строка 22), что может быть дорого для больших списков. Оберни её в `useMemo()` с зависимостью от `items`, чтобы пересчитывать только при изменении данных."

## References

@react-patterns.mdc
@security-best-practices.mdc
@performance-optimization.mdc
@testing-agent.mdc
