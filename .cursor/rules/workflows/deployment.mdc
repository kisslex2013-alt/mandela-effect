---
description: Правила деплоя проекта на Vercel через GitHub - ГЛАВНЫЙ скрипт деплоя
alwaysApply: false
# Примечание: Правила деплоя загружаются только при работе с деплоем
# Критически важно: НЕ изменять scripts/update-build-version.js без разрешения
---

# 🚀 Правила деплоя проекта

## ⚠️ КРИТИЧЕСКИ ВАЖНО: Главный скрипт деплоя

**ГЛАВНЫЙ ЕДИНСТВЕННЫЙ скрипт деплоя данного проекта:**

```
scripts/update-build-version.js
```

### Правила работы со скриптом деплоя:

1. **НЕ МЕНЯЙ** файл `scripts/update-build-version.js` **БЕЗ ЯВНОГО РАЗРЕШЕНИЯ** пользователя
2. **НЕ СОЗДАВАЙ** альтернативные скрипты деплоя без разрешения
3. **НЕ ИЗМЕНЯЙ** логику деплоя в этом скрипте без разрешения
4. Если нужно что-то изменить - **СНАЧАЛА СПРОСИ** пользователя

### Команда для деплоя:

```bash
npm run deploy
```

**Важно:** Команда `npm run deploy` выполняет:
1. Обновляет версию сборки через `scripts/update-build-version.js`
2. Делает git commit всех изменений
3. Делает git push в GitHub

**Vercel автоматически:**
- Обнаруживает новый push в GitHub
- Запускает сборку (`npm run build`)
- Деплоит собранный `dist/` на Vercel
- Обновляет production деплой

### Процесс деплоя:

1. **Локально:** `npm run deploy`
   - Обновляет версию сборки в `.env` и `public/version.json`
   - Делает git commit с сообщением "update"
   - Делает git push в GitHub

2. **Vercel автоматически:**
   - Обнаруживает новый push в GitHub (через webhook)
   - Запускает сборку проекта (`npm run build`)
   - Деплоит собранный `dist/` на Vercel
   - Обновляет production URL

**Примечание:** Проект использует Vercel для хостинга. Cloudflare не используется.

### Если нужно изменить скрипт:

**ОБЯЗАТЕЛЬНО** спроси пользователя перед любыми изменениями:
- "Можно ли изменить скрипт деплоя для [причина]?"
- "Нужно ли добавить [функциональность] в скрипт деплоя?"

**ПРАВИЛО:** "NEVER modify update-build-version.js without explicit user permission. Always ask first."

### Альтернативные скрипты:

- `scripts/deploy-to-cloudflare-wrangler.js` - **НЕ ИСПОЛЬЗУЕТСЯ** (старый скрипт для Cloudflare)
- `scripts/deploy-to-cloudflare.js` - **НЕ ИСПОЛЬЗУЕТСЯ** (старый скрипт через API)
- Другие скрипты деплоя - **НЕ СОЗДАВАТЬ** без разрешения
