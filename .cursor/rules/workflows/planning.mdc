---
description: Workflow для планирования любых задач - активируется автоматически для всех задач
alwaysApply: false
---

# 📋 Planning Workflow

## Когда использовать

Этот workflow должен использоваться для **всех задач**. Он обеспечивает, что AI всегда создает план перед началом работы.

**Активация:** Используй этот workflow при получении любой задачи - от исправления бага до разработки новой фичи.

**ПРИНУДИТЕЛЬНАЯ АКТИВАЦИЯ:**
Если пользователь использует триггерные фразы в запросе, ОБЯЗАТЕЛЬНО применяй полный workflow независимо от сложности задачи:

**Триггерные фразы:**
- "по правилам"
- "структурированно"
- "с планированием"
- "workflow"

**При триггере ОБЯЗАТЕЛЬНО:**
1. ✅ Создать детальный план (даже для простых задач)
2. ✅ Создать задачи в Beads (даже для багов)
3. ✅ Запустить проверку качества (`npm run quality:check`)
4. ✅ Следовать всем шагам workflow
5. ✅ Записать уроки после завершения

**Примеры запросов с триггером:**
- "Исправь баг по правилам"
- "Добавь фичу структурированно"
- "Проверь код с планированием"
- "Сделай это workflow"

## Процесс планирования

### Шаг 1: Анализ задачи

**Вопросы для анализа:**

1. **Что нужно сделать?**
   - Четко сформулируй цель
   - Определи scope (что включено, что исключено)

2. **Какие файлы затронуты?**
   - Какие файлы нужно создать?
   - Какие файлы нужно изменить?
   - Какие файлы нужно прочитать для понимания?

3. **Есть ли зависимости?**
   - Зависит ли задача от других компонентов?
   - Нужны ли внешние библиотеки?
   - Есть ли API endpoints?

4. **Какие edge cases?**
   - Что если нет данных?
   - Что если ошибка?
   - Что если пользователь не авторизован?

### Шаг 2: Создание задач в Beads (для сложных задач)

**Когда использовать Beads:**
- Задача требует 3+ шагов
- Есть зависимости между шагами
- Задача может занять больше одного сеанса работы

**Процесс создания задач в Beads:**

1. **Создай эпик (если задача большая):**
   ```bash
   bd create "[Название задачи]" -t epic -p 1 --json
   # Сохрани ID эпика (например: ttm-1)
   ```

2. **Создай подзадачи для каждого шага:**
   ```bash
   # Для каждого шага из плана
   bd create "Шаг 1: [Название]" -t task -p 1 --json
   bd create "Шаг 2: [Название]" -t task -p 1 --json
   # И т.д.
   ```

3. **Настрой зависимости между задачами:**
   ```bash
   # Если шаг 2 зависит от шага 1
   bd dep add ttm-3 ttm-2 --type blocks
   # ttm-3 (шаг 2) блокируется до завершения ttm-2 (шаг 1)
   ```

4. **Покажи план пользователю:**
   ```bash
   # Показать дерево зависимостей
   bd dep tree ttm-1
   
   # Показать готовые к работе задачи
   bd ready --json
   ```

5. **Начни с первой готовой задачи:**
   ```bash
   # Получить первую готовую задачу
   READY_TASK=$(bd ready --limit 1 --json | jq -r '.[0].id')
   
   # Обновить статус на "в работе"
   bd update $READY_TASK --status in_progress
   ```

**Пример для задачи "Добавить фильтрацию по категориям":**

```bash
# 1. Создать эпик
EPIC_ID=$(bd create "Добавить фильтрацию по категориям" -t epic -p 1 --json | jq -r '.id')

# 2. Создать подзадачи
TASK1=$(bd create "Изучить существующий код" -t task -p 1 --json | jq -r '.id')
TASK2=$(bd create "Добавить состояние фильтров в store" -t task -p 1 --json | jq -r '.id')
TASK3=$(bd create "Создать UI компонент фильтров" -t task -p 1 --json | jq -r '.id')
TASK4=$(bd create "Интегрировать фильтрацию в список" -t task -p 1 --json | jq -r '.id')

# 3. Настроить зависимости
bd dep add $TASK2 $TASK1 --type blocks  # Задача 2 зависит от задачи 1
bd dep add $TASK3 $TASK2 --type blocks  # Задача 3 зависит от задачи 2
bd dep add $TASK4 $TASK3 --type blocks  # Задача 4 зависит от задачи 3

# 4. Показать план
bd dep tree $EPIC_ID
bd ready --json
```

**При обнаружении бага во время работы:**

```bash
# Создать задачу-баг
BUG_ID=$(bd create "Баг: [описание]" -t bug -p 0 --json | jq -r '.id')

# Связать с текущей задачей
bd dep add $BUG_ID $CURRENT_TASK --type discovered-from
```

**При завершении задачи:**

```bash
# Закрыть задачу
bd close $TASK_ID --reason "Готово: [краткое описание результата]"

# Показать следующую готовую задачу
bd ready --json
```

### Шаг 3: Создание плана (текстовый)

**Используй этот шаблон:**

```markdown
## 📋 План выполнения: [Название задачи]

### Анализ
- **Цель:** [Что нужно достичь]
- **Scope:** 
  - ✅ Включено: [что делаем]
  - ❌ Не включено: [что НЕ делаем]
- **Зависимости:** [От чего зависит]
- **Риски:** [Что может пойти не так]

### Шаги выполнения

#### Шаг 1: [Название шага]
- **Файлы:** [список файлов]
- **Действия:** [что делаем]
- **Ожидаемый результат:** [что получим]
- **Проверка:** [как проверим]

#### Шаг 2: [Название шага]
...

### Edge Cases
- [ ] Что если [условие 1]?
- [ ] Что если [условие 2]?
- [ ] Что если [условие 3]?

### Тестирование
- [ ] Unit тесты (если применимо)
- [ ] Ручная проверка
- [ ] Проверка edge cases
- [ ] Проверка на мобильных устройствах (если UI)
```

### Шаг 3: Выполнение по плану

**Правила выполнения:**

1. **Следуй плану шаг за шагом**
   - Не пропускай шаги
   - Отмечай выполненные шаги

2. **Если обнаружил проблему:**
   ```markdown
   ## 🔄 Обновление плана
   
   **Причина:** [Почему меняем план]
   **Изменения:**
   - Было: [старый шаг]
   - Стало: [новый шаг]
   - Причина: [почему]
   ```

3. **Если план не работает:**
   - Остановись
   - Пересмотри план
   - Создай новый план
   - Объясни, почему старый план не сработал

### Шаг 4: Проверка результата

**После выполнения:**

1. Проверь, что все шаги выполнены
2. Проверь edge cases
3. Если есть проблемы → исправь и запиши урок в `lessons-learned.mdc`

## Примеры планов

### Пример 1: Простая задача (исправление бага)

```markdown
## 📋 План выполнения: Исправить ошибку рендеринга иконки

### Анализ
- **Цель:** Исправить ошибку "Objects are not valid as a React child" в BaseModal
- **Scope:** 
  - ✅ Включено: Исправление рендеринга titleIcon
  - ❌ Не включено: Изменение других частей BaseModal
- **Зависимости:** Нет
- **Риски:** Может сломать другие модальные окна

### Шаги выполнения

#### Шаг 1: Изучить проблему
- **Файлы:** `src/components/ui/BaseModal.tsx`
- **Действия:** Прочитать код, понять причину ошибки
- **Ожидаемый результат:** Понимание проблемы
- **Проверка:** Найти место, где происходит ошибка

#### Шаг 2: Исправить рендеринг
- **Файлы:** `src/components/ui/BaseModal.tsx`
- **Действия:** Использовать createElement для компонентов
- **Ожидаемый результат:** Иконки рендерятся корректно
- **Проверка:** Проверить, что ошибка исчезла

#### Шаг 3: Проверить другие модальные окна
- **Файлы:** Все файлы, использующие BaseModal
- **Действия:** Убедиться, что ничего не сломалось
- **Ожидаемый результат:** Все модальные окна работают
- **Проверка:** Ручная проверка

### Edge Cases
- [x] Что если titleIcon - это уже React элемент?
- [x] Что если titleIcon - это функция-компонент?
- [x] Что если titleIcon - это null/undefined?

### Тестирование
- [x] Ручная проверка модальных окон
- [x] Проверка с разными типами иконок
```

### Пример 2: Сложная задача (новая фича)

```markdown
## 📋 План выполнения: Добавить фильтрацию по категориям

### Анализ
- **Цель:** Добавить возможность фильтровать записи по категориям
- **Scope:** 
  - ✅ Включено: UI фильтров, логика фильтрации, сохранение в URL
  - ❌ Не включено: Сохранение фильтров в localStorage (следующая итерация)
- **Зависимости:** useEntriesStore, CategoryStore
- **Риски:** Может замедлить рендеринг при большом количестве записей

### Шаги выполнения

#### Шаг 1: Изучить существующий код
- **Файлы:** `src/store/useEntriesStore.ts`, `src/components/entries/EntriesList.tsx`
- **Действия:** Понять структуру данных и компонентов
- **Ожидаемый результат:** Понимание архитектуры
- **Проверка:** Найти, где хранятся записи и категории

#### Шаг 2: Добавить состояние фильтров
- **Файлы:** `src/store/useEntriesStore.ts`
- **Действия:** Добавить selectedCategories в state
- **Ожидаемый результат:** Состояние фильтров в store
- **Проверка:** Проверить, что состояние обновляется

#### Шаг 3: Создать UI фильтров
- **Файлы:** `src/components/entries/CategoryFilter.tsx`
- **Действия:** Создать компонент с чекбоксами категорий
- **Ожидаемый результат:** UI для выбора категорий
- **Проверка:** Визуальная проверка

#### Шаг 4: Интегрировать фильтрацию
- **Файлы:** `src/components/entries/EntriesList.tsx`
- **Действия:** Применить фильтры к списку записей
- **Ожидаемый результат:** Отображаются только выбранные категории
- **Проверка:** Ручная проверка фильтрации

### Edge Cases
- [ ] Что если нет выбранных категорий? (показывать все)
- [ ] Что если категория удалена? (убрать из фильтров)
- [ ] Что если нет записей в выбранных категориях? (показать EmptyState)

### Тестирование
- [ ] Unit тесты для логики фильтрации
- [ ] Ручная проверка UI
- [ ] Проверка производительности (1000+ записей)
```

## Интеграция с другими workflows

Этот workflow работает вместе с:

- **Feature Development** - добавляет планирование к разработке фич
- **Code Review** - план помогает в review
- **Bug Fixing** - план помогает систематически исправлять баги

## Запись уроков

После выполнения задачи, если были исправления или важные находки:

1. Запиши урок в `.cursor/rules/lessons-learned.mdc`
2. Обнови правила проекта, если нужно
3. Используй накопленные уроки в будущих планах
