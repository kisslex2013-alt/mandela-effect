# Тестирование "Паспорта Реальности"

## Быстрый способ: Скрипт для консоли браузера

### Вариант 1: Автоматический (рекомендуется)

1. Открой консоль браузера (F12) на странице `localhost:3000`
2. Скопируй и вставь этот код:

```javascript
// Генерация тестовых голосов для Identity
(async function() {
  console.log('🧪 Генерация тестовых голосов...');
  
  // Получаем ID эффектов из DOM (если открыт каталог) или используем заглушки
  let effectIds = [];
  
  // Пробуем получить из DOM
  const links = document.querySelectorAll('a[href^="/effect/"]');
  if (links.length > 0) {
    effectIds = Array.from(links)
      .map(link => link.getAttribute('href')?.replace('/effect/', ''))
      .filter(Boolean)
      .slice(0, 15);
    console.log(`✅ Найдено ${effectIds.length} ID из DOM`);
  }
  
  // Если не нашли, используем заглушки (ЗАМЕНИ НА РЕАЛЬНЫЕ ID!)
  if (effectIds.length < 10) {
    console.warn('⚠️ Используются заглушки. Замени на реальные ID!');
    effectIds = [
      // ВСТАВЬ СЮДА РЕАЛЬНЫЕ ID ЭФФЕКТОВ ИЗ БАЗЫ
      // Например: 'cmik338g2000yxriw45osi015'
    ];
  }
  
  if (effectIds.length < 10) {
    console.error('❌ Недостаточно ID! Открой /catalog и запусти скрипт снова');
    return;
  }
  
  // Очищаем старые голоса (опционально)
  const clearOld = confirm('Очистить старые голоса?');
  if (clearOld) {
    for (let i = 0; i < localStorage.length; i++) {
      const key = localStorage.key(i);
      if (key?.startsWith('voted_effect_')) {
        localStorage.removeItem(key);
      }
    }
  }
  
  // Генерируем 15 голосов: 40% A (Mandela), 60% B (Reality)
  // Это даст примерно 60% синхронизации
  let aCount = 0, bCount = 0;
  
  for (let i = 0; i < Math.min(15, effectIds.length); i++) {
    const effectId = effectIds[i];
    const variant = i < 6 ? 'A' : (Math.random() < 0.3 ? 'A' : 'B');
    
    if (variant === 'A') aCount++;
    else bCount++;
    
    const key = `voted_effect_${effectId}`;
    const data = {
      variant,
      timestamp: Date.now() - (15 - i) * 60000,
      effectTitle: `Тест ${i + 1}`
    };
    localStorage.setItem(key, JSON.stringify(data));
  }
  
  const syncRate = Math.round((bCount / (aCount + bCount)) * 100);
  console.log('✅ Готово!');
  console.log(`📊 A: ${aCount}, B: ${bCount}, Синхронизация: ${syncRate}%`);
  console.log('🎯 Перейди на /my-memory');
})();
```

### Вариант 2: Ручной (самый надежный)

1. Открой страницу `/catalog`
2. Прокликай 10-15 карточек эффектов
3. **Важно:** Выбирай РАЗНЫЕ варианты (A и B), чтобы получить смешанный результат
4. Перейди на `/my-memory`
5. Нажми "Синхронизировать Личность"

### Вариант 3: Использование готового скрипта

1. Открой файл `scripts/test-identity-votes.js`
2. Скопируй содержимое
3. Вставь в консоль браузера на `localhost:3000`
4. Скрипт автоматически найдет ID эффектов и создаст тестовые голоса

## Проверка результата

После генерации голосов:

1. Перейди на `/my-memory`
2. Должна появиться кнопка "Синхронизировать Личность" (если >= 10 голосов)
3. Нажми кнопку и дождись генерации
4. Проверь:
   - Отображается ли архетип с глитч-эффектом
   - Правильный ли процент синхронизации
   - Работает ли радарная диаграмма
   - Отображается ли QR-код

## Сброс данных

Если нужно начать заново:

1. В интерфейсе `/my-memory` нажми "[СБРОСИТЬ ID]"
2. Или в консоли: `localStorage.removeItem('identity_id')`
3. Для очистки всех голосов:
   ```javascript
   for (let i = 0; i < localStorage.length; i++) {
     const key = localStorage.key(i);
     if (key?.startsWith('voted_effect_')) {
       localStorage.removeItem(key);
     }
   }
   ```

