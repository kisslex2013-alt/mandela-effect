# 🔍 Как проверить комментарии в базе данных

Этот документ описывает несколько способов проверки комментариев и их ссылок (imageUrl, videoUrl, audioUrl) в базе данных.

---

## 📋 Способ 1: Prisma Studio (Визуальный интерфейс) - РЕКОМЕНДУЕТСЯ

**Самый простой и наглядный способ для визуального просмотра данных.**

### Шаги:

1. **Откройте терминал в корне проекта**
   ```bash
   cd "H:\Backup\Zero-Coding\Cursor AI\Project_002"
   ```

2. **Запустите Prisma Studio**
   ```bash
   npm run db:studio
   ```
   
   Или напрямую:
   ```bash
   npx prisma studio
   ```

3. **Prisma Studio откроется в браузере** (обычно по адресу `http://localhost:5555`)

4. **В левом меню выберите модель `Comment`**

5. **Просмотрите комментарии:**
   - Вы увидите список всех комментариев
   - Нажмите на любой комментарий для детального просмотра
   - Проверьте поля `imageUrl`, `videoUrl`, `audioUrl`

6. **Фильтрация комментариев на модерации:**
   - Вверху справа нажмите "Add filter"
   - Выберите поле `status`
   - Выберите значение `PENDING`
   - Нажмите "Apply"

7. **Фильтрация комментариев со ссылками:**
   - Добавьте фильтр: `imageUrl` `is not null` ИЛИ `videoUrl` `is not null` ИЛИ `audioUrl` `is not null`

---

## 📋 Способ 2: Скрипт для проверки (Автоматический отчет)

**Быстрый способ получить отчет о всех комментариях с их ссылками.**

### Шаги:

1. **Откройте терминал в корне проекта**
   ```bash
   cd "H:\Backup\Zero-Coding\Cursor AI\Project_002"
   ```

2. **Запустите скрипт проверки**
   ```bash
   npm run db:check-comments
   ```
   
   Или напрямую:
   ```bash
   npx ts-node --compiler-options '{"module":"CommonJS"}' scripts/check-comments.ts
   ```

3. **Просмотрите отчет:**
   - Увидите общую статистику комментариев
   - Список комментариев на модерации (PENDING)
   - Комментарии со ссылками (imageUrl, videoUrl, audioUrl)
   - Примеры комментариев с заполненными полями

---

## 📋 Способ 3: SQL запросы напрямую (для Supabase)

**Если у вас есть доступ к Supabase Dashboard или PostgreSQL клиенту.**

### Шаги:

1. **Откройте Supabase Dashboard:**
   - Перейдите на [supabase.com](https://supabase.com)
   - Войдите в свой проект
   - Перейдите в раздел "SQL Editor"

2. **Выполните запрос для просмотра всех комментариев:**
   ```sql
   SELECT 
     id,
     "type",
     text,
     "imageUrl",
     "videoUrl",
     "audioUrl",
     "theoryType",
     status,
     "createdAt"
   FROM "Comment"
   ORDER BY "createdAt" DESC;
   ```

3. **Запрос для комментариев на модерации:**
   ```sql
   SELECT 
     id,
     "type",
     text,
     "imageUrl",
     "videoUrl",
     "audioUrl",
     status
   FROM "Comment"
   WHERE status = 'PENDING'
   ORDER BY "createdAt" DESC;
   ```

4. **Запрос для комментариев со ссылками:**
   ```sql
   SELECT 
     id,
     "type",
     text,
     "imageUrl",
     "videoUrl",
     "audioUrl",
     status
   FROM "Comment"
   WHERE "imageUrl" IS NOT NULL 
      OR "videoUrl" IS NOT NULL 
      OR "audioUrl" IS NOT NULL
   ORDER BY "createdAt" DESC;
   ```

5. **Запрос для проверки конкретных полей:**
   ```sql
   -- Подсчет комментариев с заполненными полями
   SELECT 
     COUNT(*) as total,
     COUNT("imageUrl") as with_image,
     COUNT("videoUrl") as with_video,
     COUNT("audioUrl") as with_audio
   FROM "Comment"
   WHERE status = 'PENDING';
   ```

---

## 🔧 Что делать, если ссылки не отображаются?

### 1. Проверьте данные в БД
   - Используйте один из способов выше
   - Убедитесь, что поля `imageUrl`, `videoUrl`, `audioUrl` действительно заполнены

### 2. Если поля пустые (NULL):
   - **Проблема:** Ссылки не сохраняются при отправке комментария
   - **Решение:** 
     - Проверьте логику в `components/EffectPageClient.tsx` (AddCommentModal)
     - Убедитесь, что форма правильно определяет тип медиа по URL
     - Проверьте валидацию в `app/actions/comments.ts`

### 3. Если поля заполнены, но не отображаются:
   - **Проблема:** Данные не загружаются или не передаются в компонент
   - **Решение:**
     - Проверьте `app/admin/page.tsx` - убедитесь, что поля включены в `select`
     - Проверьте консоль браузера на наличие ошибок
     - Проверьте, что интерфейс `Comment` в `AdminClient.tsx` включает все поля

### 4. Очистка кеша
   - Перезапустите dev сервер: `npm run dev`
   - Очистите кеш браузера (Ctrl+Shift+R или Ctrl+F5)

---

## 📝 Пример ожидаемого результата

После выполнения скрипта или SQL запроса вы должны увидеть:

```
📊 Всего комментариев: 3

📷 С изображениями: 1
🎥 С видео: 0
🎵 С аудио: 0
❌ Без медиа: 2
⏳ На модерации (PENDING): 3

📋 КОММЕНТАРИИ НА МОДЕРАЦИИ (PENDING):

1. [PENDING] WITNESS - "Пикачу"
   Текст: тестовый комментарий со ссылкой на картинку
   ID: cmxxxxx...
   Создан: 12.12.2025, 15:30:00
   ✅ imageUrl: https://example.com/image.jpg
   ❌ videoUrl: null
   ❌ audioUrl: null
```

---

## 🚀 Быстрая команда для проверки

Самый быстрый способ - запустить скрипт:

```bash
npm run db:check-comments
```

Это покажет вам полный отчет о состоянии комментариев в базе данных.

