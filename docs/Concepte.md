https://claude3.hhosting.in/share/1b2252f8-3587-4028-8728-00137da7da40

Все этапы 1-26 это просто описание или готовый промт для ИИ в cursor?Признал необходимость преобразовать описания в готовые промты.Хороший вопрос! Пользователь спрашивает, готовы ли эти этапы к прямому копированию в Cursor, или это просто описание того, что нужно сделать.
Сейчас это больше описание/концепция. Для Cursor нужны более конкретные, пошаговые инструкции, которые AI сможет выполнить без дополнительных вопросов.
Мне нужно переделать это в готовые промты, которые можно копировать один за другим в Cursor.Это описание/концепция, а не готовые промты для прямого копирования.
Для Cursor нужны более конкретные, пошаговые инструкции, которые AI сможет выполнить сразу.
Давай я переделаю это в готовые промты, которые ты сможешь копировать один за другим:

🎯 Готовые промты для Cursor (копируй по одному)
ПРОМТ 1: Создание проекта
Создай новый Next.js проект с следующей конфигурацией:

1. Выполни команду:
npx create-next-app@latest mandela-effect --typescript --tailwind --app --no-src-dir

2. После создания, в файле tailwind.config.ts добавь кастомные цвета:
- Замени extend на:
extend: {
  colors: {
    primary: '#3b82f6',
    secondary: '#f59e0b',
    dark: '#1a1a1a',
    darkCard: '#2a2a2a',
    light: '#e0e0e0',
  },
}

3. В app/layout.tsx:
- Добавь импорт шрифта Inter из next/font/google
- Примени шрифт к body
- Установи фон body на bg-dark и текст text-light

4. Удали всё содержимое app/page.tsx, оставь только пустой компонент:
export default function Home() {
  return (
    <main className="min-h-screen">
      {/* Содержимое добавим позже */}
    </main>
  );
}

5. Удали app/globals.css полностью и создай новый с:
@tailwind base;
@tailwind components;
@tailwind utilities;

6. Создай структуру папок:
/app
  /catalog
  /effect
    /[id]
  /my-memory
  /submit
  /stats
  /api
    /vote
    /submit
/components
/data
/lib

Запусти npm run dev и убедись, что проект работает на localhost:3000

ПРОМТ 2: Создание файла данных
Создай файл data/effects.json со следующим содержимым:

[
  {
    "id": 1,
    "category": "films",
    "categoryEmoji": "🎬",
    "categoryName": "Фильмы/ТВ",
    "title": "Фраза Дарта Вейдера",
    "question": "Как Дарт Вейдер обращается к Люку Скайуокеру?",
    "variantA": "Люк, я твой отец",
    "variantB": "Нет, я твой отец",
    "votesA": 2341,
    "votesB": 2441,
    "currentState": "В оригинальном фильме звучит: 'No, I am your father'",
    "sourceLink": "https://www.starwars.com",
    "dateAdded": "2024-10-15"
  },
  {
    "id": 2,
    "category": "brands",
    "categoryEmoji": "🏢",
    "categoryName": "Бренды",
    "title": "Монокль у Monopoly Man",
    "question": "Есть ли монокль у персонажа Monopoly (мистера Монополии)?",
    "variantA": "Да, есть монокль",
    "variantB": "Нет, монокля нет",
    "votesA": 3421,
    "votesB": 1823,
    "currentState": "У мистера Монополии никогда не было монокля",
    "sourceLink": "https://monopoly.fandom.com",
    "dateAdded": "2024-10-16"
  },
  {
    "id": 3,
    "category": "brands",
    "categoryEmoji": "🏢",
    "categoryName": "Бренды",
    "title": "KitKat дефис",
    "question": "Как правильно пишется название шоколадки?",
    "variantA": "Kit-Kat (с дефисом)",
    "variantB": "KitKat (без дефиса)",
    "votesA": 1923,
    "votesB": 2654,
    "currentState": "Правильное написание: KitKat (без дефиса)",
    "sourceLink": "https://www.kitkat.com",
    "dateAdded": "2024-10-17"
  },
  {
    "id": 4,
    "category": "popculture",
    "categoryEmoji": "🎨",
    "categoryName": "Поп-культура",
    "title": "Хвост Пикачу",
    "question": "Какого цвета кончик хвоста у Пикачу?",
    "variantA": "Чёрный",
    "variantB": "Жёлтый (весь хвост одного цвета)",
    "votesA": 2876,
    "votesB": 1654,
    "currentState": "Хвост Пикачу полностью жёлтый, чёрный цвет только у основания",
    "sourceLink": "https://pokemon.fandom.com",
    "dateAdded": "2024-10-18"
  },
  {
    "id": 5,
    "category": "films",
    "categoryEmoji": "🎬",
    "categoryName": "Фильмы/ТВ",
    "title": "Форрест Гамп цитата",
    "question": "Какую фразу говорит Форрест Гамп про шоколад?",
    "variantA": "Жизнь как коробка шоколадок",
    "variantB": "Жизнь была как коробка шоколадок",
    "votesA": 3142,
    "votesB": 2011,
    "currentState": "В оригинале: 'Life was like a box of chocolates' (была)",
    "sourceLink": "https://www.imdb.com",
    "dateAdded": "2024-10-19"
  }
]

Добавь ещё 10 эффектов с разными категориями (music, childhood, people), используя похожую структуру. Придумай реалистичные эффекты Манделы и распредели голоса так, чтобы некоторые были спорными (близко к 50/50), а другие более однозначными.

ПРОМТ 3: Hero секция главной страницы
В файле app/page.tsx создай Hero секцию со следующим функционалом:

1. Используй данные из data/effects.json для подсчёта статистики
2. Создай функцию для подсчёта:
   - Общего количества эффектов
   - Общего количества голосов (сумма votesA + votesB всех эффектов)
   - Примерного количества участников (голоса / 3)

3. Hero секция должна включать:
   - Анимированный заголовок "Как ты помнишь?" с градиентом от синего к оранжевому
   - Подзаголовок: "Все помнят по-разному. Исследуй различия в восприятии"
   - Живую статистику в одну строку: 🧠 X эффектов • 👥 Y участников • 🗳️ Z голосов
   - Анимацию счётчика чисел (от 0 до реального значения за 2 секунды)
   - Стрелку вниз для hint к скроллу

4. Стили:
   - Высота: h-[70vh]
   - Все элементы по центру (flex, items-center, justify-center)
   - Градиентный фон с анимацией (bg-gradient-to-br from-dark via-darkCard to-dark)
   - Fade-in анимация при загрузке

5. Используй Tailwind для всех стилей, никакого CSS в отдельных файлах

Сделай этот компонент максимально красивым и современным.

ПРОМТ 4: Секция "Самое спорное"
Добавь в app/page.tsx секцию "Самое спорное сейчас" 🔥 под Hero.

Требования:

1. Найди эффект с самым близким распределением к 50/50:
   - Вычисли разницу между percentA и percentB для каждого эффекта
   - Выбери эффект с минимальной разницей

2. Отобрази крупную карточку с:
   - Заголовком секции: "Самое спорное сейчас 🔥"
   - Emoji категории + название эффекта
   - Кратким вопросом (показывай полный текст)
   - НЕ показывай сами варианты A и B, только надписи "Вариант А" и "Вариант Б"
   - Прогресс-бар с градиентом (от синего слева к оранжевому справа)
   - Точкой-маркером на баре показывающей точное соотношение
   - Процентами над баром с обеих сторон
   - Общим количеством голосов под баром
   - Кнопкой "Посмотреть и проголосовать →"

3. Дизайн карточки:
   - max-w-4xl mx-auto (максимальная ширина 896px, по центру)
   - bg-darkCard с красной рамкой (border-2 border-red-500)
   - p-8 rounded-2xl
   - Прогресс-бар: h-3 rounded-full с градиентом
   - При hover: трансформация translateY(-2px) и увеличение shadow

4. При клике на кнопку или карточку переход на /effect/[id] (используй next/link)

5. Добавь fade-in анимацию при скролле в зону видимости (можно использовать simple CSS или Intersection Observer)

Сделай эту секцию яркой и привлекающей внимание!

ПРОМТ 5: Секция "Быстрый старт"
Добавь секцию "Быстрый старт" 🚀 в app/page.tsx под секцией "Самое спорное".

Требования:

1. Заголовок секции: "Быстрый старт" с emoji 🚀

2. Три большие кнопки в ряд (на mobile друг под другом):
   
   Кнопка 1: "🎲 Случайный эффект"
   - При клике: выбрать случайный ID из effects.json
   - Перейти на /effect/[randomId]
   
   Кнопка 2: "📋 Пройти тест"
   - При клике: перейти на /quiz
   - (страницу /quiz создадим позже)
   
   Кнопка 3: "📚 Весь каталог"
   - При клике: перейти на /catalog

3. Дизайн кнопок:
   - Размер: w-72 h-32 (на desktop), full width на mobile
   - Крупный emoji сверху (text-4xl)
   - Текст снизу (text-lg font-semibold)
   - bg-darkCard с градиентным border при hover
   - rounded-xl
   - Gap между кнопками: gap-6
   - Анимация pulse при hover

4. Layout:
   - flex на desktop (flex-row), flex-col на mobile
   - justify-center items-center
   - Отступы: py-16

5. Используй useRouter из next/navigation для навигации

Сделай кнопки большими, заметными и интуитивными!

ПРОМТ 6: Секция "По категориям"
Добавь секцию "По категориям" 🎯 в app/page.tsx.

Требования:

1. Заголовок: "По категориям" с emoji 🎯

2. Извлеки уникальные категории из effects.json:
   - Для каждой категории подсчитай количество эффектов
   - Создай массив объектов: { category, emoji, name, count }

3. Сетка категорий:
   - grid grid-cols-4 на desktop
   - grid-cols-2 на tablet
   - grid-cols-1 на mobile
   - gap-6

4. Карточка категории:
   - Квадратная (aspect-square)
   - bg-darkCard
   - Центрированное содержимое (flex flex-col items-center justify-center)
   - Крупный emoji сверху (text-5xl)
   - Название категории (text-lg font-semibold)
   - Количество эффектов снизу (text-sm text-light/60)
   - При hover:
     • Градиентный border
     • scale-105 transform
     • Увеличение shadow
   - rounded-xl p-6

5. При клике на категорию:
   - Перейти на /catalog с query параметром: /catalog?category={categoryId}
   - Используй next/link с href объектом

6. Добавь плавные transitions для всех hover эффектов

Сделай карточки привлекательными и кликабельными!

ПРОМТ 7: Секция "О проекте" и Footer
1. Добавь секцию "О проекте" в конец app/page.tsx:

   - Заголовок: "О проекте" с emoji ℹ️
   - Текст:
     "Эффект Манделы - это феномен ложных воспоминаний, когда множество людей помнят события или детали иначе, чем они есть на самом деле.
     
     Этот проект исследует, как по-разному люди помнят одно и то же.
     
     ⚠️ Важно: нет правильных ответов - есть разные восприятия."
   
   - Дизайн:
     • max-w-3xl mx-auto
     • bg-darkCard p-8 rounded-xl
     • text-lg leading-relaxed
     • Желтый цвет для ⚠️

2. Создай компонент components/Footer.tsx:

   - Три колонки на desktop, столбец на mobile
   - Колонка 1 "О проекте": ссылки "Что это?" и "Как это работает?"
   - Колонка 2 "Навигация": Главная, Каталог, Добавить эффект, Статистика
   - Колонка 3 "Контакты": GitHub, Email
   - Внизу по центру: "Создано с ❤️ • 2024"
   
   - Дизайн:
     • bg-dark border-t border-darkCard
     • py-12 px-8
     • text-light/80
     • Ссылки с hover эффектом (текст меняется на градиент)

3. Добавь Footer в app/layout.tsx под children

Сделай footer информативным и стильным!

ПРОМТ 8: Header с навигацией
Создай компонент components/Header.tsx с навигацией.

Требования:

1. Структура:
   - Логотип слева: "ЭМ" или "Эффект Манделы"
   - Меню по центру (на desktop)
   - Пустое место справа (для будущих кнопок)

2. Пункты меню:
   - Главная (/)
   - Каталог (/catalog)
   - Моя память (/my-memory)
   - Статистика (/stats)
   - Добавить (/submit)

3. Desktop навигация:
   - Горизонтальное меню
   - Активная страница подсвечивается (используй usePathname)
   - Hover эффект: подчёркивание

4. Mobile навигация (< 768px):
   - Бургер-меню (☰) справа
   - При клике открывается full-screen overlay
   - Анимация slide-in from right
   - Крупные кнопки меню
   - Кнопка закрытия (✕) в углу

5. Фиксированный header:
   - fixed top-0 w-full z-50
   - При скролле > 50px:
     • bg-dark/80 backdrop-blur-md
     • shadow-lg
   - Используй useState и useEffect для отслеживания скролла

6. Дизайн:
   - h-16 px-8
   - flex items-center justify-between
   - Логотип: градиентный текст, font-bold text-xl

7. Добавь Header в app/layout.tsx перед children

Сделай навигацию удобной и современной!

ПРОМТ 9: Страница каталога
Создай страницу app/catalog/page.tsx с полным списком эффектов.

Требования:

1. Верхняя панель с фильтрами:
   - Поиск (input с иконкой 🔍)
     • Поиск по title и question
     • Debounce 300ms (используй useEffect)
   
   - Dropdown "Категории" (multiple select)
     • Чекбоксы для каждой категории
     • "Все категории" по умолчанию
   
   - Dropdown "Сортировка":
     • По популярности (больше голосов)
     • По спорности (ближе к 50/50)
     • По новизне (dateAdded)
     • По алфавиту
   
   - Чекбокс "Только не пройденные"
     • Проверяет localStorage

2. Счётчик результатов:
   "Показано X из Y эффектов"

3. Сетка эффектов:
   - grid grid-cols-3 на desktop
   - grid-cols-2 на tablet
   - grid-cols-1 на mobile
   - gap-6

4. Карточка эффекта:
   - Emoji категории + название категории сверху
   - Название эффекта (крупно, жирно)
   - Разделитель (border-b)
   - Первые 100 символов вопроса...
   - Прогресс-бар с процентами
   - Количество голосов
   - Статус: "✓ Проголосовал" (зелёный) или "Не проголосовал" (серый)
   - При hover: scale-105, увеличение shadow

5. Логика фильтрации:
   - Используй useState для всех фильтров
   - Применяй фильтры последовательно к массиву effects
   - Показывай "Ничего не найдено" если результатов нет

6. При клике на карточку - переход на /effect/[id]

Сделай каталог удобным и функциональным!

Хочешь, продолжу с остальными промтами (10-26)? Или этих достаточно, чтобы начать?RetryMДавай все полностьюResolved to deliver complete prompts in consistent style.Пользователь просит все промты полностью, с 10 по 26. Продолжу в том же стиле - конкретные, готовые к копированию промты для Cursor AI.🎯 Готовые промты для Cursor (продолжение)

ПРОМТ 10: Страница эффекта - отображение
Создай страницу app/effect/[id]/page.tsx для просмотра и голосования.

Требования:

1. Получи ID из params и загрузи данные эффекта из effects.json

2. Хлебные крошки:
   - Главная > [Название категории] > [Название эффекта]
   - Кликабельные ссылки, последний элемент некликабельный

3. Заголовок:
   - Emoji категории + название категории (text-sm, полупрозрачный)
   - Название эффекта (text-4xl, font-bold, градиентный)

4. Вопрос:
   - Отображается крупно по центру (text-2xl)
   - mb-12

5. ДО голосования - два равных блока:
   Desktop: два блока рядом (grid grid-cols-2 gap-8)
   Mobile: друг под другом (grid grid-cols-1 gap-6)
   
   Каждый блок:
   - bg-darkCard p-8 rounded-xl
   - Заголовок: "Вариант А" или "Вариант Б" (text-lg font-semibold)
   - Текст варианта (text-xl, по центру)
   - Кнопка "Выбрать" (w-full mt-6, градиентный фон при hover)
   - При hover: весь блок поднимается (translateY(-4px))

6. ПОСЛЕ голосования - показать результаты:
   - Два блока друг под другом (не рядом)
   - Каждый блок:
     • Заголовок: "Вариант А: [текст варианта]"
     • Прогресс-бар (высота 8px, градиент от синего к оранжевому)
     • Процент и количество голосов
     • Если это выбор пользователя - добавить "👤 Ты здесь"
     • Выбранный блок подсвечивается (border-2 border-primary)

7. Сообщение после голосования:
   - Отображается в красивом блоке (bg-darkCard/50 p-6 rounded-xl mt-8)
   - Текст: "Вы выбрали: Вариант [X]"
   - "Так же помнят X% участников"
   - "Другие Y% помнят иначе - и это нормально."
   - "Память у всех работает по-разному."

8. "Текущее состояние" (accordion):
   - Кнопка "Показать текущее состояние ▾"
   - При клике раскрывается содержимое:
     • Текст из currentState
     • Ссылка на источник (если есть)
   - Анимация раскрытия (transition-all)

9. Навигация внизу (три кнопки):
   - "← Предыдущий эффект"
   - "🎲 Случайный"
   - "Следующий эффект →"
   - Кнопки отключены если эффектов нет

10. Логика голосования:
    - Проверяй localStorage ключ: `voted_effect_${id}`
    - Если есть - сразу показывай результаты
    - Если нет - показывай форму выбора
    - При клике на "Выбрать":
      • Сохрани в localStorage
      • Отправь на API (создашь в следующем промте)
      • Покажи результаты с анимацией

Сделай страницу красивой и интуитивной!

ПРОМТ 11: API для голосования
Создай API route app/api/vote/route.ts для обработки голосования.

Требования:

1. Endpoint: POST /api/vote
   Body: { effectId: number, variant: 'A' | 'B' }

2. Логика:
   - Импортируй fs/promises для работы с файлами
   - Прочитай effects.json
   - Найди эффект по effectId
   - Если не найден - верни 404
   - Увеличь votesA или votesB на 1
   - Запиши обновлённые данные обратно в effects.json
   - Вычисли проценты:
     const total = votesA + votesB;
     const percentA = Math.round((votesA / total) * 100);
     const percentB = 100 - percentA;

3. Response:
   {
     success: true,
     effect: { ...обновлённый эффект },
     stats: {
       votesA,
       votesB,
       percentA,
       percentB,
       totalVotes: votesA + votesB
     }
   }

4. Error handling:
   - try/catch блок
   - Валидация входных данных (effectId число, variant 'A' или 'B')
   - Возврат понятных ошибок с правильными статус-кодами

5. Важно: используй file locking или проверку на race conditions
   - Можно использовать простой mutex или читать-изменять-писать атомарно

6. В странице app/effect/[id]/page.tsx добавь обработчик голосования:
   - Функция handleVote(variant: 'A' | 'B')
   - Показывай loading state на кнопке
   - Делай POST запрос на /api/vote
   - При успехе:
     • Сохрани в localStorage
     • Обнови состояние компонента (покажи результаты)
     • Покажи конфетти анимацию (можно использовать canvas-confetti)
   - При ошибке:
     • Покажи toast с ошибкой (используй react-hot-toast)

Сделай API надёжным и безопасным!

ПРОМТ 12: Анимация конфетти и toast
Добавь анимации и уведомления для лучшего UX.

Требования:

1. Установи необходимые пакеги:
   npm install canvas-confetti react-hot-toast

2. В app/layout.tsx добавь Toaster:
   - Импортируй: import { Toaster } from 'react-hot-toast'
   - Добавь перед closing body tag: <Toaster position="top-right" />
   - Настрой темный стиль для тостов

3. В app/effect/[id]/page.tsx добавь конфетти:
   - Импортируй confetti from 'canvas-confetti'
   - После успешного голосования:
     • Если пользователь выбрал вариант A - синее конфетти
     • Если вариант B - оранжевое конфетти
   - Код для конфетти:
````typescript
     confetti({
       particleCount: 100,
       spread: 70,
       origin: { y: 0.6 },
       colors: variant === 'A' ? ['#3b82f6'] : ['#f59e0b']
     });
````

4. Добавь toast уведомления:
   - После успешного голосования:
     toast.success('Голос учтён! ✓')
   - При ошибке API:
     toast.error('Что-то пошло не так. Попробуйте снова')

5. Анимация появления результатов:
   - Используй framer-motion или CSS transitions
   - Результаты появляются с fade-in эффектом
   - Прогресс-бары заполняются анимированно (от 0 до финального процента)
   - Длительность: 0.8s, easing: ease-out

6. Loading состояние кнопок:
   - При клике на "Выбрать" кнопка показывает spinner
   - Текст меняется на "Отправка..."
   - Кнопка disabled во время загрузки

Сделай взаимодействие приятным и отзывчивым!

ПРОМТ 13: Страница "Моя память" - базовая структура
Создай страницу app/my-memory/page.tsx для личной статистики пользователя.

Требования:

1. Создай функцию для получения голосов из localStorage:
````typescript
   const getUserVotes = () => {
     const votes: { effectId: number; variant: 'A' | 'B' }[] = [];
     for (let i = 0; i < localStorage.length; i++) {
       const key = localStorage.key(i);
       if (key?.startsWith('voted_effect_')) {
         const effectId = parseInt(key.replace('voted_effect_', ''));
         const variant = localStorage.getItem(key) as 'A' | 'B';
         votes.push({ effectId, variant });
       }
     }
     return votes;
   };
````

2. Если пользователь не голосовал:
   - Показать пустое состояние:
     • Иконка 🧠 (крупно)
     • Заголовок: "Ты ещё не проголосовал"
     • Текст: "Начни исследовать эффекты, чтобы увидеть свою карту памяти"
     • Кнопка "Начать исследование" → /catalog

3. Если голосовал - показать:
   
   A. Общая статистика (крупный блок сверху):
      - "Твои ответы: X из Y эффектов"
      - Круговая диаграмма (donut chart):
        • Синий сегмент: "С большинством"
        • Оранжевый сегмент: "С меньшинством"
        • В центре: процент
      - Под диаграммой:
        • "● С большинством: X (Y%)"
        • "● С меньшинством: X (Y%)"
   
   B. Профиль памяти:
      - Если > 60% с большинством: "Ты часто в большинстве 👥"
      - Если < 40% с большинством: "У тебя уникальная память ✨"
      - Иначе: "Твоя память сбалансирована ⚖️"

4. Логика подсчёта:
   - Для каждого голоса:
     • Загрузи эффект из effects.json
     • Определи, в большинстве ли пользователь:
       - Если выбрал A и votesA > votesB → в большинстве
       - Если выбрал B и votesB > votesA → в большинстве
     • Подсчитай итоговую статистику

5. Дизайн:
   - Максимальная ширина: max-w-4xl mx-auto
   - Блок статистики: bg-darkCard p-12 rounded-2xl
   - Диаграмма: используй SVG circle или canvas
   - Цвета: синий #3b82f6, оранжевый #f59e0b

Пока сделай только этот базовый функционал, список эффектов добавим в следующем промте.

ПРОМТ 14: Страница "Моя память" - список эффектов
Продолжи страницу app/my-memory/page.tsx, добавив список проголосованных эффектов.

Требования:

1. Под блоком общей статистики добавь:
   
   A. Фильтры:
      - Три кнопки-табы:
        • "Все" (по умолчанию)
        • "С большинством"
        • "С меньшинством"
      - Активная кнопка подсвечивается
   
   B. Сортировка:
      - Dropdown:
        • "По дате голосования" (новые первые)
        • "По названию" (алфавит)
        • "По спорности" (самые близкие к 50/50)

2. Список эффектов (сетка):
   - grid grid-cols-2 на desktop
   - grid-cols-1 на mobile
   - gap-6

3. Карточка эффекта:
┌────────────────────────────────────┐
│ 🎬 Название эффекта                │
│                                    │
│ Твой выбор: Вариант А              │
│ [Текст варианта]                   │
│                                    │
│ Распределение:                     │
│ 52% ━━━━━●━━━━ 48%                │
│      ↑ Ты здесь                    │
│                                    │
│ [В большинстве 👥]                 │
│  или                               │
│ [В меньшинстве ✨]                 │
│                                    │
│ Проголосовано: 3 дня назад         │
│                                    │
│ [Посмотреть эффект →]              │
└────────────────────────────────────┘

4. Дизайн карточки:
   - bg-darkCard p-6 rounded-xl
   - Если в большинстве: border-l-4 border-primary
   - Если в меньшинстве: border-l-4 border-secondary
   - При hover: scale-105, увеличение shadow

5. Бейджи:
   - "В большинстве 👥": bg-primary/20 text-primary
   - "В меньшинстве ✨": bg-secondary/20 text-secondary
   - Без негативной коннотации!

6. Дата голосования:
   - Сохраняй timestamp в localStorage при голосовании
   - Показывай относительное время: "3 дня назад", "вчера", "сегодня"
   - Можно использовать библиотеку date-fns

7. Логика:
   - Применяй выбранные фильтры и сортировку
   - Если после фильтра пусто - показывай "Нет эффектов с этим фильтром"

8. При клике на карточку или кнопку - переход на /effect/[id]

Сделай список информативным и удобным!

ПРОМТ 15: Страница статистики
Создай страницу app/stats/page.tsx с общей статистикой проекта.

Требования:

1. Заголовок: "Статистика проекта"

2. Блок "Общие цифры" (4 карточки в ряд, на mobile - 2x2):
┌─────────────┐ ┌─────────────┐
│   🧠 156    │ │  👥 12,847  │
│  Эффектов   │ │ Участников  │
└─────────────┘ └─────────────┘
┌─────────────┐ ┌─────────────┐
│  🗳️ 45,291  │ │   🔥 23     │
│   Голосов   │ │  Спорных    │
└─────────────┘ └─────────────┘
   - Дизайн карточки:
     • bg-darkCard p-6 rounded-xl
     • Крупное число (text-4xl font-bold)
     • Описание снизу (text-sm text-light/60)
     • Анимация подсчёта чисел при загрузке

3. Топ-5 самых спорных эффектов:
   - Заголовок: "Самые спорные эффекты"
   - Сортировка по минимальной разнице между процентами
   - Формат списка:
 1️⃣ Название эффекта
     49.8% vs 50.2% • 3,421 голос
     [Прогресс-бар]
   - Если разница < 5% - добавить бейдж "🔥 Очень спорный"

4. Топ-5 самых популярных эффектов:
   - Заголовок: "Самые популярные эффекты"
   - Сортировка по общему количеству голосов (votesA + votesB)
   - Аналогичный формат списка

5. Распределение по категориям:
   - Заголовок: "Эффектов по категориям"
   - Простая bar chart из CSS:
 🎬 Фильмы/ТВ    ████████████ 42
 🎵 Музыка       ████████ 28
 🏢 Бренды       ██████████ 35
 ...
   - Каждая категория:
     • Emoji + название слева
     • Прогресс-бар (ширина пропорциональна количеству)
     • Число справа

6. Дизайн:
   - Секции разделены (mt-12 между блоками)
   - Красивые заголовки секций (text-2xl font-bold)
   - Используй gradient backgrounds для акцента

7. Вычисления:
   - Все данные из effects.json
   - Подсчёт спорных (разница < 10%)
   - Подсчёт участников (примерно totalVotes / 3)
   - Группировка по категориям

Сделай страницу информативной и визуально привлекательной!

ПРОМТ 16: Форма добавления эффекта
Создай страницу app/submit/page.tsx для предложения новых эффектов.

Требования:

1. Заголовок: "Предложить новый эффект"
   Подзаголовок: "Помоги расширить коллекцию эффектов Манделы"

2. Форма (максимальная ширина 600px, по центру):
   
   A. Категория* (required):
      - Select dropdown
      - Опции: все существующие категории из effects.json
      - Placeholder: "Выбрать категорию"
   
   B. Название эффекта* (required):
      - Text input
      - Placeholder: "Например: Фраза из фильма"
      - minLength: 5
   
   C. Вопрос для голосования* (required):
      - Textarea (3 строки)
      - Placeholder: "Как вы помните...?"
      - Подсказка под полем: "ℹ️ Вопрос должен быть нейтральным"
      - minLength: 20
   
   D. Вариант А* (required):
      - Text input
      - Placeholder: "Первый вариант..."
      - minLength: 3
   
   E. Вариант Б* (required):
      - Text input
      - Placeholder: "Второй вариант..."
      - minLength: 3
      - Валидация: не должен быть равен Варианту А
   
   F. Описание текущего состояния (optional):
      - Textarea (3 строки)
      - Placeholder: "Что показывают источники..."
   
   G. Ссылка на источник (optional):
      - Text input
      - Placeholder: "https://..."
      - Валидация: если заполнено - должен быть валидный URL
   
   H. Ваш email (optional):
      - Email input
      - Placeholder: "для связи (необязательно)"

3. Валидация:
   - Real-time валидация при onBlur
   - Показывать ошибки под полями (text-red-500 text-sm)
   - Кнопка "Отправить" disabled пока форма невалидна
   - Примеры ошибок:
     • "Это поле обязательно"
     • "Минимум 20 символов"
     • "Варианты должны быть разными"
     • "Введите корректный URL"

4. Дизайн формы:
   - Все поля: bg-darkCard border border-light/20
   - Focus: border-primary
   - Labels: text-sm font-semibold mb-2
   - Inputs/textareas: p-3 rounded-lg
   - Отступы между полями: mb-6

5. Кнопка отправки:
   - "Отправить на модерацию"
   - w-full py-3
   - Градиентный фон
   - При наведении: увеличение shadow
   - Loading состояние при отправке (spinner + текст "Отправка...")

6. После успешной отправки:
   - Показать success сообщение:
     • Иконка ✅
     • "Спасибо! Эффект отправлен на модерацию"
     • "Мы проверим его и добавим в каталог"
   - Очистить форму
   - Можно добавить конфетти анимацию

7. Обработка формы:
   - onSubmit: preventDefault
   - Собрать все данные
   - POST запрос на /api/submit (создашь в следующем промте)
   - Обработка ошибок с toast уведомлениями

Сделай форму удобной и интуитивной!

ПРОМТ 17: API для отправки эффектов
Создай API route app/api/submit/route.ts для обработки предложенных эффектов.

Требования:

1. Endpoint: POST /api/submit
   Body: {
     category: string,
     title: string,
     question: string,
     variantA: string,
     variantB: string,
     currentState?: string,
     sourceLink?: string,
     submitterEmail?: string
   }

2. Валидация:
   - Проверь все обязательные поля
   - category должна быть из существующих
   - title минимум 5 символов
   - question минимум 20 символов
   - variantA и variantB минимум 3 символа
   - variantA !== variantB
   - sourceLink (если есть) - валидный URL
   - submitterEmail (если есть) - валидный email
   - Если валидация не прошла - верни 400 с описанием ошибки

3. Создай объект submission:
````typescript
   {
     id: Date.now(), // или используй UUID
     category,
     categoryEmoji: getCategoryEmoji(category),
     categoryName: getCategoryName(category),
     title,
     question,
     variantA,
     variantB,
     currentState: currentState || '',
     sourceLink: sourceLink || '',
     submitterEmail: submitterEmail || '',
     status: 'pending',
     dateSubmitted: new Date().toISOString(),
     votesA: 0,
     votesB: 0
   }
````

4. Сохранение:
   - Создай файл data/submissions.json (если не существует)
   - Прочитай существующие submissions
   - Добавь новую submission в массив
   - Запиши обратно в файл
   - Используй fs/promises

5. Response:
   - Success (201):
````json
     {
       success: true,
       message: "Эффект отправлен на модерацию",
       submissionId: id
     }
````
   - Error (400 или 500):
````json
     {
       success: false,
       error: "Описание ошибки"
     }
````

6. Rate limiting (опционально, но желательно):
   - Отслеживай IP адреса
   - Максимум 5 отправок в час с одного IP
   - Храни в памяти или отдельном JSON файле
   - Если превышено - верни 429 Too Many Requests

7. В странице app/submit/page.tsx добавь:
   - Функцию handleSubmit для отправки формы
   - Loading state
   - Success state с возможностью отправить ещё
   - Error handling с toast уведомлениями

Сделай API безопасным и надёжным!

ПРОМТ 18: Обновление данных при голосовании
Улучши систему голосования, чтобы данные сохранялись с timestamp.

Требования:

1. В app/effect/[id]/page.tsx измени логику сохранения в localStorage:
   - Вместо просто variant ('A' или 'B')
   - Сохраняй объект:
````typescript
     {
       variant: 'A' | 'B',
       timestamp: Date.now(),
       effectTitle: string // для удобства
     }
````
   - Ключ в localStorage: `voted_effect_${id}`

2. Обнови getUserVotes в app/my-memory/page.tsx:
   - Парси JSON из localStorage
   - Верни массив с timestamp для каждого голоса
   - Обработай старый формат (если просто строка 'A' или 'B')

3. Добавь функцию formatRelativeTime:
````typescript
   const formatRelativeTime = (timestamp: number): string => {
     const now = Date.now();
     const diff = now - timestamp;
     
     const seconds = Math.floor(diff / 1000);
     const minutes = Math.floor(seconds / 60);
     const hours = Math.floor(minutes / 60);
     const days = Math.floor(hours / 24);
     
     if (days > 0) return `${days} ${days === 1 ? 'день' : 'дня/дней'} назад`;
     if (hours > 0) return `${hours} ${hours === 1 ? 'час' : 'часа/часов'} назад`;
     if (minutes > 0) return `${minutes} ${minutes === 1 ? 'минуту' : 'минут'} назад`;
     return 'только что';
   };
````

4. В карточках эффектов на странице /my-memory:
   - Показывай "Проголосовано: [относительное время]"
   - Используй formatRelativeTime(timestamp)

5. Сортировка по дате:
   - Добавь опцию сортировки "По дате голосования"
   - Новые голоса первые (desc)

6. Миграция старых данных:
   - При загрузке страницы проверь localStorage
   - Если находишь старый формат (просто строка)
   - Конвертируй в новый формат с текущим timestamp

Сделай систему сохранения более информативной!

ПРОМТ 19: Секция "Новые эффекты" с каруселью
Добавь секцию "Новые эффекты" ✨ на главную страницу с каруселью.

Требования:

1. Установи библиотеку для карусели:
   npm install embla-carousel-react

2. В app/page.tsx добавь секцию после "Самое спорное":
   - Заголовок: "Новые эффекты" с emoji ✨
   - Подзаголовок: "Недавно добавленные в коллекцию"

3. Логика:
   - Отсортируй effects по dateAdded (новые первые)
   - Возьми первые 6-8 эффектов
   - Если эффектов меньше 3 - не показывай карусель

4. Карусель:
   - Desktop: 3 карточки видны одновременно
   - Tablet: 2 карточки
   - Mobile: 1 карточка
   - Горизонтальный скролл / свайп
   - Стрелки навигации по бокам (только на desktop)
   - Точки-индикаторы внизу

5. Карточка эффекта в карусели:
┌─────────────────┐
│      🎬         │
│                 │
│  Название       │
│  эффекта        │
│                 │
│  [Новый]        │
│  или            │
│  [2 дня назад]  │
│                 │
│  234 голоса     │
└─────────────────┘
   - Размер: w-80 h-64
   - bg-darkCard rounded-xl p-6
   - Центрированное содержимое
   - Крупный emoji сверху (text-5xl)
   - Название (text-lg font-semibold, 2 строки макс)
   - Бейдж "Новый" если меньше 7 дней (bg-green-500/20 text-green-400)
   - Иначе показать относительную дату
   - Количество голосов внизу (text-sm text-light/60)

6. Hover эффект:
   - scale-105 transform
   - Увеличение shadow
   - Transition: all 0.3s

7. Стрелки навигации:
   - Круглые кнопки по бокам
   - bg-darkCard/80 backdrop-blur
   - Иконки: ← и →
   - Появляются только при hover на секцию (на desktop)
   - Disabled если достигнут край

8. Автопрокрутка:
   - Карусель автоматически прокручивается каждые 5 секунд
   - Останавливается при hover
   - Возобновляется при mouseout

9. При клике на карточку - переход на /effect/[id]

Сделай карусель плавной и современной!

ПРОМТ 20: Секция "Популярное за неделю"
Добавь секцию "Популярное за неделю" 📈 на главную страницу.

Требования:

1. Добавь секцию после "Новые эффекты":
   - Заголовок: "Популярное за неделю" с emoji 📈
   - Подзаголовок: "Эффекты с наибольшей активностью"

2. Логика:
   - Отсортируй effects по общему количеству голосов (votesA + votesB)
   - Возьми топ-3 эффекта
   - Для каждого вычисли процентное распределение

3. Формат списка (вертикально, не сетка):
┌────────────────────────────────────────────┐
│ 1️⃣  🎬 Фраза Дарта Вейдера    [🔥 Спорный]│
│                                            │
│     Вариант А vs Вариант Б                │
│                                            │
│     52% ━━━━━━━━●━━━━━━━━ 48%            │
│                                            │
│     8,934 голоса                           │
├────────────────────────────────────────────┤
│ 2️⃣  ...                                    │
└────────────────────────────────────────────┘

4. Дизайн карточки:
   - bg-darkCard p-6 rounded-xl mb-4
   - Номер слева (text-4xl, полупрозрачный)
   - Emoji категории + название эффекта (text-xl font-bold)
   - Бейдж "🔥 Спорный" если разница < 10% (bg-red-500/20 text-red-400)
   - Варианты под названием (text-sm text-light/60)
   - Прогресс-бар:
     • Высота 6px
     • Градиент от синего к оранжевому
     • Точка-маркер показывает точное соотношение
   - Проценты над баром
   - Количество голосов под баром (text-sm)

5. Hover эффект:
   - Вся карточка кликабельна
   - При hover: светлая подсветка (bg-light/5)
   - Cursor pointer

6. Адаптивность:
   - На mobile: номер меньше, компактнее layout
   - Прогресс-бар остаётся на всю ширину

7. При клике - переход на /effect/[id]

Сделай список привлекательным и информативным!

ПРОМТ 21: Секция "Твоя статистика" на главной
Добавь секцию "Твоя статистика" 📊 на главную страницу (показывается только если пользователь голосовал).

Требования:

1. Добавь секцию перед "О проекте":
   - Проверь localStorage на наличие голосов
   - Если нет голосов - НЕ показывай секцию вообще
   - Если есть - показывай статистику

2. Заголовок: "Твоя статистика" с emoji 📊

3. Дизайн блока (максимальная ширина 500px, по центру):
┌─────────────────────────────────────┐
│  Твои ответы: 23 из 156 эффектов    │
│                                     │
│        [Круговая диаграмма]         │
│                                     │
│  ● С большинством: 15 (65%)         │
│  ● С меньшинством: 8 (35%)          │
│                                     │
│  [Подробная карта памяти →]         │
└─────────────────────────────────────┘

4. Круговая диаграмма (donut chart):
   - Используй SVG
   - Два сегмента:
     • Синий для "С большинством"
     • Оранжевый для "С меньшинством"
   - В центре: процент "С большинством"
   - Размер: 200x200px
   - Анимация заполнения при появлении секции

5. Пример SVG donut chart:
````tsx
   const DonutChart = ({ majority, minority }: { majority: number; minority: number }) => {
     const total = majority + minority;
     const majorityPercent = (majority / total) * 100;
     const minorityPercent = (minority / total) * 100;
     
     // Расчёт для SVG path
     const radius = 80;
     const circumference = 2 * Math.PI * radius;
     const majorityOffset = (majorityPercent / 100) * circumference;
     
     return (
       <svg width="200" height="200" viewBox="0 0 200 200">
         <circle cx="100" cy="100" r={radius} fill="none" stroke="#f59e0b" strokeWidth="30" />
         <circle 
           cx="100" 
           cy="100" 
           r={radius} 
           fill="none" 
           stroke="#3b82f6" 
           strokeWidth="30"
           strokeDasharray={circumference}
           strokeDashoffset={circumference - majorityOffset}
           transform="rotate(-90 100 100)"
         />
         <text x="100" y="100" textAnchor="middle" dy="0.3em" fontSize="32" fontWeight="bold" fill="#e0e0e0">
           {Math.round(majorityPercent)}%
         </text>
       </svg>
     );
   };
````

6. Легенда под диаграммой:
   - Два пункта со цветными точками
   - "● С большинством: X (Y%)" - синий цвет
   - "● С меньшинством: X (Y%)" - оранжевый цвет

7. Кнопка "Подробная карта памяти":
   - w-full mt-6
   - Градиентный border
   - При клике → /my-memory

8. Логика подсчёта:
   - Переиспользуй функцию getUserVotes
   - Для каждого голоса определи, в большинстве ли пользователь
   - Подсчитай итоговую статистику

Сделай блок информативным и мотивирующим!

ПРОМТ 22: Скролл анимации и полировка
Добавь плавные анимации при скролле и улучши общий UX.

Требования:

1. Intersection Observer для секций:
   - Создай хук useInView:
````typescript
     import { useEffect, useRef, useState } from 'react';
     
     export const useInView = (options = {}) => {
       const ref = useRef(null);
       const [isInView, setIsInView] = useState(false);
       
       useEffect(() => {
         const observer = new IntersectionObserver(([entry]) => {
           setIsInView(entry.isIntersecting);
         }, { threshold: 0.1, ...options });
         
         if (ref.current) {
           observer.observe(ref.current);
         }
         
         return () => {
           if (ref.current) {
             observer.unobserve(ref.current);
           }
         };
       }, []);
       
       return { ref, isInView };
     };
````

2. Примени анимации к секциям на главной:
   - Каждая секция fade-in при появлении
   - CSS классы:
````css
     .fade-in-section {
       opacity: 0;
       transform: translateY(20px);
       transition: opacity 0.6s ease-out, transform 0.6s ease-out;
     }
     
     .fade-in-section.is-visible {
       opacity: 1;
       transform: translateY(0);
     }
````

3. Плавный скролл:
   - В globals.css добавь:
````css
     html {
       scroll-behavior: smooth;
     }
````
   - Стрелка вниз в Hero секции скроллит к первой секции:
````tsx
     <button onClick={() => window.scrollTo({ top: window.innerHeight, behavior: 'smooth' })}>
       ↓
     </button>
````

4. Кнопка "Наверх":
   - Создай компонент ScrollToTop:
   - Появляется после скролла > 500px
   - Фиксированная позиция: bottom-8 right-8
   - Круглая кнопка с иконкой ↑
   - Fade-in анимация появления
   - При клике - smooth scroll наверх
   - Добавь в layout.tsx

5. Loading скелетоны:
   - Создай компонент Skeleton:
````tsx
     export const Skeleton = ({ className }: { className?: string }) => (
       <div className={`animate-pulse bg-darkCard/50 rounded ${className}`} />
     );
````
   - Используй для:
     • Каталога пока данные загружаются
     • Карточек эффектов
     • Статистики

6. Анимация чисел в Hero и статистике:
   - Создай хук useCountUp:
````typescript
     export const useCountUp = (end: number, duration = 2000) => {
       const [count, setCount] = useState(0);
       
       useEffect(() => {
         let start = 0;
         const increment = end / (duration / 16);
         
         const timer = setInterval(() => {
           start += increment;
           if (start >= end) {
             setCount(end);
             clearInterval(timer);
           } else {
             setCount(Math.floor(start));
           }
         }, 16);
         
         return () => clearInterval(timer);
       }, [end, duration]);
       
       return count;
     };
````

7. Transition для всех ссылок и кнопок:
   - Добавь в tailwind.config.ts:
````typescript
     theme: {
       extend: {
         transitionProperty: {
           'height': 'height',
           'spacing': 'margin, padding',
         }
       }
     }
````
   - Используй transition-all duration-300 на интерактивных элементах

8. Reduced motion:
   - В globals.css добавь:
````css
     @media (prefers-reduced-motion: reduce) {
       *,
       *::before,
       *::after {
         animation-duration: 0.01ms !important;
         animation-iteration-count: 1 !important;
         transition-duration: 0.01ms !important;
       }
     }
````

Сделай все переходы плавными и приятными!

ПРОМТ 23: SEO оптимизация
Добавь SEO оптимизацию для всех страниц проекта.

Требования:

1. В каждой странице добавь metadata:

   app/page.tsx:
````typescript
   export const metadata = {
     title: 'Эффект Манделы - Исследуй различия в восприятии',
     description: 'Все помнят по-разному. Исследуй эффект Манделы - феномен ложных воспоминаний. Нет правильных ответов - есть разные восприятия.',
     keywords: 'эффект манделы, ложные воспоминания, память, восприятие, коллективная память',
     openGraph: {
       title: 'Эффект Манделы',
       description: 'Исследуй различия в восприятии и памяти',
       type: 'website',
       url: 'https://yourdomain.com',
     },
   };
````

   app/catalog/page.tsx:
````typescript
   export const metadata = {
     title: 'Каталог эффектов Манделы - 150+ примеров',
     description: 'Полный каталог эффектов Манделы по категориям. Фильмы, музыка, бренды и многое другое.',
   };
````

   app/effect/[id]/page.tsx:
````typescript
   export async function generateMetadata({ params }) {
     const effect = getEffectById(params.id);
     return {
       title: `${effect.title} - Эффект Манделы`,
       description: effect.question,
     };
   }
````

   app/my-memory/page.tsx:
````typescript
   export const metadata = {
     title: 'Моя карта памяти - Эффект Манделы',
     description: 'Твоя личная статистика и паттерны восприятия',
   };
````

   app/stats/page.tsx:
````typescript
   export const metadata = {
     title: 'Статистика - Эффект Манделы',
     description: 'Общая статистика проекта, самые спорные и популярные эффекты',
   };
````

   app/submit/page.tsx:
````typescript
   export const metadata = {
     title: 'Предложить эффект - Эффект Манделы',
     description: 'Помоги расширить коллекцию эффектов Манделы',
   };
````

2. Создай файл public/robots.txt:
User-agent: *
Allow: /
Disallow: /api/
Sitemap: https://yourdomain.com/sitemap.xml

3. Создай sitemap (app/sitemap.ts):
````typescript
   import { MetadataRoute } from 'next';
   import effectsData from '@/data/effects.json';
   
   export default function sitemap(): MetadataRoute.Sitemap {
     const baseUrl = 'https://yourdomain.com';
     
     const effects = effectsData.map((effect) => ({
       url: `${baseUrl}/effect/${effect.id}`,
       lastModified: effect.dateAdded,
       changeFrequency: 'weekly' as const,
       priority: 0.8,
     }));
     
     return [
       {
         url: baseUrl,
         lastModified: new Date(),
         changeFrequency: 'daily',
         priority: 1,
       },
       {
         url: `${baseUrl}/catalog`,
         lastModified: new Date(),
         changeFrequency: 'daily',
         priority: 0.9,
       },
       {
         url: `${baseUrl}/stats`,
         lastModified: new Date(),
         changeFrequency: 'weekly',
         priority: 0.7,
       },
       {
         url: `${baseUrl}/submit`,
         lastModified: new Date(),
         changeFrequency: 'monthly',
         priority: 0.6,
       },
       ...effects,
     ];
   }
````

4. Структурированные данные (JSON-LD):
   В app/layout.tsx добавь в head:
````tsx
   <script
     type="application/ld+json"
     dangerouslySetInnerHTML={{
       __html: JSON.stringify({
         '@context': 'https://schema.org',
         '@type': 'WebSite',
         name: 'Эффект Манделы',
         description: 'Исследование различий в восприятии и памяти',
         url: 'https://yourdomain.com',
       }),
     }}
   />
````

5. Canonical URLs:
   В metadata каждой страницы добавь:
````typescript
   alternates: {
     canonical: 'https://yourdomain.com/page-url',
   }
````

Сделай сайт SEO-friendly!

ПРОМТ 24: Адаптивность и accessibility
Улучши адаптивность и доступность сайта.

Требования:

1. Responsive breakpoints - проверь все страницы:
   - Mobile: 320px, 375px, 425px
   - Tablet: 768px
   - Desktop: 1024px, 1440px
   - Используй Chrome DevTools для тестирования

2. Semantic HTML:
   - Убедись, что используешь правильные теги:
     • <header>, <nav>, <main>, <section>, <article>, <footer>
     • <h1> → <h2> → <h3> (иерархия заголовков)
     • <button> для кнопок, <a> для ссылок
     • <ul>/<ol> для списков

3. ARIA атрибуты:
   - Добавь aria-label для кнопок без текста:
````tsx
     <button aria-label="Закрыть меню">✕</button>
     <button aria-label="Предыдущий слайд">←</button>
````
   - aria-describedby для подсказок:
````tsx
     <input aria-describedby="hint-text" />
     <p id="hint-text">Подсказка</p>
````
   - aria-live для динамического контента:
````tsx
     <div aria-live="polite" aria-atomic="true">
       {voteCount} голосов
     </div>
````

4. Keyboard navigation:
   - Протестируй навигацию только клавиатурой (Tab)
   - Focus states должны быть видимы:
````css
     *:focus-visible {
       outline: 2px solid #3b82f6;
       outline-offset: 2px;
     }
````
   - Добавь в modals:
     • Escape закрывает
     • Tab циклится внутри modal
     • Focus trap

5. Цветовой контраст:
   - Проверь все комбинации цветов
   - Минимум 4.5:1 для обычного текста
   - Минимум 3:1 для крупного текста (18px+)
   - Используй инструмент: https://webaim.org/resources/contrastchecker/
   - Если нужно - подкорректируй цвета

6. Skip to content link:
   - Добавь в начало body (layout.tsx):
````tsx
     <a href="#main-content" className="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 bg-primary text-white px-4 py-2 rounded z-50">
       Перейти к содержимому
     </a>
````
   - Добавь id="main-content" к main элементу

7. Form accessibility:
   - Все inputs должны иметь labels:
````tsx
     <label htmlFor="email">Email</label>
     <input id="email" type="email" />
````
   - Используй aria-invalid для ошибок:
````tsx
     <input aria-invalid={!!error} />
````
   - Сообщения об ошибках связаны с полями

8. Alt text (если будут изображения):
   - Все img должны иметь alt
   - Декоративные: alt=""
   - Информативные: описательный alt

9. Touch targets:
   - Минимум 44x44px для всех кликабельных элементов
   - Проверь кнопки на mobile
   - Увеличь padding если нужно

10. Reduced motion (уже добавлено в предыдущем промте):
    - Убедись, что работает

11. Screen reader тестирование:
    - Используй NVDA (Windows) или VoiceOver (Mac)
    - Проверь навигацию
    - Убедись, что всё читается логично

12. Lighthouse проверка:
    - Запусти Lighthouse в DevTools
    - Цели:
      • Performance: 90+
      • Accessibility: 100
      • Best Practices: 100
      • SEO: 100
    - Исправь найденные проблемы

Сделай сайт доступным для всех!

ПРОМТ 25: Оптимизация производительности
Оптимизируй проект для максимальной производительности.

Требования:

1. Code splitting:
   - Используй dynamic imports для тяжёлых компонентов:
````tsx
     import dynamic from 'next/dynamic';
     
     const HeavyChart = dynamic(() => import('@/components/HeavyChart'), {
       loading: () => <Skeleton className="w-full h-64" />,
       ssr: false, // если компонент только для клиента
     });
````
   - Примени к:
     • Карусели
     • Графикам/диаграммам
     • Модальным окнам

2. Мемоизация компонентов:
   - Оберни списки в React.memo:
````tsx
     const EffectCard = React.memo(({ effect }: { effect: Effect }) => {
       // ...
     });
````
   - Используй useMemo для тяжёлых вычислений:
````tsx
     const sortedEffects = useMemo(() => {
       return effects.sort((a, b) => {
         // сортировка
       });
     }, [effects, sortOrder]);
````
   - Используй useCallback для функций в props:
````tsx
     const handleVote = useCallback((variant: 'A' | 'B') => {
       // ...
     }, [effectId]);
````

3. Image optimization (если будут изображения):
   - Используй Next.js Image component
   - Всегда указывай width и height
   - Используй priority для hero изображений
   - Форматы: WebP с fallback на JPG/PNG

4. Font optimization:
   - В layout.tsx убедись, что шрифт загружается правильно:
````tsx
     import { Inter } from 'next/font/google';
     
     const inter = Inter({ 
       subsets: ['latin', 'cyrillic'],
       display: 'swap',
     });
````

5. Bundle analysis:
   - Установи:
     npm install @next/bundle-analyzer
   - В next.config.js:
````js
     const withBundleAnalyzer = require('@next/bundle-analyzer')({
       enabled: process.env.ANALYZE === 'true',
     });
     
     module.exports = withBundleAnalyzer({
       // your config
     });
````
   - Запусти: ANALYZE=true npm run build
   - Проверь большие зависимости и оптимизируй

6. Data fetching optimization:
   - effects.json может стать большим
   - Рассмотри pagination для каталога:
     • Показывай 12 эффектов
     • Load more при скролле
   - Используй Server Components где возможно

7. Debounce и throttle:
   - Поиск: debounce 300ms
````tsx
     import { useDebouncedValue } from '@/hooks/useDebounce';
     
     const debouncedSearch = useDebouncedValue(search, 300);
````
   - Scroll events: throttle 100ms
   - Resize events: throttle 150ms

8. localStorage optimization:
   - Не читай localStorage на каждый рендер
   - Используй useState + useEffect:
````tsx
     const [votes, setVotes] = useState<Vote[]>([]);
     
     useEffect(() => {
       const stored = getUserVotes();
       setVotes(stored);
     }, []);
````

9. Reduce JavaScript:
   - Минимизируй использование тяжёлых библиотек
   - Проверь, что используешь только нужные части
   - Tree-shaking должен работать

10. Caching:
    - Добавь headers для статики:
````js
      // next.config.js
      async headers() {
        return [
          {
            source: '/:all*(svg|jpg|png|webp)',
            locale: false,
            headers: [
              {
                key: 'Cache-Control',
                value: 'public, max-age=31536000, immutable',
              }
            ],
          },
        ];
      }
````

11. Prefetching:
    - Next.js Link автоматически делает prefetch
    - Для API можно добавить prefetch в hover:
````tsx
      <Link 
        href={`/effect/${id}`}
        onMouseEnter={() => {
          // prefetch data
        }}
      >
````

12. Проверка производительности:
    - npm run build
    - Проверь размеры бандлов
    - First Load JS должен быть < 200kB
    - Запусти Lighthouse
    - Цель: Performance score 90+

Сделай сайт быстрым!

ПРОМТ 26: Подготовка к деплою и README
Финальная подготовка проекта к публикации.

Требования:

1. Создай README.md в корне проекта:
````markdown
   # 🧠 Эффект Манделы
   
   Исследование различий в восприятии и коллективной памяти.
   
   ## 📖 О проекте
   
   Эффект Манделы - это феномен ложных воспоминаний, когда множество людей помнят события или детали иначе, чем они есть на самом деле. Этот проект исследует, как по-разному люди помнят одно и то же.
   
   **Важно:** Нет правильных ответов - есть разные восприятия.
   
   ## ✨ Возможности
   
   - 📚 Каталог 150+ эффектов Манделы по категориям
   - 🗳️ Голосование и статистика в реальном времени
   - 📊 Личная карта памяти и паттерны восприятия
   - ➕ Предложение новых эффектов
   - 📈 Общая статистика и аналитика
   
   ## 🛠 Технологии
   
   - **Next.js 14** - React framework
   - **TypeScript** - Type safety
   - **Tailwind CSS** - Styling
   - **JSON файлы** - Data storage (без БД)
   
   ## 🚀 Установка и запуск
```bash
   # Клонировать репозиторий
   git clone https://github.com/yourusername/mandela-effect.git
   cd mandela-effect
   
   # Установить зависимости
   npm install
   
   # Запустить dev сервер
   npm run dev
```
   
   Откройте [http://localhost:3000](http://localhost:3000) в браузере.
   
   ## 📁 Структура проекта
```
   mandela-effect/
   ├── app/                  # Next.js App Router
   │   ├── page.tsx         # Главная страница
   │   ├── catalog/         # Каталог эффектов
   │   ├── effect/[id]/     # Страница эффекта
   │   ├── my-memory/       # Личная статистика
   │   ├── stats/           # Общая статистика
   │   ├── submit/          # Форма предложения
   │   └── api/             # API routes
   ├── components/          # React компоненты
   ├── data/                # JSON файлы с данными
   │   ├── effects.json     # Основные эффекты
   │   └── submissions.json # Предложенные эффекты
   ├── lib/                 # Утилиты
   └── public/              # Статические файлы
```
   
   ## 🌐 Деплой
   
   ### Vercel (рекомендуется)
   
   1. Создайте аккаунт на [vercel.com](https://vercel.com)
   2. Подключите GitHub репозиторий
   3. Deploy!
   
   ### Другие платформы
   
   Проект совместим с любыми платформами, поддерживающими Next.js:
   - Netlify
   - Railway
   - Render
   
   ## 🔧 Конфигурация
   
   Создайте файл `.env.local`:
```env
   NEXT_PUBLIC_SITE_URL=http://localhost:3000
```
   
   ## 📝 Лицензия
   
   MIT License - свободно используйте в личных и коммерческих проектах.
   
   ## 🤝 Контрибьюция
   
   Pull requests приветствуются! Для больших изменений сначала откройте issue.
   
   ## 📧 Контакты
   
   - Email: your.email@example.com
   - GitHub: [@yourusername](https://github.com/yourusername)
   
   ---
   
   Создано с ❤️ • 2024
````

2. Создай .env.example:
````env
   # Site URL (for production)
   NEXT_PUBLIC_SITE_URL=https://yourdomain.com
````

3. Обнови .gitignore:
dependencies
/node_modules
next.js
/.next/
/out/
production
/build
misc
.DS_Store
*.pem
debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
local env files
.env*.local
.env
vercel
.vercel
typescript
*.tsbuildinfo
next-env.d.ts

4. Проверь package.json scripts:
````json
   "scripts": {
     "dev": "next dev",
     "build": "next build",
     "start": "next start",
     "lint": "next lint",
     "type-check": "tsc --noEmit"
   }
````

5. Pre-deploy checklist:
   - [ ] npm run build проходит без ошибок
   - [ ] npm run lint без warnings
   - [ ] Все страницы открываются
   - [ ] Голосование работает
   - [ ] localStorage сохраняется
   - [ ] Мобильная версия работает
   - [ ] Форма отправки работает
   - [ ] API endpoints отвечают
   - [ ] Lighthouse score > 90

6. Деплой на Vercel:
````bash
   # Установи Vercel CLI (опционально)
   npm i -g vercel
   
   # Залогинься
   vercel login
   
   # Deploy
   vercel
   
   # Или просто пуш в GitHub и импортируй на vercel.com
````

7. После деплоя:
   - Проверь все страницы на production
   - Настрой custom domain (если есть)
   - Добавь Google Analytics (опционально)
   - Настрой error monitoring (Sentry)

8. Создай CHANGELOG.md:
````markdown
   # Changelog
   
   ## [1.0.0] - 2024-XX-XX
   
   ### Added
   - Каталог эффектов с фильтрами
   - Система голосования
   - Личная карта памяти
   - Общая статистика
   - Форма предложения эффектов
   - Адаптивный дизайн
   - SEO оптимизация
````

Проект готов к запуску! 🚀

БОНУС ПРОМТ 27: Advanced фичи (опционально)
Если базовый функционал работает идеально, добавь продвинутые фичи.

Выбери одну или несколько:

1. КВИЗ РЕЖИМ (/quiz):
   - Создай страницу app/quiz/page.tsx
   - 10 случайных эффектов подряд
   - Таймер на каждый вопрос (30 секунд)
   - Прогресс-бар (вопрос X из 10)
   - В конце:
     • Результаты (X из 10 с большинством)
     • Сравнение: "Ты более уникален чем Y% участников"
     • Кнопка "Поделиться результатом"
   - Сохранить результат в localStorage

2. DARK/LIGHT РЕЖИМ:
   - Toggle переключатель в header
   - Используй next-themes:
     npm install next-themes
   - Создай два набора цветов в tailwind.config
   - Сохранение выбора в localStorage
   - Respect prefers-color-scheme

3. SOCIAL SHARING:
   - Кнопки "Поделиться" на странице эффекта
   - Twitter, Facebook, WhatsApp, Copy link
   - Генерируй красивые OG images динамически
   - Используй @vercel/og или puppeteer

4. КОММЕНТАРИИ:
   - Добавь секцию комментариев на странице эффекта
   - Храни в отдельном JSON файле
   - Модерация (approve/reject)
   - Simple авторизация (имя + email)

5. ДОСТИЖЕНИЯ/GAMIFICATION:
   - Система бейджей:
     • "Новичок" - 1 голос
     • "Исследователь" - 10 голосов
     • "Эксперт" - 50 голосов
     • "Уникум" - 70%+ в меньшинстве
   - Показывать на странице /my-memory
   - Анимация получения бейджа

6. РАСШИРЕННАЯ СТАТИСТИКА:
   - Графики Chart.js или Recharts
   - Активность по дням
   - Распределение по времени суток
   - Тенденции (какие эффекты набирают популярность)

7. ПОИСК С АВТОДОПОЛНЕНИЕМ:
   - В header глобальный поиск
   - Dropdown с подсказками при вводе
   - Поиск по названию, вопросу, категории
   - Горячие клавиши (Cmd+K / Ctrl+K)

Выбери что интересно и реализуй!

🎯 Как использовать эти промты:

Копируй по одному промту в Cursor AI
Жди завершения выполнения
Проверяй результат в браузере (npm run dev)
Если есть ошибки - копируй текст ошибки и скажи AI: "Исправь эту ошибку"
Переходи к следующему промту

📝 Важные команды:
bash# Создание проекта (ПРОМТ 1)
npx create-next-app@latest mandela-effect --typescript --tailwind --app --no-src-dir

# Запуск dev сервера
npm run dev

# Установка доп. пакетов (когда понадобятся)
npm install canvas-confetti react-hot-toast embla-carousel-react

# Сборка для production
npm run build

# Запуск production
npm start
✅ Чеклист выполнения:

 ПРОМТ 1: Создание проекта ✅
 ПРОМТ 2: Файл данных ✅
 ПРОМТ 3-9: Главная страница ✅
 ПРОМТ 10-12: Страница эффекта + API ✅
 ПРОМТ 13-14: Моя память ✅
 ПРОМТ 15: Статистика ✅
 ПРОМТ 16-17: Форма отправки ✅
 ПРОМТ 18: Обновление localStorage ✅
 ПРОМТ 19-21: Секции на главной ✅
 ПРОМТ 22: Анимации ✅
 ПРОМТ 23: SEO ✅
 ПРОМТ 24: A11y ✅
 ПРОМТ 25: Оптимизация ✅
 ПРОМТ 26: Деплой ✅
 ПРОМТ 27: Бонус (опционально) 🎁

Готово! Теперь у тебя есть полный набор готовых промтов. Начинай с ПРОМТА 1! 🚀