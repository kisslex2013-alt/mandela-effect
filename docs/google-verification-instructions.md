# 🔐 Инструкция: Подтверждение права собственности Google Search Console

## 📋 Пошаговая инструкция

### Шаг 1: Скачайте HTML файл
1. В интерфейсе Google Search Console нажмите кнопку **"Скачать"** рядом с файлом `google5a37637f8e2ca5f1.html`
2. Сохраните файл на компьютер

### Шаг 2: Поместите файл в проект
1. Скопируйте скачанный файл `google5a37637f8e2ca5f1.html`
2. Вставьте его в папку `public/` вашего проекта:
   ```
   Project_002/
   └── public/
       └── google5a37637f8e2ca5f1.html  ← сюда
   ```

### Шаг 3: Проверьте содержимое файла
Файл должен содержать что-то вроде:
```html
google-site-verification: google5a37637f8e2ca5f1.html
```
или просто строку с кодом верификации.

### Шаг 4: Зафиксируйте в Git
```bash
git add public/google5a37637f8e2ca5f1.html
git commit -m "Add Google Search Console verification file"
git push
```

### Шаг 5: Деплой
После деплоя на Vercel файл будет доступен по адресу:
```
https://www.mandela-effect.ru/google5a37637f8e2ca5f1.html
```

### Шаг 6: Подтвердите в Google Search Console
1. Вернитесь в интерфейс подтверждения
2. Нажмите кнопку **"ПОДТВЕРДИТЬ"**
3. Google проверит наличие файла по указанному пути

---

## ✅ Важно!

1. **Не удаляйте файл** — он должен оставаться на сайте постоянно для сохранения верификации
2. **Файл должен быть в Git** — чтобы он не удалялся при пересборке проекта
3. **Файл должен быть доступен публично** — без авторизации

---

## 🔍 Проверка доступности

После деплоя проверьте, что файл доступен:
- Откройте: `https://www.mandela-effect.ru/google5a37637f8e2ca5f1.html`
- Должен открыться HTML файл с кодом верификации

---

## 📝 Что происходит автоматически

✅ **Next.js автоматически:**
- Делает файлы из `public/` доступными по корневому пути
- Включает их в деплой на Vercel
- Сохраняет их при всех пересборках

✅ **Git автоматически:**
- Отслеживает файл (если он добавлен)
- Включает его во все коммиты
- Сохраняет его в репозитории

---

## ⚠️ Если файл не найден после деплоя

1. Проверьте, что файл находится именно в `public/`
2. Проверьте, что файл закоммичен в Git
3. Проверьте, что после деплоя файл доступен по прямому URL
4. Убедитесь, что имя файла точно совпадает (регистр важен!)

